# Reusable implementation prompt

Act as the implementation owner in this task, in the existing CaseChain checkout. Keep my selected model/reasoning. Follow `AGENTS.md`, the current handoff/ledger and `docs/plans/operations/2026-09-08-agent-delivery-workflow.md`. Read that workflow's **Current bounded run** first for the objective and queue; read other plans/receipts only for the selected outcome and necessary dependencies. The October assessment is context, not a required startup read or an approved release plan.

Create or continue a matching durable goal for that bounded objective, confirming actual activation. Do not reuse an unmatched broad portfolio goal, create one goal per tranche, set an unrequested token budget, or call a partial result complete. If goal controls are unavailable, disclose that and continue authorised work without promising automatic continuation.

Use bounded subagents according to the workflow: one active code writer, independently reviewed consequential work, stable revisions for QA and consolidated repairs. Do trivial changes directly. Continue ready items within the current queue without routine confirmation; when one is blocked, inspect its dependencies and the other queued work, not the whole portfolio.

You may install ordinary project test dependencies, implement the test runner, repair verified defects in the scoped approved consumers, and commit owned/reviewed changes locally. You may create/reset disposable local Supabase/Docker fixtures and exercise synthetic auth, local captured mail, local Storage and browser tests after confirming all services/jobs/destinations remain local. Pre-pilot legacy test data is disposable; avoid compatibility solely for it. Keep real application security and source-integrity contracts.

Preserve unrelated changes, including documentation, UI concepts and `.obsidian` settings. Use current unstaged instructions in this checkout; do not bundle all dirty files into a commit. Do not create another coordinator/worktree, push, deploy, change remote data/DNS/SMTP, send real email, use paid providers or restart another task without separate existing authority.

Start with the first executable queued outcome. Record actual code/test/reviewer evidence, repairs and remaining gaps. Finish the bounded goal when its scoped acceptance passes; if impeded, preserve concrete blockers under runtime goal rules. Do not expand into new feature domains just to keep running.
