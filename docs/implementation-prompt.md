# Reusable implementation prompt

Act as the implementation owner in this task, in the existing CaseChain checkout. Keep my selected model/reasoning. Follow `AGENTS.md` and `docs/plans/operations/2026-09-08-agent-delivery-workflow.md`. Reconcile `docs/implementation-handoff.md`, the ledger's active card, current decisions and Git state before choosing work. Read other plans/receipts only for the selected outcome and necessary dependencies.

Use my latest explicit objective; otherwise resume the recorded unfinished authorised objective. Do not replay a completed historical queue. If I explicitly delegate selection of the next outcome, choose one ready approved outcome within that direction, state its scope and acceptance, and proceed without routine confirmation. Otherwise, when no unfinished authorised objective remains, recommend a concrete next scope for selection. Verify the previous writer has stopped before taking ownership. Record the current objective, exclusions and next action in the handoff/active card, not as a permanent dated queue in this prompt.

Create or continue one matching durable goal for the selected outcome-sized packet, confirming actual activation. Do not reuse an unmatched broad portfolio goal, split one observable journey into multiple goals, set an unrequested token budget, or call a partial result complete. If goal controls are unavailable, disclose that and continue authorised work without promising automatic continuation.

Use bounded subagents according to the workflow: normally one implementation writer and one independent read-only QA agent, never more than one active code writer. Spawn helpers without inherited conversation history and give them compact, self-contained briefs; use limited recent history only when an essential user decision exists solely there. Helpers do not delegate or commit. Do trivial changes directly. Complete exactly one outcome-sized packet, record its evidence and stop; recommend the next packet without starting it. When the selected packet is blocked, inspect only its dependencies and approved alternatives within the user-selected objective.

You may implement approved features and necessary repairs within that objective, install ordinary project test dependencies, extend the existing acceptance runner as needed, and commit owned/reviewed changes locally. You may create/reset disposable local Supabase/Docker fixtures and exercise synthetic auth, local captured mail, local Storage and browser tests after confirming all services/jobs/destinations remain local. Apply the current data phase: pre-pilot legacy test data is disposable; real-client onboarding activates preservation requirements. Keep real application security and source-integrity contracts.

Preserve unrelated changes, including documentation, UI concepts and `.obsidian` settings. Use current unstaged instructions in this checkout; do not bundle all dirty files into a commit. Do not create another coordinator/worktree, push, deploy, change remote data/DNS/SMTP, send real email, use paid providers or restart another task without separate existing authority.

Before implementation, state the packet's observable user outcome, release or objective contribution, dependencies and evidence, inclusions, exclusions, acceptance, verification, unresolved gates, model allocation and qualitative cost. Record actual code/test/reviewer evidence, repairs and remaining gaps. Finish the bounded goal when its scoped acceptance passes; if impeded, preserve concrete blockers under runtime goal rules. Do not expand into new feature domains just to keep running.
