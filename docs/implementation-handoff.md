# Implementation handoff

Updated: 2026-09-15. D06-T14 is locally integrated at exact accepted revision `8e38b9bbb51bfc1571db4041afa894ba575a4a0b`; evidence is [D06-T14](delivery-evidence/D06-T14.md). The checkout was clean before tracker/evidence recording.

- **Current objective / next action:** D06-T14 is complete. Ordinary ready global Intake placement now commits canonically and opens the exact returned Workbench source and assigned Matter. Under the user's continuous goal-mode direction, immediately reconcile and start the earliest ready remaining mandatory journey packet; prefer typed ambiguous-placement or possible-duplicate Review when its producer/dependencies are executable locally, otherwise advance the next independent mandatory deadline or Team closure. Do not start conditional retrieval, relationship suggestions, Tasks/reminders or Financials first.
- **Branch / implementation:** `dev`; implementation `8e38b9b`. D08-T03 `e6f8f9d`/`3eed22b`/`e11e292`, D03-T08 `2afcbeb`/`9cf298c` and D11-T10 `c68ac8a`/`ac49028` are ancestors; do not restart them.
- **Delivered D06-T14:** Owner/Admin/Associate can assign a ready global Intake item to an accessible lifecycle-active Matter even when its procedural status is `tribunal`. The unchanged secure database command fences tenant, role, uploader, stale/duplicate/replay and lifecycle state, materialises one document/current version/assignment/Activity/processing intent without copying bytes, and returns the exact immutable version. The Hub navigates to document + Matter + returned version + page 1, renders the private PDF, and returns to the assigned Matter without a misleading post-commit refresh.
- **Verification:** one Terra/medium writer completed two bounded repairs. Coordinator clean-replayed all 158 migrations and the full local SQL/concurrency harness, seeded private synthetic Storage, passed 13/13 focused tests, TypeScript, scoped lint and diff hygiene, then completed the real Chromium journey in 15.6s. Independent Luna/high exact-commit QA passed migration/static/type/lint gates and found no P0–P3; its sandbox could not relaunch Supabase/browser because telemetry attempted a disallowed home write, so the coordinator runtime evidence remains decisive.
- **Relevant completed dependencies:** D03-T07 provides verified-email create-or-join; D04-T03 proves global and Matter signed upload locally; D06-T13 supplies shared Intake; D08-T02 supplies the canonical extraction-conflict Review foundation; D05-T01 supports first attachment; D06-T12 proves exact private-PDF Workbench/Matter reopening; D11-T07–T10 cover read-only graph, governed manual relationships and supporting-file authority. Reconcile their receipts against changed code before reuse.
- **Remaining mandatory product work:** ordinary source-grounded extraction must still meet the acquisition/quality gates; ambiguous placement and possible-duplicate typed Review remain open; manually verified deadlines, minimum Team invitation/suspension closure, schedules/workers and release-wide acceptance remain unfinished. Conditional features remain behind mandatory closure.
- **Production gate:** confidential upload/deployment remains blocked by exact environment/revision identity, named go-live/rollback/support/incident owners, compliant hosting, tested database-plus-private-Storage recovery, provider/configuration acceptance, monitoring/rollback and exact deployed rehearsal. No remote service, production data, paid provider, scheduler or real email was changed in D06-T14.
- **Local environment:** the repository-owned disposable local `dms` stack was reset through migration `00158`, its SQL/concurrency harness passed, and private synthetic browser fixtures remain reproducible. The focused browser assignment consumed the seeded Intake as expected. No production or remote state changed.
- **Processes / ownership:** writer and QA helpers are stopped; there is no active code writer. Tracker/evidence files are the only post-implementation changes expected before the evidence commit. Preserve unrelated work, complete the current durable goal, then create the next packet goal under the user's continuous direction.
