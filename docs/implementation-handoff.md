# Implementation handoff

Updated: 2026-09-08. The checkout remains in a single-writer, uncommitted concept-review checkpoint.

- **Implementation objective:** the next implementation task uses the reusable prompt's durable goal to continue approved ready portfolio work, checking current evidence and searching the wider documentation when the preferred tranche is blocked.
- **Parked product review:** obtain visual approval for the revised Organisation Team/departure concept without representing the fixture as a live Team implementation. Preserve the concept evidence below while independent work proceeds.
- **Branch / HEAD observed during planning:** `dev` / `a762850`, with uncommitted changes. Reinspect at implementation startup; no branch switch or commit is made by this planning update.
- **Existing work:** the checkout already contains unrelated application, concept, plan, portal, and Task-reader edits. Preserve and identify their ownership before staging anything. This planning task owns only its documentation updates and any required portal registration for the workflow plan.
- **Last verified implementation:** against `a762850` plus the current dirty `OrganisationDepartureTeamConcept.tsx`, the Team fixture now matches the approved progressive-disclosure workspace: every desktop row is keyboard/pointer selectable, selected mode drops repeated columns, and the aligned 60/40 inspector keeps one width with a fixed identity header, independent body scroller, and fixed action footer. Members and Departure queue share one compact 48px desktop row contract. The full member table uses stable column proportions and a short name-only Person index; selected mode removes Joined and the inspector carries the full work area, email, and joined date. Departure timing is kept to one inline table value, with secondary identity detail in the inspector. Mobile retains larger touch-friendly list rows. The table keeps the same explicit 12px left collection gutter before and after the inspector opens, so its edge remains anchored while its width changes; in the normal full-table state, Invite member anchors the toolbar's far-right edge. The inspector body uses a compact scan-first hierarchy rather than nested cards: a lightweight departure marker, member details, one two-area work handoff summary, the actual teammate note, and a dense four-field access grid. Mobile uses list-to-full-detail navigation with its own Back/identity header and footer. Team list and member detail were visually exercised at desktop and a true 320px emulated viewport; document/body scroll widths match the viewport. `Before leaving` and `My departure` now share one 3:2 desktop alignment grid: summary and timing cards form the first aligned row, while work and teammate-context cards form the second. They collapse to the same single-column mobile reading order with no horizontal overflow. `My departure` has no duplicate secondary header: its desktop request/withdraw actions sit in the departure summary while mobile retains the fixed bottom action bar. Shared Review items do not appear in member impact or reassignment controls; departure releases only a temporary Review claim. Scoped ESLint and whitespace checks pass; the browser console reported no errors. This is current-tree concept evidence, not a production Team reader or mutation.
- **Known failures:** the 2026-09-08 current-tree `npx tsc --noEmit --pretty false` check still fails in `src/components/tasks/task-model.test.ts`, `src/lib/supabase/provider-usage-rpc.type-test.ts`, and `src/lib/supabase/task-rpc-nullability.type-test.ts`; none of its diagnostics point to the Organisation concept. The historical worker/native build boundary, broad lint debt, and unavailable Docker SQL acceptance still require independent rechecking rather than being assumed current or cleared.
- **Decisions:** first PDF attachment is in scope; replacement UI is deferred; metadata similarity cannot merge records; bounded direct resumable upload is the chosen transport direction; one implementation task, no parallel worktrees. See canonical plans for acceptance details.
- **Open dependencies:** see [decision queue](approval-based-blockers.md), especially Review visual approval, OCR thresholds and pilot retention scope. These do not block independent build, authorization or source-state repairs.
- **Next executable outcome:** reconcile the current build/type failures under D01 and select its smallest unresolved repair; if current evidence already clears it, choose the next ready approved outcome. The parked `ORG-DEPARTURE-TEAM-CONCEPT-2026-09-01` review does not block this work. Record its actual approval before replacing the live Team caller.
- **Planning verification:** the static portal build passed for all 19 archived plans after registering the workflow and two previously unregistered plans. Document-link/index/review-coverage and whitespace checks cover this documentation change only; they are not application build, database, browser or deployment acceptance.
- **Helpers / running processes:** this planning handoff launches no implementation helpers or app servers. A subsequent owner must record and stop/transfer its own processes before handoff.

Replace these bullets at each handoff. Keep only current state and pointers to durable verification receipts; do not accumulate a second history or copy large logs here.

## Current implementation checkpoint

HEAD is `168d69e` on `dev` before the evidence commit. D11-T04 makes exact current-version chronology the sole live Timeline until the planned relationship graph, with strict filters/totals/order/paging/off-page selection, URL and focus state, bounded selected-note preview, and exact active/Trash parity. Receipt: [D11-T04](delivery-evidence/D11-T04.md). Fifty-nine focused Matter tests, seven exact-resource regressions, migration/type/runtime checks, TypeScript, scoped lint, disposable-database rollback fixtures, diff checks, the webpack production build, and final separate Sol/high QA pass. The default Turbopack build was interrupted after bounded silent optimisation and is not claimed as passing. Authenticated browser acceptance at 320px/200%, keyboard/focus, Back/Forward, dark mode, live scroll ownership, deployed schema cache, representative scale, and release acceptance remain unavailable. D05 is blocked on `WORK-REVIEW-CONCEPT`; D09 is blocked on `MATTER-IDENTIFIER-POLICY-2026-09-08`. No main database, remote service, deployment, or production data changed; all helpers are stopped and no development server is running. Preserve the unstaged approval, decision/review, design-system, concept, portal, platform-plan, handoff-top, and proxy changes. Concrete next action: commit this evidence separately, then reconcile the next D11 graph/relationship dependency and scan all canonical plans for another approved dependency-ready tranche.
