# Implementation handoff

Updated: 2026-09-15. D08-T05 is locally integrated at exact accepted revision `7cb8cc3`/[D08-T05](delivery-evidence/D08-T05.md); D13-T03 remains an ancestor at `9f6baf1`/[D13-T03](delivery-evidence/D13-T03.md). D12-T02 remains an ancestor at `8fa70fa`/[D12-T02](delivery-evidence/D12-T02.md), D13-T02 at `f2d1465`/[D13-T02](delivery-evidence/D13-T02.md), D11-T12 at `48a1261`/[D11-T12](delivery-evidence/D11-T12.md). D11-T10 was implemented at `c68ac8a` and evidenced at `ac49028`; do not restart it.

- **Current objective / next action:** D08-T05 is closed locally after bounded repair and independent exact-commit QA. Under continuous goal direction, select one earliest ready approved release prerequisite from current code/receipts and September 14 order, favouring mandatory truthful Review/recovery/security/stabilisation before optional relationship suggestions. Multiple active verified target Matters currently abstain from the single-target conflict Review; inspect whether a provider-free typed multi-target decision is the next ready mandatory packet. Possible-duplicate calibration and conditional Search remain separate. D12 corpus/paid-provider and D13 production decisions remain approval-gated.
- **Branch / implementation:** `dev`; D08-T05 `35086d9`/`7cb8cc3` and linked receipt; D13-T03 `9f6baf1`, D12-T02 `a4797bd`/`8fa70fa`/[D12-T02](delivery-evidence/D12-T02.md), D13-T02 `f2d1465`/`3392e88`, D11-T12 `48a1261`/`2b5e558`, D06-T15 `ff25482`/`dc78d2b`, D12-T01 `ec0f2dd`/`f7d8cf8`, D03-T09 `a16b8fe`/`66b3cf0`, D13-T01 `33b6625`/`eab76d4`, D08-T04 `f4418eb`/`9132c76`, D15-T03 `0952cc1`/`6dc1654`, D11-T11 `2d8a488`/`10f72e8`, D06-T14 `8e38b9b`/`d7ef28f`, D03-T08 `2afcbeb`/`9cf298c` and D11-T10 `c68ac8a`/`ac49028` are ancestors; do not restart them.
- **Delivered D13-T02:** distinct reproducible 4-page synthetic PDF, actual native printed OIO/SCN page text and exact uploaded-byte hash, clean no-call validated metadata without Review, then competing no-call validated candidate with one typed extraction conflict, browser human SCN selection and exact page-2 Workbench, actor/key replay/stale fence and precise SQL decision/Activity counts. Existing manual recovery, both origins, placement, graph and verified manual deadline stay in the stateful run. Fixture graph classification is not an automatic product claim.
- **Delivered D12-T02:** opaque child run/lease payload, service-only tenant/current-version/active lineage/private asset source grant, exact downloaded-byte hash/size and regrants before begin/page/model, no legacy ignored direct reads/object-key payload or sensitive worker result/log fields. Transactional no-call expired historical lease recovery terminalizes nested attempt/run, adds content-free Trash lineage and closes outer; immutable call marker blocks possibly-called legacy replay. Document Copy ignores only no-call lineage, not validated AI/page evidence. Existing upload duplicate authority and typed Review remain.
- **Delivered D13-T03:** authorised exact version signer recognises trusted Storage SDK API `404` even when its local HTTP response is 400, requires body-free successful signed HEAD before releasing URL, never infers missing from generic 400/text, and strips raw Storage/DB errors from affected logs. Shared PDF viewer classifies explicit response 404 and rechecks an ambiguous old link through labelled typed exact-version renewal. The placed global-Intake object was removed only in disposable Storage: Workbench permanent missing/no Refresh, Matter chronology opens that exact record, SQL document/version/hash/assignment/closed Review survives, unaffected source opens, mobile dark/keyboard/overflow pass. Assigned Intake intentionally leaves all Upload Queue scopes under 00143; there is no stale source action or invented assigned row. No rehydration or backup claim.
- **D13-T03 verification:** Sol/medium writer's final 00166 isolated replay/error database lint/repeated generated types/production-webpack Chromium 1/1 and actual loss SQL passed after earlier non-accepted Review-message timing, local Storage HTTP-400/API-404 and assigned-queue assertion failures were diagnosed. Independent Terra/high exact `9f6baf1` QA no P0–P3, one full 1/1 Chromium in 57.3s, focused 15 source/model + 4 caller + 8 runner tests, type/lint/diff. All exclusively owned stacks/locks/temp builds and Playwright artifacts removed; ordinary local stack/live `.next` untouched.
- **Delivered D08-T05 / verification:** dedicated single-target filed-document placement Review for canonical printed self-identity candidate matching an active human-verified key in another Matter. Atomic governed Keep or fingerprint-fenced Move; exact source remains, truthful old/selected/live filing history, active target/Trash/Restore and independent-Move reevaluation, stale/tenant/Viewer/replay fences. Sol/medium writer `35086d9`; Terra/high QA found two P2 false closed-history labels, one `7cb8cc3` repair, exact re-QA no P0–P3. Isolated 168 migration/lint/types/RLS SQL and production-webpack desktop/320 dark keyboard Chromium 1/1 passed; owned stack/build removed, ordinary local untouched. Multiple active distinct target Matters abstain from a one-target decision; deliberate fault injection *after* nested Move was not run.
- **D12-T02 verification:** Sol/medium writer and independent Terra/high QA. Initial exact `a4797bd` QA found two P1 marked-replay and expired no-call orphan cases; one bounded `8fa70fa` repair and exact re-QA passed with no P0–P3. Pinned Node 24 disposable 00166 migration/error lint/repeated generated types, rollback grant/role/expiry/marker/Copy SQL, two-session Trash lock, focused tests/type/lint/diff passed. Exclusively owned stack removed; ordinary stack and unrelated edits untouched. Marked uncertain call remains non-replayable, operator recovery needed; no paid provider quality claim.
- **Verification:** one Sol/medium writer and independent Terra/high exact `f2d1465` QA no P0–P3. Writer's owned fourth stateful 165-migration/build/Chromium 1/1 passed; QA's isolated attempt retained only its initial banner despite cleanup/no reported error, so coordinator independently observed exact pinned-Node-24 165 replay, zero error-level lint, deterministic generated/refined types, production-webpack Chromium 1/1 in ~1.1 minutes and final SQL hash/source/decision/replay/stale checks. Focused runner tests 8/8, type/lint/syntax/diff and fixture byte parity pass. All exclusively owned stacks removed, normal local stack/live `.next` untouched.
- **Relevant completed dependencies:** D03-T07 verified-email create-or-join, D04-T03 both signed upload origins, D06-T13 shared Intake/D06-T14 exact ready placement, D08-T02 typed extraction Review/D08-T03 terminal manual recovery/D08-T04 ambiguous placement, D06-T12 private-PDF Workbench, D11-T11 governed manual deadlines, D12-T01 local numeric evaluator only. Reconcile receipts against changed code before reuse.
- **Remaining mandatory product work:** authorised independent acquisition/extraction real corpus and paid/provider quality; calibrated possible-duplicate Review producer if enabled, provider-free recovery/security/stabilisation prerequisites and release-wide deployed acceptance. Conditional cited Search remains behind mandatory closure. Synthetic successful and missing-source paths are wiring/recovery evidence, not ordinary throughput, model quality or restored backup evidence.
- **Production gate:** confidential upload/deployment remains blocked by exact environment/revision identity, named go-live/rollback/support/incident owners, compliant hosting, tested database-plus-private-Storage recovery, provider/configuration acceptance, monitoring/rollback and exact deployed rehearsal. Actual Trigger schedule/hosted Storage cleanup/fleet cost and paid OCR/corpus authenticity remain unverified. No remote service configuration, production data, paid provider, scheduler or real email was changed in D13-T03.
- **Local environment:** exclusively owned disposable `dms-release-journey-162` and earlier `dms-processing-source-166` stacks/data/locks and isolated build copies were removed. Ordinary `dms` stack and user-owned live Next process/`.next` were untouched. No production or remote state changed.
- **Processes / ownership:** D08-T05 writer and independent QA are stopped; no active code writer or mutable fixture owner. Root owns the receipt/tracker commit and goal closure, then one fresh ready packet under the user's continuous direction. Preserve unrelated changes and gates.
