# Implementation handoff

Updated: 2026-09-15. D15-T02 is locally integrated at exact accepted revision `f7d50668a2fb394bf28d6504d692f5e735c7849f`; evidence is [D15-T02](delivery-evidence/D15-T02.md). The checkout was clean before tracker/evidence recording.

- **Current objective / next action:** D15-T02 is complete. Team now owns governed invitation administration through explicit onboarding acceptance. Under the user's continuous goal-mode direction, reconcile and start the separate mandatory D15-T03 immediate Owner/Admin suspension packet; provider-quality extraction remains mandatory but externally gated.
- **Branch / implementation:** `dev`; implementation `9e8786a`, accepted delivery-retry repair `f7d5066`. D11-T11 `2d8a488`/`10f72e8`, D06-T14 `8e38b9b`/`d7ef28f`, D08-T03 `3eed22b`/`e11e292`, D03-T08 `2afcbeb`/`9cf298c` and D11-T10 `c68ac8a`/`ac49028` are ancestors; do not restart them.
- **Delivered D15-T02:** Owner/Admin can create Associate/Viewer invitations in Team and Owner alone can create Admin invitations; authorised users can inspect pending/terminal history, resend with token rotation and revoke. Exact membership/capability, recipient, email privacy, direct-authority, replay, revision, rate and concurrency boundaries fail closed. A verified recipient joins only through explicit onboarding acceptance. Personal Settings no longer owns invitations; local mail requires an explicit loopback capture.
- **Verification:** one Sol/medium writer completed bounded coordinator and QA repairs. Clean isolated replay through all 160 migrations, parsed DB lint, authority/privacy/lifecycle/rate/replay SQL, real concurrency, deterministic types, focused and repository contract checks, TypeScript, scoped lint and production webpack passed. Exact repaired commit Chromium passed 3/3, including actual create/captured-link/resend/token-rotation/revoke/history, 320px dark keyboard/focus/touch/overflow and forged Viewer denial. Terra/high QA found one P1 delivery-truth race at `9e8786a`; after `f7d5066` it found no P0–P3. Its sandbox could not run Docker/Node 24, so coordinator exact-commit runtime evidence is recorded.
- **Relevant completed dependencies:** D03-T07 provides verified-email create-or-join; D15-T01 provides the safe Team directory; D04-T03 proves global and Matter signed upload locally; D06-T13 supplies shared Intake; D08-T02 supplies extraction-conflict Review; D05-T01 supports first attachment; D06-T12 proves exact private-PDF Workbench/Matter reopening; D11-T11 supplies manual verified deadlines. Reconcile receipts against changed code before reuse.
- **Remaining mandatory product work:** immediate shell-wide suspension of an ordinary member; ordinary source-grounded extraction acquisition/quality gates; enabled placement/possible-duplicate typed Review; extracted deadline Review; schedules/workers and release-wide acceptance. Conditional features remain behind mandatory closure.
- **Production gate:** confidential upload/deployment remains blocked by exact environment/revision identity, named go-live/rollback/support/incident owners, compliant hosting, tested database-plus-private-Storage recovery, provider/configuration acceptance, monitoring/rollback and exact deployed rehearsal. No remote service, production data, paid provider, scheduler or real email was changed in D06-T14.
- **Local environment:** isolated `dms-team-invitations-160` stacks on loopback-only 560xx ports plus an in-memory loopback mail capture were created and removed by the packet runner. Synthetic invitation/browser data were removed; no production or remote state changed and no real email was sent.
- **Processes / ownership:** writer and QA helpers are stopped; there is no active code writer. Tracker/evidence files are the only post-implementation changes expected before the evidence commit. Preserve unrelated work, complete the current durable goal, then create the next packet goal under the user's continuous direction.
