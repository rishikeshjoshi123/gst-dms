# Implementation handoff

Updated: 2026-09-15. D08-T04 is locally integrated at exact accepted revision `f4418eb85755e74814f676258a6a5d0187f34fed`; evidence is [D08-T04](delivery-evidence/D08-T04.md). The checkout was clean before tracker/evidence recording.

- **Current objective / next action:** D08-T04 closes the mandatory global-Intake ambiguous-placement decision through exact reopening. Under continuous goal-mode direction, preserve the blocked D12 provider/corpus gate and select the next earliest ready mandatory typed Review dependency—normally possible-duplicate handling if its approved inputs are locally available—before relationship, extracted-deadline or conditional breadth.
- **Branch / implementation:** `dev`; implementation `840a597`, repair `f4418eb`. D15-T03 `0952cc1`/`6dc1654`, D15-T02 `f7d5066`/`38f57d2`, D11-T11 `2d8a488`/`10f72e8`, D06-T14 `8e38b9b`/`d7ef28f`, D08-T03 `3eed22b`/`e11e292`, D03-T08 `2afcbeb`/`9cf298c` and D11-T10 `c68ac8a`/`ac49028` are ancestors; do not restart them.
- **Delivered D08-T04:** a server-only typed handoff accepts already-validated structured placement candidates and the service-only database producer re-derives all authority and source facts. Global Intake with multiple eligible destinations enters one typed Review; exact-active Owner/Admin/Associate selection atomically materialises through the canonical assignment command, records immutable decision/Activity evidence and opens the exact Workbench version/page with Matter return. Matter-origin Intake and automatic/provider matching remain excluded.
- **Verification:** one Sol/medium writer completed one bounded repair cycle after exact-commit Terra/high QA. Exact `f4418eb` passed clean replay through 162 migrations, DB lint, deterministic types, new and legacy Review SQL, stale/source/role/replay and two-actor concurrency, TypeScript, focused tests, scoped lint and isolated production webpack. Chromium passed six Review journeys including production-boundary fixture creation, exact signed-PDF download, placement/reopening, Viewer denial, unavailable closure and responsive/accessibility states. Final independent QA found no P0–P3.
- **Relevant completed dependencies:** D03-T07 provides verified-email create-or-join; D15-T01 provides the safe Team directory; D04-T03 proves global and Matter signed upload locally; D06-T13 supplies shared Intake; D08-T02 supplies extraction-conflict Review; D05-T01 supports first attachment; D06-T12 proves exact private-PDF Workbench/Matter reopening; D11-T11 supplies manual verified deadlines. Reconcile receipts against changed code before reuse.
- **Remaining mandatory product work:** ordinary source-grounded extraction acquisition/quality gates; possible-duplicate typed Review; extracted deadline Review; schedules/workers and release-wide acceptance. Conditional features remain behind mandatory closure.
- **Production gate:** confidential upload/deployment remains blocked by exact environment/revision identity, named go-live/rollback/support/incident owners, compliant hosting, tested database-plus-private-Storage recovery, provider/configuration acceptance, monitoring/rollback and exact deployed rehearsal. No remote service, production data, paid provider, scheduler or real email was changed in D08-T04.
- **Local environment:** isolated `dms-review-153` stacks on loopback-only 553xx ports, synthetic records/private PDFs and a loopback production-webpack server were created and removed. The user-owned live Next process, `.next/lock` and local `dms` Supabase stack were preserved. No production or remote state changed and no email was sent.
- **Processes / ownership:** writer and QA helpers are stopped; there is no active code writer. Tracker/evidence files are the only post-implementation changes expected before the evidence commit. Preserve unrelated work, complete the current durable goal, then create the next packet goal under the user's continuous direction.
