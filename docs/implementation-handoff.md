# Implementation handoff

Updated: 2026-09-15. D11-T11 is locally integrated at exact accepted revision `2d8a488f6254050c73e0235aaaafb812ed46d08c`; evidence is [D11-T11](delivery-evidence/D11-T11.md). The checkout was clean before tracker/evidence recording.

- **Current objective / next action:** D11-T11 is complete. The Matter now has governed manually verified legal deadlines with truthful temporal and completion history. Under the user's continuous goal-mode direction, reconcile and start the earliest ready mandatory administration or extraction packet before conditional work; the minimum Team invitation/suspension closure is the strongest locally executable candidate while provider-quality execution remains externally gated.
- **Branch / implementation:** `dev`; implementation `d7f7365`, accepted authority/backfill repair `2d8a488`. D06-T14 `8e38b9b`/`d7ef28f`, D08-T03 `3eed22b`/`e11e292`, D03-T08 `2afcbeb`/`9cf298c` and D11-T10 `c68ac8a`/`ac49028` are ancestors; do not restart them.
- **Delivered D11-T11:** Owner/Admin/Associate can create, reason-amend, satisfy and cancel an explicitly manual-basis verified legal deadline. The bounded Matter agenda derives Upcoming/Due soon/Due today/Missed in the organisation timezone, preserves every content/outcome revision and gives Viewer read-only access. Exact membership, tenant, role, active Client/Matter, Trash, input, replay and concurrency boundaries fail closed; direct authenticated deadline tables are unavailable. Legacy data remains provisional.
- **Verification:** one Sol/medium writer completed two bounded repair rounds. Clean isolated replay through all 159 migrations, parsed DB lint, adversarial legacy backfill, authority/replay/timezone/concurrency SQL, deterministic types, 19 focused tests, migration/type/runtime checks, TypeScript, scoped lint and production webpack passed. Post-repair Chromium passed 2/2 in 39.7s across 320px keyboard/dark and desktop Viewer journeys. Terra/high QA found two P1 and one P2 initially; after repair it found no P0–P3 and passed affected exact-commit gates. Its sandbox did not retain browser output, so the writer runtime pass is recorded with that limitation.
- **Relevant completed dependencies:** D03-T07 provides verified-email create-or-join; D04-T03 proves global and Matter signed upload locally; D06-T13 supplies shared Intake; D08-T02 supplies the canonical extraction-conflict Review foundation; D05-T01 supports first attachment; D06-T12 proves exact private-PDF Workbench/Matter reopening; D11-T07–T10 cover read-only graph, governed manual relationships and supporting-file authority. Reconcile their receipts against changed code before reuse.
- **Remaining mandatory product work:** ordinary source-grounded extraction must still meet the acquisition/quality gates; enabled placement/possible-duplicate producers still require typed Review; extracted deadline Review is absent; minimum Team invitation/suspension closure, schedules/workers and release-wide acceptance remain unfinished. Conditional features remain behind mandatory closure.
- **Production gate:** confidential upload/deployment remains blocked by exact environment/revision identity, named go-live/rollback/support/incident owners, compliant hosting, tested database-plus-private-Storage recovery, provider/configuration acceptance, monitoring/rollback and exact deployed rehearsal. No remote service, production data, paid provider, scheduler or real email was changed in D06-T14.
- **Local environment:** isolated `dms-deadlines-159` stacks on loopback-only 559xx ports were created and removed by the packet runner. Synthetic legacy/deadline/browser data and the temporary production-build workspace were removed; no production or remote state changed.
- **Processes / ownership:** writer and QA helpers are stopped; there is no active code writer. Tracker/evidence files are the only post-implementation changes expected before the evidence commit. Preserve unrelated work, complete the current durable goal, then create the next packet goal under the user's continuous direction.
