# D11-T05 — additive Matter work-state and forum normalization

- **Outcome / owner / date:** D11-T05; current implementation task; 2026-09-09. This tranche completes the Matter plan's standalone additive status-normalization step without changing matter identity, financial year, placement, Restore, relationships, or the later Details redesign.
- **Base and implementation:** base/evidence parent `a192a4b`; implementation commit `9663a10`. The commit contains migration `00141`, its governed typed and compatibility commands, three SQL fixtures, deterministic generated types/refiner support, a shared state adapter, live Matter/client readers and UI cutover, exact Trash enrichment, and focused tests. Existing approval, ledger/handoff-top, design-system, concept, portal, platform-plan, proxy, decision-history, and review-document changes were preserved.
- **Data and command authority:** all eight legacy statuses map deterministically to typed `work_state` and `current_forum`; closed rows map to `closed/other` and receive an explicit `ambiguous_closed` report instead of an inferred court. Legacy `status` remains synchronized for compatibility. Typed create/update commands derive the actor and sole active tenant, deny Viewer/suspended/removed identities, revalidate same-organisation active Client/Matter lineage, enforce optimistic revision and idempotent replay, reserve close/reopen for Owner/Admin, and leave client, financial year, matter code, Trash state, document metadata, links, deadlines, and financial facts unchanged. Legacy overloads delegate into the same authority.
- **Live cutover:** Matter lists, client summaries, exact active/Trash reads, shell, Details, and installed server lifecycle gates use the typed state. Details presents separate work-state and forum controls; ordinary Associate edits cannot close or reopen a Matter. Material profile, state, forum, close, and reopen changes append canonical Activity/outbox evidence exactly once.
- **Independent QA and repair:** separate Sol/high read-only QA found that update lacked an active parent-Client fence and that the original post-migration fixture did not prove actual backfill/report behavior. The stopped writer repaired only those findings. The same reviewer passed the repaired worktree with no P0–P3 findings, confirming parent locking/replay checks, non-disclosing denial with no receipt, all eight genuine pre-migration mappings, one Trash row, and exact report counts of seven mapped, one ambiguous closed, and zero unmapped.
- **Verification:** a populated pre-`00141` disposable replay and the full rollback authority fixture pass. Twenty-three focused state/action/shell/list/client/Trash tests, three migration tests, four type/refiner tests, deterministic generated-type comparison, TypeScript, changed-file ESLint, database lint, whitespace/diff checks, and the Next 16.2.9 webpack production build pass. Schema lint reports only the two recorded pre-existing findings in `get_team_directory` and `realtime.apply_rls`; the build reports only the local Node 20 Supabase deprecation against the repository's Node 24 contract. Disposable databases and temporary artifacts were removed; the main database was untouched.
- **Gaps / remaining work:** authenticated browser behavior, 320px/200% zoom, keyboard/focus, dark mode, deployed schema cache, remote environment, representative scale, default Turbopack completion, and release acceptance remain unverified. D09 identity work remains decision-blocked. Relationship foundations must precede the graph, and the later complete Details side-sheet/mobile form remains a separate plan step.
