# D05-T01 — Governed first PDF attachment

- **Outcome / owner / date:** D05-T01; current implementation task; 2026-09-13. An authorised Owner, Admin or Associate can attach one private PDF from the canonical Workbench to an active, assigned, genuinely metadata-only document. Validation atomically creates immutable version 1 on that same document; a concurrent loser is explicit and never becomes replacement.
- **Base and accepted code:** implementation and exact QA commit `443af3ac0d5c55cd48643a1c5eeef432a3dab67f`, based on D08-T02 evidence commit `51baedd`. Migration `00154`, deterministic types, D05 SQL/concurrency/browser harnesses, target-bound TUS recovery, server actions, Workbench UI and the minimal labelled Review-baseline extension are included. Existing instruction, decision/plan, proxy, portal, design-system and concept changes remain outside the commit.
- **Delivered authority and lifecycle:** the authenticated reservation derives the one active membership, organisation, target document, Matter, revision and capability server-side, then binds file identity and target under the existing actor/idempotency fence. Private forced-RLS intent and baseline tables retain tenant-safe lineage and deny application-role DML. Finalisation and validated materialisation recheck Client/Matter/document lifecycle in deterministic lock order. The existing Intake, private asset, validation, immutable-version and outbox contracts remain canonical. Same-key replay cannot retarget or switch to ordinary Intake; cancellation, unfinished expiry, stale revision, Trash, duplicate PDF and another first attachment fail explicitly. Exactly one version, assignment, canonical Activity and version-fenced processing intent are created.
- **Metadata and Review:** attachment preserves document identity, Matter placement, notes, relationships, existing field decisions and all nine legacy human metadata columns. An immutable pre-PDF record baseline is labelled as record provenance, never PDF evidence. Five exact scalar paths use conservative case, Unicode whitespace/dash, reference-separator and unambiguous year-first date normalization. Harmless variants stay silent; genuine content/numeric/year differences produce one clarification-only D08 extraction conflict with real PDF candidate evidence. Structured fields, financial-year and summary comparison remain explicitly outside this bridge. Later processing cannot overwrite the captured human fields.
- **Developer/coordinator evidence:** the Node 24 isolated runner replayed 154 migrations, returned no database-lint errors and generated/refined types twice with exact parity. D05 core/baseline authority, legacy materialisation and D08 lifecycle SQL passed. Three observed-blocking races passed without timeout/deadlock: attach/attach, Client-Trash/attach and attach/Client-Trash. The production webpack build rendered 22/22 pages; Playwright passed 4/4 for a real private TUS four-page PDF, Viewer absence, 320px dark/reduced-motion invalid-PDF retry, desktop true 200% zoom, network retry, cancellation, keyboard/focus restoration, long names and overflow. TypeScript, scoped lint, 12 focused tests, four canonical-page tests, migration/type/runtime checks and diff checks passed. Coordinator review repaired replay/lock/FK/expiry compatibility and false scalar conflicts before accepting the diff.
- **Independent QA:** reset Sol/medium QA accepted exact `443af3ac0d5c55cd48643a1c5eeef432a3dab67f` with no P0–P3 finding from a clean archive. QA repeated replay/lint/type parity, D05 plus relevant upload/duplicate/Trash and D08 suites, all concurrency orders, focused checks, Next type generation, TypeScript, the webpack build and all four browser scenarios. No repository edit was made.
- **Isolation / remaining work:** only disposable `dms-attachment-154` and `dms-review-153` resources on loopback ports were used and removed; existing `supabase_db_dms` was untouched. No provider/OCR, hosted schema/Storage, production data, deployment, remote service or email was used. Remote 25 MiB/token-expiry, physical assistive-technology, representative performance, structured baseline comparison, provider processing and release-environment acceptance remain open. Replacement/version-management UI remains deferred. D11-T09 governed manual relationship authoring/archive in the approved Matter chronology/graph is the next ready local tranche; relationship-candidate production and Review resolution remain a separate later tranche.
