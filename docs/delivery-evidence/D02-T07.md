# D02-T07 — Retire legacy document placement writes

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `989bf6e`

**State:** review; independent QA and clean migration replay remain unverified.

The legacy `reassignDocumentMatter` action now returns an explicit unavailable result before constructing a database client for both Move and Copy. Migration `00130_document_placement_update_authority` removes authenticated/service direct `UPDATE` authority over `documents.matter_id` and `documents.source`. This follows the approved lifecycle requirement to keep the multi-step mutation disabled until D10 supplies an atomic, version-aware, asset-reusing command with consequence preview and rollback evidence; it prevents partial link/deadline/placement changes without pretending the replacement exists.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: rollback-only `document_placement_update_authority.sql`; local error-level schema lint; the 130-version migration check; the scoped eight-test document action suite; `npm run typecheck`; `git diff --check`; and both webpack and default Turbopack production builds. The privilege-only migration does not change generated TypeScript schema shape. Changed-file ESLint reports only the pre-existing `any` diagnostic at `src/lib/actions/document.ts:38`.

Coordinator review inspected the staged diff and live `ReassignDocumentDialog` caller, verified both modes fail before database/Storage access, and confirmed the legacy link deletion, document update, deadline update, and activity sequence is gone. Independent QA is pending because no independent reviewer was authorized. Deployment is not verified.

The disposable database still cannot clean-reset under Supabase CLI 2.111.0 (`LegacyDbBootstrapError`). Migration `00130` was applied explicitly on the locally current additive schema and verified with rollback-only SQL. This closes only D02-T07; the future D10 Move/Copy command, classification and legacy review/status writes, live note-moderation reason UX, independent QA, clean replay, and release acceptance remain.
