# D03-T01 — Enforce the development-only Usage boundary

**Revision:** `4d7d31e`
**State:** review; independent QA and the remaining D03 release matrix are unverified.

Legacy cross-testing-organisation Usage remains available only when the server runtime is explicitly `development`. Outside development, the `/usage` page terminates before authentication/data access with the route's not-found boundary, the service-role pricing action returns an unavailable result before constructing either database client, and desktop/mobile tenant navigation omits the Usage entry. Tenant Admin membership therefore cannot authorize the legacy global pricing mutation in production. The separate platform console and canonical usage projections remain future owning work.

Verification passed: the pure environment-boundary matrix for development/production/test/unset values; source-order checks proving route/action guards precede privileged client construction; navigation propagation coverage; scoped ESLint; `npm run typecheck`; `git diff --check`; and both Next 16.2.9 webpack and default Turbopack production builds under Node 24. The initial sandboxed webpack attempt could not resolve the configured Google Fonts host; the required network-permitted rerun passed. The later default build also resolved the transient B04 stall recorded during D02-T04.
