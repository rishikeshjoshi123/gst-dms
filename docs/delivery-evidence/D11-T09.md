# D11-T09 — governed manual Matter relationship interaction

- **Outcome / owner / date:** D11-T09; current implementation task; 2026-09-13. Authorised Owner, Admin and Associate users can deliberately add a Timeline-visible relationship from chronology or the eligible desktop graph, review catalogue-owned canonical and progression sentences, and archive an effective relationship with a required reason. Viewer, closed, Trash, foreign and ineligible-document states remain non-mutating.
- **Base and commits:** base `5c295ea`; implementation `ea683ab16de402b8a228994390e784d160b55791`; independent-QA repair and exact accepted revision `19bb3494556f4cd5a2056d1c98061fbd363f59a7`. The implementation commit contains only the 25 relationship-authoring, Matter Timeline, migration/type and acceptance files. The repair commit contains 11 scoped files and leaves historical migration `00146_effective_relationship_core.sql` byte-identical.
- **Implemented authority:** migration 00155 adds a private, bounded authoring projection with true document lifecycle revisions, canonical-row identity discriminators, latest eligible catalogue phrases and the effective relationship source revision. Timeline activation binds exact source, target and catalogue versions; a catalogue `SHARE` lock prevents a newer version appearing between validation and the governed core command. Genuine unchanged-payload response-loss replay remains valid, while stale, withdrawn, non-Timeline, non-acyclic and disallowed-class requests fail without relationship, decision, Activity, outbox or receipt effects. Matter-bound archive delegates to the existing reason-required lifecycle command. Client reconciliation hides only the exact successfully archived ID/revision and permits a later reactivation.
- **Verification:** the final coordinator and fresh QA each ran `/opt/homebrew/opt/node@24/bin/node scripts/acceptance/run-relationship-authoring.mjs --types --browser` in the exclusively owned `dms-relationship-authoring-155` profile. All 155 migrations replayed; database lint, `effective_relationship_core.sql`, `timeline_relationship_authoring.sql`, repeated generated-type parity and the production webpack build passed. Playwright passed 5/5 at QA in 53.4 seconds, covering duplicate titles, Owner/Associate success, Viewer absence, response-loss replay, stale revisions, archive continuity, 320px, 900px, true 200% zoom, dark/reduced motion, keyboard/Escape/focus and overflow. QA also passed 35/35 focused tests, 7/7 migration/refiner tests, scoped ESLint, `next typegen`, TypeScript and diff checks.
- **Independent QA:** fresh Sol/medium QA accepted exact `19bb3494556f4cd5a2056d1c98061fbd363f59a7` with no unresolved P0–P3 finding. Its first review had rejected the implementation commit for missing catalogue-version authority and incomplete graph Escape scope; both findings were repaired and reverified in the separate repair commit.
- **Environment / remaining work:** no remote service, provider, email, production data, deployment or pull request changed. Disposable QA stacks and archives were removed. An earlier writer mistake applied the initial two 00155 functions and grants directly to existing local container `supabase_db_dms` without a migration-ledger row; no data changed, but removal requires explicit approval and remains an environment cleanup gate. Matter Activity is not yet a live section, so acceptance proves canonical `activity_events` persistence rather than Activity UI. Candidate production/Review resolution, legacy-link disposition, Realtime, representative performance, physical assistive-technology review and deployment remain separate.
