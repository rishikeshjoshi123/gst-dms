# D13-T03 — Truthful exact-source reopening after private object loss

- **Outcome / decision / date:** D13-T03, 2026-09-15. In the [September 14 mandatory release journey](../decision-history/2026-09-14-first-production-release-scope.md), a formerly valid, placed private PDF whose bytes are genuinely missing now reopens at its exact immutable Workbench version/page with `PDF file unavailable`, no misleading signed-link Refresh action and an intact document record. Matter chronology still navigates to that exact record. This is synthetic local object-loss acceptance, not automatic rehydration, backup, or deployed recovery.
- **Implementation / authority:** `9f6baf1` follows D04-T03 private exact upload, D06-T12 authenticated Workbench, D13-T01/T02 fresh confirmation-required journey, and D12-T02 worker source authority. The signer retains its authenticated RLS/exact canonical/Trash read-grant boundary before the service client sees a private locator. A trusted Storage SDK `statusCode='404'` (or HTTP 404) denotes missing bytes; a general HTTP 400, permission fault, unsupported HEAD, server failure or network error remains temporary. A valid signed link is released only after a body-free 2xx HEAD. The viewer honors PDF.js 404; an ambiguous old-link failure offers one labelled exact-version server renewal, which can turn a typed missing outcome permanent without guessing from response text. Affected signer logs no longer print raw Storage/DB errors, paths, hashes, identifiers or URLs. Shared viewer state, `/dev/design-system` and its component contract were kept aligned.
- **Actual disposable acceptance:** the stateful release fixture identifies one *already placed* global-Intake PDF by verified uploaded-byte SHA-256/size and canonical tenant/document/current-version/asset lineage, then removes only that object from its exclusively owned loopback Storage stack. The SDK probe measured valid `info/sign=ok`, signed HEAD 200; after removal, metadata/sign/download each returned HTTP 400 with typed API `404`, while an old signed HEAD returned ambiguous 400. The browser and SQL assert the explicit typed API code, unchanged document/version/file-asset hash/bytes/organisation, exactly one assignment and closed typed placement Review, permanent Workbench and Matter reopening, an unaffected PDF still viewable, and 320px dark/keyboard/no-overflow. Assigned Intake is intentionally absent from every Upload Queue scope under `00143`; the check proves there is no stale queue source action rather than inventing a postplacement row.
- **Verification / QA:** One Sol/medium writer passed focused source/viewer tests 26/26, runner tests 3/3, TypeScript, scoped lint and diff hygiene. Its first full run stopped on a transient Review message before loss; a durable closed-item projection replaced that flaky assertion. Its next two runs exposed the SDK HTTP-400/API-404 distinction and an invalid assigned-queue assertion; neither was counted as loss acceptance. The final isolated production-webpack Chromium journey passed 1/1 (about 1.2 minutes) after all corrective assertions. Independent Terra/high exact-commit QA at `9f6baf1` found no P0–P3; it ran the focused caller/source/runner tests, TypeScript/lint and one clean replay through `00166`, zero error-level database lint, repeated generated-type parity, isolated webpack Chromium 1/1 (57.3 seconds), actual loss SQL/browser and cleanup. Owned stacks, locks, temporary builds and Playwright artifacts were removed; ordinary local stack and user `.next` were untouched. No paid provider, remote or production service, production data, real email or deployment was used.
- **Open gate:** real independently adjudicated D12 acquisition/extraction corpus and authorised paid Mumbai provider quality remain unverified. D13 confidential upload still requires named go-live/rollback/support/incident authority, a compliant host, tested database-plus-private-Storage recovery for the selected tier, monitoring and exact deployed rehearsal. The partner's retained originals and later governed exact-hash rehydration are not an initial automatic restore promise.
