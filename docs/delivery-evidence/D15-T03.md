# D15-T03 — Immediate governed member suspension

- **Outcome / owner / date:** D15-T03; current implementation task; 2026-09-15. An exact-active Owner or Admin can preview and immediately suspend one same-organisation active Associate or Viewer from Team. Every active open or in-progress Task assigned to that member is explicitly returned to the team queue while status, due date, Matter context, authorship and history remain intact. The affected user’s existing session loses protected access on its next request, and a later sign-in reaches the non-disclosing `Access suspended` state. This closes the September 14 local administration minimum.
- **Base and accepted code:** implementation `15c93ef2362858db99c8884ef4435c06c4d8ba43`, access/race/browser repair `d131cb5ef71857fe6d96b8730f5070900d3a9d1b`, and final boundary repair `0952cc1722dd593b638db14517f208b2e9d0326c`, based on D15-T02 evidence commit `38f57d2`. Migration `00161`, generated types, Team actions/UI, request proxy, notification guards, SQL/concurrency fixtures and Chromium runner are included. D15-T01 supplies the safe member directory; D15-T02 supplies the Team administration shell.
- **Authority and atomicity:** fixed-search-path functions derive exactly one canonical active membership and the suspension capability, lock actor and eligible ordinary target, reject self/Owner/Admin/foreign/stale requests, bind globally unique idempotency to actor, subject, reason, disposition, membership revision and the disclosed Task-set fingerprint, and write canonical suspension plus administration evidence atomically. Task assignment uses membership-before-Task ordering; bounded `NOWAIT` contention returns a controlled impact conflict. A stale preview cannot move undisclosed work. Task revision, transition history, Activity and outbox are appended for each return-to-team mutation. Direct membership writes and private transition execution remain revoked; committed Task-command replay remains stable after later suspension.
- **Access and UI:** the Next 16 request proxy rechecks exact-active context for protected page, API and client-navigation requests and preserves refreshed/cleared Supabase cookies on redirects and denial responses. The protected layout remains a full-load defense. Notification RLS and actions independently deny suspended sessions; Search and signed-source paths retain their database authority. The public secret revalidation route now fails closed when its secret is missing or invalid. Team provides impact, bounded reason, explicit disposition acceptance, stale-impact re-preview, responsive/dark presentation, 48px consequential actions and deterministic focus on cancel and success.
- **Verification:** exact `0952cc1` passed clean replay through all 161 migrations, parsed database lint, adversarial authority/RLS/ACL/replay cases, stale-impact conflict plus fresh retry, both-direction two-session concurrency, deterministic generated types, 12 focused tests, TypeScript, scoped ESLint, migration/type/runtime contracts and diff hygiene. Chromium passed 4/4: Owner with a second live target session and future sign-in denial, Admin success, Viewer control absence, and 320px dark keyboard/focus/touch/overflow. Coordinator production webpack passed from an isolated temporary copy; the live pre-existing `.next/lock` was untouched. Disposable stacks, servers and build copies were removed.
- **Independent QA / remaining gates:** Terra/high QA found notification access, cached-layout, inverse-deadlock, stale-impact and focus defects at `15c93ef`, then cookie propagation, replay-order and revalidation-secret defects at `d131cb5`. Final exact clean `0952cc1` re-QA found no P0–P3; focused checks and an independent disposable SQL/concurrency run passed. Reactivation, Owner/Admin suspension, role/ownership/removal/departure, new deadline/reminder/Review-claim lifecycles, broad Realtime, remote services, providers and deployment remain excluded. D13 still requires approved hosting, database/private-Storage recovery, incident ownership, provider acceptance and exact deployed rehearsal before confidential data.
