# D09-T05 — Verified document identifiers and exact reference resolution

- **Outcome / owner / date:** D09-T05; current implementation task; 2026-09-12. The backend now lets an authorised Owner, Admin or Associate activate, correct and revoke a current document's source-grounded self identifier. Eligible outbound reference observations become durable mentions and resolve bidirectionally, in either arrival order, through the exact organisation/namespace/kind/normalized-value key.
- **Base and tested code:** implementation and independently accepted revision `e7f918c311ba73a0877586c1405564fe2bcbc051`, following accepted D09-T04 evidence `5eb2e01`. Migration `00151`, generated database types, the registered SQL fixture and the stressed concurrency harness are included. Existing instruction, decision, plan, ledger, handoff, proxy, portal, design-system and concept changes remained outside the implementation commit.
- **Delivered authority and behavior:** verified identifiers retain exact logical-document candidate, source run, binding, current version, canonical source anchor, normalizer/catalogue and optional exact same-Matter identifier provenance. Lifecycle decisions and replay receipts are append-only and CAS/idempotency governed; corrections preserve predecessor lineage and reevaluate old and new keys. Mention materialization and resolution are synchronously transaction-durable from the fenced v4 finisher and identifier/document-state hooks, without a remote dispatcher dependency. Resolution records immutable runs/results plus a secured current projection for `unique_exact`, `ambiguous`, `conflicting` and `unresolved`; non-unique outcomes disclose no target. Exact-key and document-leading indexes support resolution and affected-key discovery. Current-version, active document/Matter/client, Trash, Restore and retained-shell physical-purge rules prevent stale or purged targets from remaining visible or reusable.
- **Independent QA:** fresh QA accepted exact `e7f918c` with no P0–P3 finding. A clean disposable run applied all 151 migrations, passed every registered fixture and private Storage seed, and passed D09-T05 authority, tenant opacity, candidate forgery, lifecycle, replay/fingerprint, four-outcome, both-arrival-order, exact-plan, version replacement, Trash/Restore, controlled purge and prohibited-effect assertions. Forty stressed interleavings passed: six mention/target, eight correction/resolver, eight Trash/resolver, six cross-key correction swaps and twelve duplicate same-key runs. The legacy `review_required` wrapper regression passed. Migration tests passed 3/3, type-refiner tests 4/4, the 151-file inventory and generated/refined type parity matched, and TypeScript, scoped lint, error-level database lint and exact diff checks passed. An isolated provider-neutral Next 16.2.9 webpack build compiled in 15.3 seconds and generated 22/22 static pages. Disposable QA database/build resources were removed.
- **Integration / deployment:** locally integrated at `e7f918c`; this receipt records the accepted parent. Not deployed. No provider, hosted database, Supabase mutation, Vertex call, real email or production service was changed.
- **Remaining work:** live verification/Review UI, fuzzy matching, relationship/effective-edge creation, placement or reassignment, governed split/copy repair, provider/browser/deployed acceptance and populated-load testing above the audited fail-closed 10,000-mention boundary remain separate gates. D09-T06 governed split/copy repair is the next chronology/relationship foundation candidate.
