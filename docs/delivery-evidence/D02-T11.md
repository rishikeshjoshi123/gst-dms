# D02-T11 — Expose live note moderation reasons

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `54179d4`

**State:** review; independent QA and browser acceptance remain unverified.

All three enabled legacy note-removal consumers now use one feature-level `RemoveNoteDialog`. It explains the audit tombstone consequence, accepts the Admin/Owner moderation reason required by the governed `remove_case_note` command, tells authors to leave the field blank for their own note, constrains input to 500 characters, keeps failures open for correction, disables controls during submission, and removes local state only after server success. Labels, help text, descriptive actions, mobile-safe stacking, semantic tokens, and Radix dialog behavior follow the current design-system contract.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: the expanded three-test note-removal suite proves every live consumer passes the reason and closes only after success; the full rollback-only `secured_legacy_note_removal.sql` authorization matrix covers author, non-author, Admin/Owner moderation, Viewer, inactive, and cross-tenant cases; `npm run typecheck`; `git diff --check`; lint for the new dialog and changed test; and both webpack and default Turbopack production builds. Broader consumer-file lint retains existing `any`, effect, escaped-quote, and unused-prop debt; no new dialog diagnostic remains.

Coordinator review inspected all live callers and the staged diff, verified errors preserve the dialog/reason, and confirmed the action still uses the authenticated command rather than a service client. Independent QA and browser interaction checks are pending because no independent reviewer/browser acceptance was authorized. Deployment is not verified.

This completes D02’s currently scoped implementation boundaries, but not its outcome acceptance: independent QA, clean database replay, browser acceptance, and release verification remain. The later Notes-domain migration still owns immutable message versions and the full conversation UI.
