# D02-T01 — Secured legacy note removal

- **Outcome / owner / date:** D02-T01; current implementation task; 2026-09-08. This tranche implements Notes [source-integrity repair](../plans/features/2026-08-25-notes-and-case-brief.md#existing-notes-and-source-integrity-repairs), [message lifecycle](../plans/features/2026-08-25-notes-and-case-brief.md#message-authoring-and-lifecycle), and relevant [acceptance](../plans/features/2026-08-25-notes-and-case-brief.md#testing-and-acceptance-criteria): reading a note no longer authorizes its removal.
- **Base and tested code:** base `93d474e` with unrelated dirty concept, design-system, approval, proxy, Task-reader, and planning edits preserved. Implementation commit `7e9bed3` contains only the removal command, enforcement trigger, audit fields, server-action migration, generated types/refinement, and focused tests.
- **Developer evidence:** migration `00124` applied successfully to the disposable local database already at `00123`; `supabase db lint --local --level error` reports no findings. The rollback SQL fixture passes author success and stable retry time, non-author Associate/Viewer/suspended/cross-tenant denial, Admin/Owner moderation with a bounded recorded reason, inactive-Matter denial, preserved tombstone source fields, and direct authenticated/service-role bypass rejection. A two-session test proves membership suspension waits behind one atomic author removal and denies later mutation. Local type generation emitted the new table/RPC shapes; unrelated pre-existing enum-order churn was excluded. Type-refiner tests (3), focused action tests (2), TypeScript, scoped ESLint, migration uniqueness (124), whitespace, and both Next 16.2.9 webpack/default Turbopack production builds pass under Node 24.
- **Independent QA:** pending. Security and migration work requires a separate read-only reviewer before D02 can become integrated; the sequential single-writer task did not launch a parallel reviewer.
- **Coordinator check:** inspected all three live `deleteNote` callers and the full scoped diff. The live server action now calls only the authenticated `remove_case_note` RPC for removal, accepts an optional moderation reason, handles idempotent replay, and revalidates only the context returned by the command. The decisive direct-path test confirms even `service_role` cannot change removal fields or execute the RPC. Physical purge remains available only through postgres-owned governed lifecycle functions.
- **Integration / deployment:** locally committed as `7e9bed3`; not deployed. A clean migration replay remains unverified because Supabase CLI 2.111.0 fails before reset with `LegacyDbBootstrapError`; additive apply, type generation, schema lint, rollback acceptance, and concurrency were verified against the confirmed local stack.
- **Remaining work:** this closes D02-T01 only. D02 remains in review pending independent QA, a live moderation-reason UI path, client-mutation authorization, safe-directory migration, removed-member coverage, and clean database replay. Resume with the next smallest approved D02 authorization/directory repair or another ready outcome if its caller overlaps preserved dirty files.
