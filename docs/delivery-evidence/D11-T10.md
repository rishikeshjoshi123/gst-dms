# D11-T10 — Matter supporting-file authority foundation

- **Outcome / owner / date:** D11-T10; current implementation task; 2026-09-14. CaseChain now has a versioned supporting-file category catalogue, governed category assignment for supporting documents, and optional same-matter supporting-to-proceeding evidence associations. Viewer access remains read-only; Owner, Admin and Associate roles receive the approved mutation authority; catalogue administration remains Owner/Admin only.
- **Base and accepted commit:** base `d7357e37559ac4e1bb463c27bc6814fc9894f463`; scoped implementation and accepted revision `c68ac8ab3da79988035a47b78db34f46db27058d`. The commit contains only migration `00156`, its generated database types, the Matter supporting-files SQL acceptance suite and its isolated Node 24 runner.
- **Implemented authority:** six immutable system category keys and tenant-versioned extensions are projected through narrowly granted readers. Category changes and evidence association activation/archive use membership and capability checks, lifecycle guards, optimistic revisions, durable idempotency receipts and append-only decisions. Associations require active same-organisation, same-matter supporting-to-proceeding endpoints and one active row per pair and kind. Successful document mutations append canonical Activity and exactly the two approved outbox event kinds; they do not create Timeline or `document_relationships` rows.
- **Verification:** Antigravity's Gemini 3.8 Flash Medium implementation pass and the coordinator independently ran `/opt/homebrew/opt/node@24/bin/node scripts/acceptance/run-matter-supporting-files.mjs --build`. All 156 migrations replayed in the disposable `dms-matter-supporting-files-156` profile on database port 57422; database lint returned no findings; repeated generated-type output was byte-stable; `effective_relationship_core.sql`, `timeline_relationship_authoring.sql` and all 33 Matter supporting-files cases passed; and the Next.js 16.2.9 webpack production build completed successfully. `npm run db:migrations:check`, 3/3 migration unit tests and diff checks also passed.
- **Independent QA:** the coordinator reviewed the full authority boundary and rejected multiple intermediate model revisions for permission, replay, RLS-fixture, lifecycle-revision, outbox-envelope and test-quality defects. The final accepted content passed an independent Docker-backed rerun, including isolation and cleanup checks. No unresolved P0-P3 finding remains in the scoped tranche.
- **Environment / remaining work:** no remote service, production data, deployment, push or pull request changed. The isolated acceptance stack and temporary build workspace were removed, and `scripts/acceptance/runtime.mjs` remained unchanged. During the earlier Gemini 3.1 Pro experiment, the model mistakenly reset the default local `supabase_db_dms`; under the active pre-pilot policy its fixture data was disposable, but the event remains an explicit safety caveat for future delegation. Files UI integration and deployed-environment acceptance remain separate tranches.
