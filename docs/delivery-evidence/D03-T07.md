# D03-T07 — Normal create-or-join onboarding

- **Outcome / owner / date:** D03-T07; bounded implementation coordinator; 2026-09-11. This tranche implements the approved local email signup → verification → create-or-join onboarding path. It does not deploy, configure production mail/DNS or scheduling, send real email, or implement deferred member departure/removal.
- **Base and integrated code:** base `bc0a676`; implementation `34402a0`, accessibility/test repair `4ff68ab5d1b5b11d464215dfd23bcce6c268c47e`. Migration `00145` adds authenticated, verified-email organisation creation with a stable idempotency receipt and the same per-user advisory lock as invitation acceptance. Creation atomically establishes the organisation, active Admin/explicit Owner membership, operational/retention/storage defaults, administration audit, Activity records and projector outbox. Active/suspended membership remains the one-current-organisation fence.
- **Product flow:** ordinary signup no longer requires an invitation. Confirmation-required local signup delivers only to captured loopback mail, follows the real verification URL and completes the PKCE callback. Verified users without a current membership see all account-bound live invitations plus a distinct creation action; viewing or following a link does not auto-join. Active users route to the workspace, while unverified and suspended users fail closed.
- **Developer and coordinator evidence:** clean replay through migration `00145`; Team, upload, exact Trash/shared Intake and onboarding SQL fixtures; create-versus-accept concurrency; 9/9 established Chromium scenarios; 2/2 isolated confirmation-required onboarding scenarios; final database verification; TypeScript; 12/12 focused tests; migration/type-refiner/runtime tests; scoped lint; 145-migration inventory; diff checks; and provider-neutral webpack build passed. Creation replay proved one organisation, membership, receipt, audit row, Activity log/event and outbox row.
- **Independent QA:** reviewer `/root/tranche_qa` repeated `npm run acceptance:local` on exact `34402a0`, verified authority and race behavior directly, and found no P0/P1. It found one P2 unnamed mobile Log out control and one P3 inaccurate dark-mode test; both were repaired in `4ff68ab`. Narrow re-QA of exact `4ff68ab5d1b5b11d464215dfd23bcce6c268c47e` reported no P0–P3: 2/2 isolated onboarding, a visible/accessibly named 88.28×44px Log out action, persisted application dark mode, 14.31:1 text/surface contrast, zero 320px overflow, 12/12 focused tests, typecheck, lint and diff checks.
- **Integration / limits:** locally integrated; not deployed. All accounts, organisations, invitations and messages were disposable local fixtures. Production SMTP/DNS/email, hosted redirects/schema, deployment, scheduler, departure/removal, actual 200% browser zoom and physical assistive-technology review remain unverified or intentionally outside this tranche.
