# D01-T02 — Application/worker wake boundary

- **Outcome / owner / date:** D01-T02; current implementation task; 2026-09-08. This tranche implements Lifecycle [Review follow-through: runtime and move/copy](../plans/platform/2026-08-24-document-record-and-file-lifecycle.md#review-follow-through-runtime-and-movecopy): application wake helpers enqueue fixed dispatchers through the Trigger SDK without importing task implementations and native PDF dependencies into Next.
- **Base and tested code:** base `0d10ba3` with unrelated dirty concept, design-system, approval, proxy, and Task-reader edits preserved. Implementation commit `e975856` contains only the document-outbox and Trash-purge wake boundaries plus focused tests.
- **Developer evidence:** the helpers now call fixed `dispatch-document-outbox` and `dispatch-trash-permanent-delete` identifiers through `tasks.trigger`. Document wakes retain an empty payload and the existing 5–30 second debounce; Trash wakes retain no payload and the 2–15 second debounce. Both stay inside `next/server`’s post-response scheduler and swallow/log gateway rejection because durable database work and scheduled recovery remain authoritative. `tsx --test src/lib/outbox/wake.test.ts src/lib/trash/purge-wake.test.ts` passes 11 tests, including fixed identifiers, empty payload, delayed scheduling, safe failure, caller constraints, database-derived worker identity, delivery fencing, and explicit absence of `@/trigger/*` imports. Full TypeScript, scoped ESLint, and whitespace checks pass.
- **Independent QA:** pending. This boundary carries asynchronous delivery semantics and requires a separate reviewer before D01 can become integrated; no parallel writer/reviewer was started in this sequential task.
- **Coordinator check:** inspected every live caller of both schedule helpers and the complete scoped diff. Repository search finds no application/lib import of `@/trigger/outbox`. The successful production output contains no native canvas import trace, while worker task identifiers remain unchanged in `src/trigger/outbox.ts`.
- **Build evidence:** with network access for `next/font`, Next 16.2.9 webpack compiles, type-checks, generates all 23 static pages, and completes route tracing. The default Turbopack build also compiles, type-checks, generates all 23 pages, and completes. Both warn that local Node 20 is deprecated by the current Supabase client; this is the next runtime tranche, not a waived failure.
- **Integration / deployment:** locally committed as `e975856`; remote Trigger deployment and application deployment not verified.
- **Remaining work:** this closes only D01-T02. D01 remains in review pending independent QA, Node/package-manager pinning, the approved Trigger Node 24/SDK alignment and worker smoke/rehearsal, database replay, and lint/release gates.
