# D11-T04 — authoritative Matter Timeline chronology

- **Outcome / owner / date:** D11-T04; current implementation task; 2026-09-08. This tranche makes chronology the sole live Timeline presentation until the separately planned relationship graph, without claiming graph commands, Review, deadlines, financials, Realtime, or release acceptance.
- **Base and implementation:** base/evidence parent `468253a`; implementation commit `168d69e`. The commit contains migration `00140`, rollback fixtures, generated RPC types, the authoritative chronology reader and URL-state helpers, the Matter route and Timeline UI, exact Trash projection parity, and focused tests. Existing approval, ledger/handoff-top, design-system, concept, portal, platform-plan, proxy, decision-history, and review-document changes were preserved.
- **Database authority:** authenticated-only `read_matter_timeline_chronology` derives identity from `auth.uid()`, requires one active tenant plus `document.view`, and fences the active same-organisation client/matter lineage. It accepts only strictly shaped filters, returns exact filtered and unfiltered totals with a canonical clamped page, orders deterministically, and supports a separately authorised off-page selection. Timeline metadata comes only from the current valid version's effective fields; a metadata-only fallback is allowed only when the current pointer is null and no historical version exists.
- **Trash compatibility:** the original exact Trash projection was preserved behind a private revoked implementation and the public RPC signature was recreated as a fixed-search-path, authenticated-only wrapper. Its selected and related authorised documents are enriched with the same safe current-version versus genuine metadata-only semantics, without widening legacy row visibility or exposing storage identity.
- **Live workflow:** the Timeline owns `q`, type, direction, date-range, attention, page, selected-document, and inspector URL state. Desktop uses a semantic chronology table with an in-flow 392px inspector; constrained and mobile layouts retain direct Workbench access and present direction, content availability, attention, effect availability, and key-fact availability. Selection close/navigation restores focus across streaming updates. Active note preview is re-fenced to the selected document, bounded in the database, and exposes no quote locators, member directory, or derived identity.
- **Independent QA:** an initial Terra/medium bounded writer produced an incomplete draft. A focused Astra/high diagnosis identified SQL authority, compatibility, fixture, and UI defects. A Sol/high repair writer then completed the scope in strict single-writer turns. A separate Sol/high read-only reviewer inspected the actual `468253a` worktree revision, real callers, migration and fixtures; after two repair cycles, its final result was PASS with no P0–P3 findings. It reconfirmed RPC compatibility/security, current-version authority, Trash parity, bounded note reads, distinct lifecycle statuses, exact filters/order/paging/selection, and focus behavior.
- **Coordinator verification:** 59 focused Matter Timeline/shell/route/read tests and seven exact-resource regressions pass. TypeScript, scoped ESLint, migration numbering/checks, migration fixtures, generated-type refinement, runtime contract tests, and diff checks pass. Migration `00140` compiled in a fresh disposable database, and both chronology and exact-Trash rollback fixtures passed with constraints forced; the disposable database and dumps were removed and the main database was untouched. The Next 16.2.9 webpack production build passed, including `/matters/[id]`; its only reported warning was the local Node 20 Supabase deprecation against the repository's Node 24 contract. The default Turbopack build emitted no error but made no bounded progress during optimisation and was interrupted, so it is not recorded as passing.
- **Gaps / remaining work:** no browser surface was available, so authenticated desktop/mobile behavior, 320px and 200% zoom, keyboard/focus, Back/Forward, dialog Escape, dark mode, long-content layout, and live scroll ownership were not visually exercised. Deployed schema-cache behavior, remote Supabase, representative-scale performance, selective freshness/Realtime, and release acceptance remain unverified. D11 still needs complete typed shell counts where authoritative projections are absent, category/association foundations, the planned graph/relationship workflow, released delegated panels, and authenticated responsive acceptance.
