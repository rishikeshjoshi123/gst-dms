# D11-T07 — deterministic read-only Matter graph

- **Outcome / owner / date:** D11-T07; current implementation task; 2026-09-12. This tranche implements the Matter plan's horizontal graph, node/edge/filter, route-state and Timeline acceptance over the D11-T04 chronology and D11-T06 effective-relationship projection. Desktop Timeline can show a stable left-to-right graph; constrained clients retain the authoritative chronology.
- **Base and tested code:** base `68434ae`; implementation `d3a3954`, QA repair `f6b5d8e`, and strict-projection repair plus exact accepted revision `f115329`. The commits contain only 15 Matter graph/reader/route source and test files. Existing instruction, decision, plan, ledger, handoff and review-concept changes were preserved and excluded.
- **Developer evidence:** Node 24 Matter tests passed 83/83; the final coordinator-focused suite passed 37/37 and the strict graph subset 14/14. Fixtures cover zero/one, branch/merge/disconnected, undated/same-day siblings, catalogue-ordered relationship bundles, filtered endpoint removal, stable non-overlapping 10/50/100/250-node layouts, date/ID rank order, URL/filter/selection state, deferred desktop request policy, collision-free request identity, accessible controls/names and unavailable-source presentation. TypeScript, scoped ESLint, diff checks and the provider-neutralized Next 16.2.9 webpack build passed. Build audit kept React Flow/graph UI in lazy chunks and Dagre out of browser chunks; initial rendering contains only chronology and the desktop gate invokes an argumentless secured Server Function.
- **Independent QA:** Sol/high reviewer `/root/relationship_qa` inspected exact `f115329` after two adversarial repair cycles and reported no P0–P3 findings. Its raw matrix accepted a legitimate secured RPC row and rejected all malformed/duplicate relationship rows, invalid MD5 revisions and invalid/impossible timestamps without retaining partial edges. An independent 250-sibling star retained date/ID order with no overlap. QA passed 46/46 focused tests, Node 24 typecheck, scoped lint, an isolated provider-neutral webpack build, diff checks, lazy-chunk audit and Server Function manifest inspection.
- **Coordinator check:** inspected the complete implementation and both repair diffs, verified the real Matter caller and directly reran deterministic scale, strict projection, routing, accessibility and fallback tests. The graph consumes only the secured chronology and effective-relationship RPCs; it adds no route handler, relationship mutation, candidate/Review behavior, legacy-link inference, Realtime or D09 behavior.
- **Integration / deployment:** locally integrated at `f115329`; not deployed. No schema, remote service, hosted data or email changed.
- **Remaining work:** authenticated feature-fixture browser smoke remains unrun for desktop graph, light/dark, keyboard/assistive technology, 900px and literal 200% zoom. Representative browser performance/FMP and deployment remain open. Manual relationship interaction is separate and must preserve `WORK-REVIEW-CONCEPT`; candidate/AI matching, D09 identity, Realtime and legacy-link disposition remain later work. This closes D11-T07 only, not parent D11.
