# D11-T13 — source-grounded relationship suggestion Review

- **Outcome / owner / date / contract:** 2026-09-16. A D09 `unique_exact`, current, same-Matter reference between two current active proceeding PDFs now creates one non-authoritative `relationship_suggestion` Review item with immutable page evidence. It proposes only source → target `refers_to`; no relationship exists before an authorised human accepts or corrects it. The Matter Timeline relationship-review interaction, Work/Review typed-decision contract, September 13 interaction decision and September 14 release scope govern.
- **Base and tested code:** base/evidence `3b1c6288880963f0da5bfc7906ceed64f7d0d98a`; implementation `4e7870ce62dab52a6c55a6b0ca2155e9adf052d5`; accepted QA repair `7fbdc83786dbaea5b3ea78a5eccb06e6ae4c8fe5`. Migration `00173` binds the mention, exact resolution/result, verified identifier revision, both current document versions/assets/hashes/lifecycle revisions, analysis/binding/page/quotation, Matter/client state and current catalogue version. One revision- and idempotency-fenced resolver performs accept, corrected current type/direction, or rejection atomically with one decision and one Activity. Viewer, suspended/ambiguous, outsider, anonymous, service-role and direct-table paths are denied; stale, competing, cyclic or unavailable state fails closed.
- **Developer / coordinator evidence:** the final isolated Node-24 relationship runner replayed migrations 1–173, returned zero database-lint findings, reproduced generated/refined types byte-for-byte, passed exact-reference/currentness/authority/idempotency SQL and a two-session race with one winner, one stale loser and exact replay. Focused tests passed 40/40, TypeScript, scoped ESLint, migration tests 3/3, refiner tests 4/4 and diff hygiene passed. The private-PDF Chromium journey passed with exact immutable source/version/page and target Workbench links, desktop dashed non-effective graph context, 320px dark/keyboard/list equivalence, cancellation without mutation, shared-resolver acceptance, non-progression `refers_to` and exact return location. The coordinator inspected the complete migration and real Review/Timeline callers, tightened the return locator to exact `/review` routes, and verified affected route tests 19/19 plus typecheck.
- **Independent QA:** fresh Terra/high QA inspected exact `4e7870c`, independently passed the full SQL/types/concurrency runner, private-PDF browser 1/1, focused units, TypeScript, scoped lint, migration/refiner checks and webpack build, but rejected it with one P1: mobile or explicit Chronology still exposed normal Add/archive relationship authoring. Repair `7fbdc83` nulls normal authoring context for every relationship-Review route and adds direct mobile plus `view=chronology&inspector=relationships` assertions. Proportionate exact re-QA passed 25 affected tests, TypeScript, scoped lint, diff checks and a fresh isolated Chromium 1/1 in 42.5 seconds; no P0–P3 finding remains. The default Turbopack build was terminated after remaining idle at optimized-build start; the production webpack build passed. All owned stacks and temporary data were removed.
- **Integration / limits:** integrated locally at `7fbdc83`; this receipt commit follows and the authorised ordinary `origin/dev` push is recorded by the handoff. No provider, remote service, production data, confidential PDF, real email or deployment changed. Fuzzy/AI/embedding inference, cross-Matter suggestions, legacy-link migration, Realtime and notifications remain excluded. D12 still requires the separately authorised adjudicated real corpus and paid Mumbai provider benchmark; D13 still requires named production authority, compliant hosting, tested database-plus-private-Storage recovery, incident response and exact deployed rehearsal. This closes D11-T13 local acceptance, not the D11 parent or production readiness.
