# D08-T06 — typed multi-target filed-document placement Review

- **Outcome / date / contract:** 2026-09-15. When a placed current PDF has canonical printed self-identifier candidates matching two distinct active, human-verified, namespaced identity-eligible Matter keys, the filing remains unchanged and one typed `multi_placement_conflict` Review presents the exact quoted page and verified key for each destination. An authorised Owner/Admin/Associate keeps the current filing or chooses exactly one target after its own current governed Move impact. Hub placement/reevaluation §§190–251, 676–680, Work/Review §§112–128, the September 11 Matter-identity decision and September 14 mandatory Review journey govern. The printed candidate remains provisional; no inferred merge, split or Copy occurs.
- **Base / code:** D08-T05 accepted `7cb8cc3`/[receipt](D08-T05.md), D09-T06 governed Move `24e19a7` and D08-T04 Intake multi-candidate Review `f4418eb` are Git ancestors. Scoped implementation `b09423d`; bounded target-preview repair accepted at `d1f04b1ce04dc5a78c8982d18b1476e323c8742e`. Migration `00169` adds private immutable per-target snapshots, candidate-set digest and exact-set Keep memory, distinct typed queue/detail and target-specific preview, source/version/key/target/lifecycle fences, and an atomic Review resolver composing D09's fingerprint-fenced Move. UI/actions/model/types and local SQL/Chromium fixtures are included. The repair binds displayed/confirmed impact to the latest selected item/target with synchronous tickets; closed one-target history is not repurposed.
- **Writer / coordinator evidence:** Sol/medium sole writer replayed 169 migrations on exclusively owned `dms-review-153`, zero error-level database lint, deterministic generated/refined types twice, focused SQL for one→two transition, complete quoted candidates, tenant/Viewer/inactive denial, source/key/version/Trash/impact/replay/Keep/one chosen Move and exact asset/version/two distinct Activity events. Focused production-webpack Chromium 1/1 used two private one-page synthetic PDFs, 320px dark/keyboard and desktop exact Workbench/Matter. TypeScript, touched ESLint, migration/type tests and diff checks passed. Coordinator reviewed complete scoped producer, SECURITY DEFINER/RLS, lock/runner and live caller diff. First browser attempts exposed a nullable detail source-run and quote locator fixture error, both corrected before the accepted implementation commit; the first delayed-POST interception could not dispatch a second action while the first was held, so it was removed rather than claimed as overlap proof.
- **Independent QA / bounded repair:** Terra/high exact `b09423d` QA independently captured exit 0 for 169 replay/lint/parity/SQL and isolated Chromium 1/1 in 39.3s, plus TS/lint/runtime/diff, but rejected one P1: A's late preview could be rendered/confirmed while B was selected. SQL refused the mismatched fingerprint; the UI impact was still false. One client-only `d1f04b1` repair added target/item/sequence-bound preview state and deterministic deferred A↔B, Keep-cancel, unavailable and item-switch test. Exact read-only re-QA no P0–P3: helper exit 0, TS/lint/diff and affected isolated Chromium 1/1 in 40.8s; owned stack/temp build removed, ordinary local database/live `.next` untouched.
- **Integration / remaining:** locally integrated at `d1f04b1`, not deployed. Browser Server Action transport serialized held preview POSTs, so forced overlapping browser responses were **unrun**; the deferred helper and live client wiring were independently assessed. True two-session resolver-versus-key/Trash/source interleavings and a deliberate post-inner-Move fault injection were **unrun**; sequential rollback probes and static lock review passed, not stressed concurrency. More than one printed observation per same Matter is represented by one deterministic source candidate. D12 real adjudicated corpus/paid Mumbai quality and D13 named operators, compliant hosting, tested database-plus-private-Storage recovery, incident response and exact deployed acceptance remain open. No paid provider, production data, remote service or real email changed.
