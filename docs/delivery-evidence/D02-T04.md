# D02-T04 — Govern matter mutations

**Revision:** `92c4dea`
**State:** review; independent QA and clean replay remain unverified.

Migration `00127_matter_mutation_authority` removes authenticated and service-role direct `INSERT`/`UPDATE` privileges on `matters`, adds private command receipts and matter revisions, and exposes authenticated create/update commands to active Owner, Admin, and Associate members while denying Viewer, suspended, removed, unauthenticated, service-role, and cross-tenant paths. Updates use optimistic concurrency; retries are fingerprinted and replay-safe; activity and Unknown-FY document propagation occur in the same transaction. The live create and detail forms preserve idempotency keys across retries.

Verification passed: rollback-only `supabase/tests/matter_mutation_authority.sql`; `supabase/tests/matter_mutation_membership_change_concurrency.sh`; local schema lint at error level; local type generation plus `npm run test:db:types`; migration filename tests; scoped action tests and ESLint; `npm run typecheck`; `git diff --check`; and the Next 16.2.9 webpack production build under Node 24. The default Turbopack build was attempted three times, including once from a fresh `.next` cache, but remained silent in compilation for more than three minutes and was stopped; it is not claimed as passing for this revision.

The disposable local database could not be clean-reset because the recorded Supabase CLI 2.111.0 `LegacyDbBootstrapError` persists. Migration `00127` was applied explicitly after the history through `00123`; the rollback fixture, concurrency case, type generation, and schema lint passed against that local schema. Concurrency setup rows remain in the disposable database until reset by repository convention.
