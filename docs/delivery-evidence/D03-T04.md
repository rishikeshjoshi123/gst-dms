# D03-T04 — Disable public organisation creation

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `4b8ad88`

**State:** review; independent QA, browser acceptance, and deployed invitation/email verification remain unverified.

The controlled pilot no longer lets a signed-in account create an arbitrary tenant. Onboarding is now invitation-only: it loads pending invitations, provides labelled accept/decline actions, and gives a truthful recovery message when none is available. The exported legacy `createOrganisation` server action consults the shared pilot policy and contains no Supabase client, creation RPC, cookie, or redirect path. Migration `00135` retains the broader approved command for a later release while revoking execution from `PUBLIC`, `anon`, `authenticated`, and `service_role`, so a direct browser RPC cannot bypass the UI/action boundary.

The touched organisation action now uses its generated RPC contracts instead of sixteen legacy `any` casts; invitation behavior is otherwise unchanged. The replacement onboarding view preserves semantic tokens, descriptive actions, 44px mobile controls, loading/empty/error behavior, and an explicit principal page flow.

Developer evidence passed against the disposable local database and Node 24 / Next 16.2.9: migration `00135` applied explicitly; the rollback SQL fixture proves no application role retains execute and an authenticated direct call raises `insufficient_privilege`; local error-level schema lint is clean. Ten focused signup, invitation, Wiki policy, organisation-action, and onboarding tests pass. The 135-version migration check and unit suite, TypeScript, changed-file ESLint, whitespace validation, and both webpack and default Turbopack production builds pass. The older `task_single_organisation_authority.sql` intentionally asserts that a zero-membership user may create a tenant; that pre-pilot expectation is superseded by this release boundary and is not claimed as current pilot acceptance.

Coordinator review inspected the staged UI/action/grant diff, direct-call denial, typed RPC cleanup, and retained invitation path. Independent QA was unavailable under the single-writer instruction. The implementation is locally committed as `4b8ad88`; it is not deployed. D03 remains in review pending the typed release manifest/exact route-role-worker matrix, retention approval, clean-browser/email acceptance, independent QA, and remote release verification.
