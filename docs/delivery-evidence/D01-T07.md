# D01-T07 — provider-free release-candidate gate

- **Outcome / date / contract:** 2026-09-16. One Node-24 repository command, `npm run acceptance:release-candidate`, now runs the approved local release-candidate checks in a deterministic fail-fast order and names every stage and exit. It covers migration uniqueness, refined database types, focused release/application contracts, TypeScript, the disclosed repository lint baseline plus owned-file lint, a provider-neutralised production webpack build, and the confirmation-required stateful synthetic Chromium journey. The pilot plan's “Freeze the pilot journey and fixtures” release-checklist step, D03-T08 release matrix and D13-T01/T02 local journey contracts govern.
- **Base and tested code:** D08-T09 evidence commit `ca5869c` was the clean pushed base except coordinator-owned tracker drafts. Implementation commit `161f8422d36aaaa8ce971474cb4a430a65b1df41` adds the command, focused contracts, manual-only read-permission GitHub workflow and lint fingerprint (`285` diagnostics; `d62c173e8b66ec7274da770693242e84bc12ae5ddd77631503873ca275d604e3`). The temporary build excludes `.env*`, strips provider/release configuration, uses local bundled fonts and removes its workspace. Non-loopback provider/app/mail targets, provider credentials, release identity and a Supabase project reference fail before children start.
- **Developer / coordinator evidence:** The Terra/medium writer's final full command exited 0 across all eleven stages, with a 23-route webpack build and Chromium 1/1. That run exposed a real integration defect: extraction Review detail projected `source_analysis_run_id: null`, which the strict live model correctly rejected. Migration `00172` now retains an authenticated public `read_review_detail` façade, hides its delegate, omits only an irrelevant null key and preserves a processing-recovery UUID; SQL and model tests cover both shapes. The coordinator inspected the complete diff and real journey/build callers, found and repaired inherited remote app/mail/project configuration gaps through the same writer, and directly verified the CLI refusal, focused tests, dry-run order and diff hygiene. One coordinator shell assertion initially used zsh's read-only `status` variable after the checks had passed; the corrected assertion exited 0 and the shell mistake is not acceptance.
- **Independent QA:** Fresh Terra/high inspected exact `161f842` and found no P0–P3. Its first command used Node 20 and was correctly rejected before any child. The corrected Node-24 run captured stages 1–9 at exit 0: contracts 6/6, runtime 2/2, migrations 3/3, types 4/4, pilot contracts 17/17, journey contracts 8/8, TypeScript and both lint gates. The execution stream then disappeared after webpack despite observed journey execution and cleanup, so QA did not claim the missing results. A proportionate exact-commit rerun of only those two unchanged stages produced durable logs: provider-free webpack exit 0; stateful journey exit 0, full replay, zero database-lint results, generated/refined type parity and byte stability, and Chromium 1/1 in 1.1 minutes. The owned `dms-release-journey-162` stack and temp directories were removed; ordinary containers were untouched.
- **Integration / limits:** Integrated locally at `161f842`; evidence commit follows; not deployed. The workflow is manual-only and sends no secrets or deployment. No paid provider, remote service, production data, confidential PDF, real email, provider quality, backup/Storage recovery, monitoring, incident response, hosting compliance or deployed acceptance was exercised. D12 corpus/paid Mumbai quality and D13 production-owner/environment/recovery/deployed gates remain open, so this closes only D01-T07, not production readiness or the D01 parent.
