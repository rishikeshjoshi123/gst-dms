# D07-T01 — Immutable PDF quotation source identity

- **Outcome / owner / date:** D07-T01; current implementation task; 2026-09-08. This closes the enabled legacy Notes integrity repair approved in [Matter Notes and Cited Case Brief](../plans/features/2026-08-25-notes-and-case-brief.md#existing-notes-and-source-integrity-repairs): new PDF quotations retain exact immutable document-version and validated one-based page identity, while ambiguous legacy excerpts remain explicitly unverified.
- **Base and tested code:** base `102a33d`; implementation commit `fa054a9`. Unrelated approval, Team/concept, broader design-system, portal, proxy, ledger, handoff, and plan changes were preserved. Only the quotation-source hunks were staged from the already-dirty design-system documentation and gallery files.
- **Developer evidence:** migration `00137` adds the protected one-quote-per-note locator, guarded authenticated projection, exact version/page/asset validation, locator-aware idempotency, direct-write protection, and governed purge cleanup. New writes lock current membership, join the hierarchy Trash advisory fence, and hold state-conflicting share locks through insertion. The shared PDF viewer exposes typed source-bound quotation callbacks only when an authorised sink exists; drafts are source-keyed, composer retries retain a payload-bound idempotency key, and late signed-URL renewal is rejected after a source change. Canonical and legacy routes preserve complete locators and fail closed on malformed, repeated, or incomplete pairs. Timeline, Matter Notes, and Organisation Notes share truthful exact, unavailable, and legacy-unverified presentation; historical bytes are distinguished from current metadata and mutations.
- **Independent QA:** Sol/high reviewed the actual unstaged revision and live callers. Its first review failed the candidate for non-conflicting database locks, incomplete locator fallback to current bytes, and a regenerated composer retry key. After a bounded Terra/medium repair, the same reviewer passed the final revision with no material findings. It independently reran 43/43 focused tests and inspected the repaired SQL, route, composer, canonical caller, Trash/purge integration, and source renewal fencing.
- **Coordinator check:** two clean disposable local database replays applied all 137 migrations after repair; error-level DB lint and the exact quotation, Task adapter, and permanent-purge rollback fixtures passed. A two-session adversarial check held the Trash hierarchy advisory lock: exact-quote creation timed out specifically at that fence, then succeeded after release; a clean reset removed the synthetic rows. Canonical refined type generation reproduced SHA-1 `edb29f2739ffcc6e74072798149008c0166ed276`; its four refinement tests, Node 24 TypeScript, scoped central-file lint, 43 focused TypeScript tests, diff checks, and the Next 16.2.9 webpack production build passed.
- **Browser evidence / gap:** no authenticated browser round trip was established. Mobile/desktop responsive behavior, 200% zoom, keyboard selection/focus return, screen-reader state, dark mode, exact reopen/highlight, and signed-URL retry remain unverified interactively; source tests and compilation are not browser evidence.
- **Integration / deployment:** locally committed as `fa054a9`; not deployed. No remote database, Storage object, production data, provider, or external service was changed.
- **Remaining work:** D07 remains in review. CaseWiki compatibility links and the wider post-pilot thread/region/highlight model are not implemented. Authenticated browser acceptance and release-environment verification remain open, and D06's OCR/Realtime/release gates are unchanged.
