# D03-T06 — Exact local Trash expiry

- **Outcome / owner / date:** D03-T06; bounded implementation coordinator; 2026-09-11. This tranche implements the approved exact logical-expiry boundary for local Trash readers and Restore while leaving physical scheduling and deployment outside the run.
- **Base and integrated code:** base `79de711`; implementation `f0f54dd`, repaired through `b66c064f6e2589a734171fd6b3e7dceb1386d62a`. Migration `00143` uses database wall-clock time for workspace, retention, exact Client/Matter/Document projections, trashed-PDF grants, Restore preflight and Restore. Restore locks the operation and its transition guard rejects a deadline crossed during the command. Logical visibility is filtered before ordering and limiting. Expired blocked operations expose only content-safe status to Owner/Admin; ordinary, `purging` and `purge_failed` content stays unavailable.
- **Presentation:** `/trash` shows final-seven-day days/hours and blocked status, exact Trash strips use the fixed organisation timezone `Asia/Kolkata`, and the retired 24-hour Dashboard attention panel is absent. The 320px dark/keyboard scenario verifies a 44px target and no page horizontal overflow.
- **Developer and coordinator evidence:** clean local replay through migration `00144`, Team/upload/Trash+Intake rollback fixtures, deterministic seed and both private Storage PDFs passed. The Trash fixture establishes `now() < deadline` while `clock_timestamp() >= deadline`, rejects direct restore transitions, covers expired roots and descendants, expired PDF grants, blocked role visibility, purge-state non-resurrection, and an expired-newer-row pagination adversary. TypeScript, focused tests/lint, diff checks and a provider-neutral webpack build passed.
- **Independent QA:** Sol/high reviewer `/root/tranche_qa` repeated `npm run acceptance:local` on exact `b66c064`: all 144 migrations, three SQL suites, 9/9 Chromium scenarios and the authority verifier passed. QA reported no P0–P3 finding.
- **Integration / limits:** locally integrated; not deployed. Actual browser 200% zoom remains unverified. The production scheduler is intentionally unchanged: daily midnight-IST purge cadence and retirement of the minute attention projector remain required release work, not a pass from this tranche. No remote service, production data, scheduler or email changed.
