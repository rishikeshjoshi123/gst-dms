# D09-T04 — Typed material-field observations

- **Outcome / owner / date:** D09-T04; current implementation task; 2026-09-12. This tranche completes the approved typed material-field audit for client identifiers, legally classified dates, issuer/recipient actors, parties, exact monetary facts and legal provisions. The live extraction path now retains these as immutable, individually evidenced observations while deriving only transitional legacy reader fields outside the provider contract.
- **Base and tested code:** base `3c73b41`; implementation `a1ba0e2`; QA repairs `144b7c9` and exact accepted revision `61bc850614f8d50888456044f2121b1cf4940c90`. Migration `00150`, generated database types, strict Zod/provider schemas, prompts, deterministic normalizers, provenance materialization and Node/SQL fixtures are included. Existing instruction, decision, plan, ledger, handoff, proxy, portal, design-system and concept changes remained outside the implementation commits.
- **Developer and coordinator evidence:** typed GSTIN/PAN/TAN/CIN and catalogued identifiers retain invalid values safely; dates retain legal meaning without relative calculation; direction is derived only from validated issuer/recipient roles; money uses exact strings without rounding or invented totals; parties and legal provisions retain raw/display evidence and typed normalized values. Structured candidates use stable content keys and remain provisional or invalid, with no Matter/client identity, effective-metadata, placement, relationship or outbox consequence. Coordinator diff review and Node 24 tests passed 38/38 after the final repair.
- **Independent QA:** the first audit rejected `a1ba0e2` for provision-value source integrity, caller-controlled v4 direction, U+FEFF parity and outer-envelope validation; `144b7c9` repaired those but was rejected because Act identity was not separately grounded. Fresh QA `/root/normalization_qa` accepted exact `61bc850614f8d50888456044f2121b1cf4940c90` with no P0–P3 finding. Independent probes rejected forged provision number, Act label and Act kind; accepted NFKC and catalogued long-form aliases; retained contradictions only as invalid/unanchored evidence; derived v4 direction from persisted actors; preserved v3 isolation; and proved strict bounded candidate envelopes. All 150 migrations, every registered fixture, both concurrency harnesses, deterministic seed and private Storage passed. Focused tests passed 38/38, migration tests 3/3, type-refiner tests 4/4, generated types were byte-identical, and TypeScript, scoped ESLint, database lint and full-range diff checks passed. An isolated environment-free Next 16.2.9 webpack build compiled successfully; disposable resources were removed without touching the checkout build or port owners.
- **Integration / deployment:** locally integrated at `61bc850`; this receipt records that tested parent. Not deployed. No provider, OCR service, hosted database, remote service or real email was invoked.
- **Remaining work:** this closes D09-T04 only. Next ready is D09-T05: authorised verification/lifecycle for document self identifiers and indexed bidirectional exact resolution of outbound mentions, including later-target reevaluation and durable unique/ambiguous/conflicting/unresolved outcomes. Candidate activation, automatic placement, fuzzy matching, procedural relationship creation, Review UI, relative deadlines and governed split/copy repair remain separate gated tranches.
