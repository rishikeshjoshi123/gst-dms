# D08-T07 — concurrent filed-document Review safety rehearsal

- **Outcome / scope / date:** 2026-09-15. A lawyer acting on a current, source-grounded single- or multi-target filed-document Review cannot accidentally commit an old Keep/Move after a competing lawyer or a verified key, current source or Matter availability changes. The scoped local packet establishes real two-session and late-fault acceptance for existing D08-T05/D08-T06 typed resolvers and D09 governed Move; it adds no new producer or product mutation.
- **Dependencies / code:** [D08-T05](D08-T05.md) `7cb8cc3`, [D08-T06](D08-T06.md) `d1f04b1`, governed Move `24e19a7` and source-unavailable handling `9f6baf1` are Git ancestors. Implementation `1f77a27` adds only a `--stabilisation` branch to the existing Review acceptance runner, two synthetic persistent fixture setup SQL files and a Node 24 two-session orchestrator. It uses an exclusively owned disposable local Review database and content-free assertions; runtime SQL/UI/migrations and remote state remain unchanged.
- **Writer / bounded repair evidence:** Sol/medium sole writer captured exit 0 for the initial focused `/opt/homebrew/bin/node scripts/acceptance/run-review.mjs --stabilisation`: 169 disposable migration replay, zero error-level database lint, observed competing Keep-versus-Move locks for both types with one Move/decision/related Activity and the original exact version/asset, and mutation-first locks for human key, current source and Matter Trash. Each committed key correction, source transition and Trash won an observed two-session wait; the waiting old resolver returned `stale`, with no Move receipt, Review decision or related Activity. Governed source return/key re-verification/Restore established fresh current Review for later choices. A synthetic trigger refused decision insertion *after* inner governed Move had changed the destination and written its receipt; both typed resolvers returned `failed` with identical before/after filing/version/asset/decision/Activity counts. Candidate validation-row UPDATE was rejected by the immutable-row guard, not counted as a mutable race pass. Initial exact QA suggested count-only Activity was weak; its reviewer later withdrew this and missing role/tenant duplication as blockers because prior unchanged D08-T05/T06 acceptance covers both. One bounded test-only `811b85b` enhancement asserts successful actor, Matter, mode/action, revision, subject and exact target/version. Its first run exited 1 on SQL `->>` concatenation precedence, corrected before a final captured exit 0 with empty stderr; no product defect. Touched Node 24 syntax and diff hygiene passed; owned stacks removed, ordinary local stack/live `.next` untouched.
- **Independent QA / integration:** Initial Terra/high read-only QA confirmed exact scoped static two-session/fault structure, diff/syntax and Node 24 runner-contract 2/2 but retained only stdout from its first full run; it stopped before fixture output and lost exit/stderr, so that invocation is **unclassified and not acceptance**. Independent exact `811b85b4f58627f20f091c9c43065436217ef79b` re-QA captured pinned Node `v24.8.0`, runner process exit **0** in 42.18s, 169 disposable replay, `{"results":[],"message":"db lint"}`, fixture `DO`, every observed single/multi overlap, committed stale/zero-effects case, Restore/reverification, late fault rollback and event attribution, with final owned Docker container absent. No P0–P3 findings; static syntax/diff and unaffected runner contract 2/2 pass. No browser/build was rerun because no product UI/runtime code changed; earlier exact D08-T05/T06 browser/type/RLS evidence remains a prerequisite, not a new deployed pass. Forced overlapping browser Server Action responses, ordinary paid extraction quality, representative scale, actual hosted concurrency and release-wide deployed rehearsal remain unverified. D12 real adjudicated corpus/paid Mumbai provider quality and D13 named operators, compliant host, tested database-plus-private-Storage recovery and incident response still block confidential deployment. No production data, paid provider, real email or remote service changed in this packet.
