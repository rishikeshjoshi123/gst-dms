# D12-T01 — Local OCR acquisition benchmark acceptance evaluator

- **Outcome / owner / date / contract:** D12-T01, current implementation task, 2026-09-15. An existing provider-free page-mode evaluator now produces a content-safe, fail-closed local report against the [September 11 acquisition decision](../decision-history/2026-09-11-ocr-acquisition-benchmark-thresholds.md) and the canonical [AI evaluation plan](../plans/platform/2026-08-24-ai-extraction-and-model-lifecycle.md#prompt-and-model-evaluation). This is an executable prerequisite for the mandatory source-grounded extraction journey, not a claim that provider quality or a production corpus passed.
- **Implementation:** `7f1acf0`, bounded repair `ec0f2dd`. The version-2 manifest selects 60–100 unique source pages, the exact pinned Mumbai processor, required representative categories and an independent-source-comparison label claim. Every applicable GSTIN, official reference, date, amount, material table cell and evidence anchor has item-level adjudicated labels; unknown or failed critical items and absent applicable coverage fail. The gate computes OCR-routing 100% recall and at least 90% precision, mean adjudicated OCR character/word accuracy at least 98%/95% with category and missing-label breakdown, mean acquisition at most 10 seconds/page, a measured bounded 100-page run at most 20 minutes, and OCR charges at most USD 0.01/billed page/USD 1 per fully OCR-processed 100 pages. Unknown currency conversion fails rather than inventing FX.
- **Evidence and safety:** evaluator selections, source/page hashes, output directories, route/charge summary and selected-page raw output are matched to the generated evaluator run. Private, regular, SHA-verified local source-comparison/adjudication, billing/pricing and 100-page event artifacts bind the exact run, selection, labels, billed count and measured span; missing/tampered/mismatched artifacts reject. The report contains hashes, counts and dispositions, not source content, field values, directory paths or raw provider text. A successful local threshold computation is explicitly `local_evidence_linked_not_provider_approved` with external authenticity `not_verified` and provider approval `not_evaluated`; fabricated *authenticity* cannot be ruled out by local hashes alone. Field-class Review requirements are separate from page transcript disposition. Every page remains non-indexable pending external acceptance and Search gates; global numeric failure blocks all page indexing without declaring otherwise accurate text unreadable. No Search mutation occurs.
- **Verification / QA / coordinator:** Sol/medium writer passed `npm run test:ocr-acquisition-benchmark` on Node 24 (26/26), `npm run typecheck`, scoped lint, diff hygiene and deterministic synthetic private CLI report. Terra/high independent QA at `7f1acf0` identified four P1/P2 evidence/indexing/report defects, which the one bounded repair closed. Exact-commit re-QA at `ec0f2dd` passed with no P0–P3, focused tests 26/26 and TypeScript. The coordinator inspected both scoped source diffs, the evaluator linkage/caller, precise September 11 thresholds and fail-closed synthetic cases before committing the code. No disposable database, UI or provider execution was applicable for this local script; no broad build was repeated. The reviewed integration is `ec0f2dd`, with this receipt committed separately.
- **Open acceptance:** the authorised 60–100-page real independently adjudicated corpus, authentic provider/pricing/billing and 100-page measurement, paid Mumbai execution, and separate source-grounded extraction and page-aware Search quality/citation/rollback gates remain unverified. Confidential deployment remains blocked by production identity/owners, compliant hosting, tested database-plus-private-Storage recovery, incident readiness and exact deployed rehearsal. No paid provider, remote service, production data, real email or deployment was changed.
