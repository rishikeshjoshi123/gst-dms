# D09-T03 — Typed extraction periods and reference observations

- **Outcome / owner / date:** D09-T03; current implementation task; 2026-09-12. This tranche applies the [September 11 Matter-identity decision](../decision-history/2026-09-11-matter-identity-and-extraction-normalization.md) and the AI [normalization](../plans/platform/2026-08-24-ai-extraction-and-model-lifecycle.md#approved-normalization-contract-2026-09-11) and [candidate](../plans/platform/2026-08-24-ai-extraction-and-model-lifecycle.md#append-only-extraction-and-candidates) contracts to ordered typed tax periods and role-separated official-reference observations. Structurally valid extraction can retain these facts as immutable, page-evidenced candidates without verifying Matter identity or performing matching, placement or relationship creation.
- **Base and tested code:** implementation `1e943f1`; validation repair and exact accepted revision `62b42d5f0e9b3aa72cf3925966e87dc22aa331a4`. The commits contain the canonical strict Zod input, recursively derived provider schema, prompt/schema/catalogue/normalizer version updates, D09-compatible reference normalization, typed provenance materialization, migration `00149`, generated types, SQL/Node fixtures, and removal of the D09-T03 path's legacy relationship-placement effect. Existing instruction, decision, plan, ledger, handoff, proxy and concept changes stayed outside the implementation commits.
- **Developer evidence:** fixtures cover month, quarter, exact-date range, financial year, multi-year, non-contiguous and unclear periods without invented day bounds; printed-versus-derived FY conflict is deterministic candidate data. Self identifiers and outbound mentions retain separate stable keys, source role, namespace, normalized components and evidence; partial, unknown and uncatalogued values remain non-matchable. The structured database boundary recomputes period derivation/conflict, validates exact JSON types and every FY array, fences source-run versions, preserves immutable source-to-document/copy materialization, and denies cross-tenant/direct DML. Replays produced no Matter identifiers, document links, effective relationships, placement or outbox effects. After QA's first rejection, the repair made pre-normalization identifier checks Unicode-aware, rejected control characters before trimming, and rejected semantically impossible printed FY values. Coordinator review and focused Node 24 tests passed 19/19.
- **Independent QA:** `/root/normalization_qa` rejected `1e943f1` for the three validation gaps above, then accepted exact `62b42d5f0e9b3aa72cf3925966e87dc22aa331a4` with no P0–P3 finding. QA independently reproduced Unicode/NFKC parity, impossible-FY rejection and seven control-character cases; replayed all 149 migrations, every registered fixture, both concurrency harnesses, seed and private Storage; passed migration tests 3/3, type-refiner tests 4/4, generated/refined type byte parity, TypeScript, scoped ESLint, error-level database lint and commit-range diff checks. An isolated provider-neutral Next 16.2.9 webpack build compiled in 19.0 seconds and completed all static generation. The existing checkout lock/processes were not touched and the disposable QA copy was removed.
- **Integration / deployment:** locally integrated at `62b42d5`; this receipt records that tested parent. Not deployed. No provider, hosted database, remote service or real email was invoked.
- **Remaining work:** this closes D09-T03 only. Typed client identifiers, legally classified dates, issuer/recipient and party roles, exact monetary components/legal posture, and normalized legal provisions remain on the approved material-field audit; complete those as D09-T04 before relying on them as matching signals. Bidirectional exact matching, targeted reevaluation, activation/placement, fuzzy suggestions, governed split/copy repair and Review UI remain later bounded tranches. The `WORK-REVIEW-CONCEPT` gate remains unresolved.
