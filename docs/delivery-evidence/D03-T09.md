# D03-T09 — Daily midnight-IST durable Trash purge recovery

- **Outcome / owner / date:** D03-T09; current implementation task; 2026-09-15. The first-release routine permanent-deletion sweep is registered once daily at `00:00 Asia/Kolkata`, rather than every minute. Each wake scans due and overdue operations in bounded keyset pages, claims bounded durable jobs and continues when a page or claim batch is full. Failed Storage effects, expired leases and exhausted attempts use database-owned state and failure-specific delayed reconciliation. The governed manual purge wake remains immediate; exact logical expiry already ends ordinary access/Restore before physical cleanup.
- **Base and accepted code:** implementation `22b52f1` on D13-T01 evidence `eab76d4`; bounded repair `a16b8fe`. Migrations `00163`–`00164`, generated types, Trigger registration/worker, focused tests and rollback-only SQL fixtures are included. The minute purge reconciler and parked 24-hour Team-attention projector are no longer registered, and the pilot release manifest gates the daily sweep. No remote Trigger schedule was changed.
- **Durable safety:** the worker catches thrown Storage transport errors and records failed deletion effects rather than silently abandoning a live lease. SQL snapshots `next_attempt_at` with bounded exponential backoff, keeps Retry unavailable before its due clock, reconciles stale leases, and terminalizes exhausted `retryable` and `waiting_storage` work as content-safe `blocked/retry_exhausted` with a `purge_failed` root; governed Owner/Admin manual retry can resume the prepared job. A private, force-RLS singleton sweep cursor advances transactionally past held roots and survives a lost Trigger continuation. A short page resets progress so released holds are revisited. Holds, shared assets, dependency fences, source grants and tombstones remain under the existing canonical purge commands.
- **Dependency repair:** clean purge SQL exposed a D08-T04 `ambiguous_intake_review_lifecycle` trigger-shape regression: a Matter update could evaluate file-asset-only fields. Migration `00163` replaces that function with explicit per-table branches while preserving Intake/asset/Matter invalidation. Expanded D08 SQL covers Intake, asset, Client and Matter Trash closure; the ordinary `dms` stack was not patched in place.
- **Developer runtime evidence:** an exclusively owned disposable PostgreSQL stack replayed all 164 migrations, returned zero error-level database lint and generated/refined types twice byte-identically. The root governed purge rollback fixture passed exact stale-lease and Storage backoff, due clock, exhausted `waiting_storage`/manual recovery, shared-asset and hold guards. A 102-root adversarial fixture passed 100 held-first roots, lost continuation, next null daily wake reaching later overdue work, hold release, duplicate wake and direct ACL denial. Existing two-session claim/Restore/post-claim blocker concurrency and D08 placement producer/resolver/lifecycle SQL passed. Node 24 focused/manifest/release checks 32/32, migration checks 3/3, type-refiner 4/4, TypeScript, scoped lint, diff hygiene and isolated no-env Next 16 webpack build passed.
- **Independent QA / limits:** Terra/high QA on `22b52f1` found blocked-root starvation after a lost continuation, unclaimable exhausted `waiting_storage` and stale schedule manifest/tests. The bounded repair closed all three. Exact-commit re-QA passed `a16b8fe` with no P0–P3 after independent source/Trigger fairness review and focused Node checks; QA did not duplicate the writer's disposable SQL stack. Both owned database stacks and temporary build/source copies were removed; the user-owned live `.next` and ordinary `dms` Supabase stack were untouched. Actual deployed India-time wake, duplicate/missed Trigger delivery, hosted private-Storage physical deletion, fleet cost, monitoring, compliant hosting, database-plus-Storage recovery, named go-live/incident owners and confidential-data acceptance remain open. No production data, paid provider, remote service, real email or deployment changed.
