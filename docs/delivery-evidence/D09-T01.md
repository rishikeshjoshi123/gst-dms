# D09-T01 — governed Matter external-identifier authority

- **Outcome / owner / date:** D09-T01; current implementation task; 2026-09-12. This tranche implements the foundation approved by the [Matter identity decision](../decision-history/2026-09-11-matter-identity-and-extraction-normalization.md), the Hub Matter-identity contract, AI normalization contract and Trash reservation boundary. CaseChain can store, correct, revoke and narrowly read source-verified typed external identifiers without treating client/year or fuzzy metadata as identity.
- **Base and tested code:** implementation and exact accepted revision `6e9fa9e`. The commit contains migration `00147`, its rollback fixture/runner registration, deterministic generated types and refiner coverage. Existing instruction, decision, plan, ledger, handoff, proxy and concept work remained outside the commit.
- **Developer evidence:** a clean Node 24 reset replayed all 147 migrations and every registered SQL fixture. The identifier fixture proves immutable versioned kind/normalizer catalogues; conservative separator-preserving normalization; raw, display, component and current-source evidence retention; Owner/Admin/Associate authority; Viewer, suspended, ambiguous-member and foreign-tenant denial; suggestion/non-identity non-reservation; organisation-wide exact reservation across active, Trash and permanent purge; replay/CAS; atomic correct/revoke; append-only decisions, private receipts and exact Activity/projector-outbox effects; narrow projection; and authenticated/service-role direct-DML denial. Coordinator review found substring evidence could accept a longer alphanumeric token; the implementation was repaired to require literal whole-token boundaries and added prefix/suffix near-collision checks. Migration tests passed 3/3, refiner tests 4/4, generated types regenerated byte-for-byte, TypeScript and scoped ESLint passed, and diff checks were clean.
- **Independent QA:** reviewer `/root/relationship_qa` found no P0–P3 issues on exact `6e9fa9e`. Clean `acceptance:db` passed through migration 147, onboarding concurrency, seed and private Storage. An independent simultaneous normalized-key race returned one `ok` and one `identifier_conflict`, leaving exactly one identifier, decision, receipt, Activity and projector-outbox effect; winner/loser Matter revisions were correct. A superseded-version probe returned `invalid_evidence`; service-role mutation was permission-denied; an owner-level immutable-field forgery was trigger-rejected. Type parity, migration inventory, type/refiner tests, Node 24 typecheck, scoped lint, diff checks and an isolated provider-neutral webpack build passed.
- **Integration / deployment:** locally integrated at `6e9fa9e`; not deployed. All database identities and records were disposable local fixtures. No remote service, hosted database or real email changed.
- **Remaining work:** this closes D09-T01 only. D09-T02 should integrate the authority into manual Matter creation and Restore, remove the legacy client/year uniqueness only with explicit same-year and multi-year acceptance, and preserve exact active/Trash collision denial. Placement, bidirectional matching, extraction/OCR suggestions, split/copy repair, Copy/Move, Review/UI and deployment remain later tranches or gates.
