# D01-T04 — Deterministic signed-URL result narrowing

- **Outcome / owner / date:** D01-T04; current implementation task; 2026-09-08. This bounded repair restores repeatable execution of the repository’s ordinary incremental TypeScript command after it intermittently retained invalid narrowing state for the canonical document page.
- **Base and tested code:** discovered while verifying D02-T02 on base `9ef998f` with its owned client changes still unstaged and all unrelated dirty work preserved. Implementation commit `19f81dc` contains only `src/app/(app)/documents/[docId]/page.tsx`.
- **Developer evidence:** `npm run typecheck` reported two `TS2339` errors for `.url` on the signed-document success/error union, while `npx tsc --noEmit --pretty false --incremental false` and both Next production build type phases passed. The page now discriminates the union explicitly with `'error' in signedDocument` and `'url' in signedDocument`, preserving the same error, PDF, and no-file rendering states. Two consecutive ordinary `npm run typecheck` runs and scoped ESLint/whitespace checks pass after the change. The final combined checkout also passes webpack and default Turbopack production builds.
- **Independent QA:** not required for this local type-narrowing-only repair. It changes no authorization, persistence, routing, source identity, visible copy, or layout; the parent D01 still requires independent release/build review.
- **Coordinator check / integration:** inspected the exact one-file diff and the `getCanonicalDocumentVersionSignedUrl` return union. Locally committed as `19f81dc`; not deployed.
- **Remaining work:** this closes only D01-T04. D01 remains in review for its previously recorded independent QA, clean database replay, broad lint classification, and remote Trigger/release rehearsal gates.
