# D01-T06 — Repeatable local browser and database acceptance

- **Outcome / owner / date:** D01-T06; bounded implementation run; 2026-09-11. This tranche implements the local acceptance foundation selected by the [delivery workflow](../plans/operations/2026-09-08-agent-delivery-workflow.md#current-bounded-run). It does not claim hosted, release, provider, or email acceptance.
- **Base and commits:** base `7ef8520`; implementation `ef00570`; bounded repair `8804653`. The existing checkout was clean at startup. Unrelated work was preserved. The runner refuses non-loopback Supabase, Storage, Trigger, or application targets and now requires the repository's exact Node 24 runtime for orchestration, Next, Playwright, and Supabase module launches.
- **Repeatable database path:** `npm run acceptance:db` starts the configured local Supabase stack, performs a clean `db reset --no-seed`, applies every migration through `00142`, executes the existing Team and upload SQL fixtures with fail-fast error handling, builds the deterministic five-user/two-tenant seed, generates a synthetic four-page PDF, and proves private Storage upload/download/hash identity. Both SQL fixtures finish inside transactions and roll back their test mutations.
- **Repair:** the live Team fixture exposed PostgreSQL `42804` in `get_team_directory`: `auth.users.email` was not returned as the RPC's declared `text`. Migration `00142_team_directory_email_text_return.sql` casts the email and types empty-state sentinel values while preserving the RPC signature, `SECURITY DEFINER`, stable/search-path posture, and authenticated-only execution grant. The fixture now also creates a canonical duplicate membership before proving corruption rejection. Shared `FormField` markup gained required label association plus merged hint/error descriptions, `aria-errormessage`, invalid state, and alert semantics after QA found the original accessibility repair incomplete.
- **Verification:** clean replay, both SQL fixtures, deterministic seeding, two runtime-enforcement tests, TypeScript, changed-file ESLint, migration checks, and diff checks passed. Independent Sol/high QA first reported the Node/runtime and field-description defects, then re-ran the exact repaired commit and found no P0–P3 issue. A provider-neutralized Next 16.2.9 webpack production build also passed. The complete later `npm run acceptance:local` replay is recorded in D04-T03 because it exercises this runner end to end.
- **Integration / limits:** locally committed; not deployed. Hosted schema cache, remote Trigger/provider execution, confirmation-required signup/captured mail, release-environment configuration, and the exact production journey remain gates. The local seed uses auto-confirmed synthetic users and contains no client data.
