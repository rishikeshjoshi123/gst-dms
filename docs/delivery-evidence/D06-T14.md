# D06-T14 — Ready Intake placement to exact Workbench and Matter

- **Outcome / owner / date:** D06-T14; current implementation task; 2026-09-15. An authorised Owner, Admin or Associate can choose an accessible lifecycle-active Matter for a ready global Intake item, commit one canonical placement, and continue directly into the exact server-returned document version at page 1 in Workbench. “Back to Matter” opens the assigned Matter. A Matter's procedural status, such as `tribunal`, no longer incorrectly blocks placement.
- **Base and tested code:** exact implementation commit `8e38b9bbb51bfc1571db4041afa894ba575a4a0b`, based on D08-T03 evidence commit `e11e292`. Migration `00158`, the canonical assignment action/Hub handoff and focused SQL/route/browser coverage are included. D04-T03, D05-T01, D06-T12/D06-T13 and D03-T08 remain the relevant upload, materialisation, exact-source, shared-Intake and release-policy dependencies.
- **Delivered authority and lifecycle:** `assign_intake_to_new_document` retains its `SECURITY DEFINER` fixed search path, capability-derived actor, tenant/uploader/asset/current-state fences, advisory locks, actor-bound idempotency, duplicate detection and atomic document/version/assignment/Activity/processing-intent effects. Destination eligibility uses the Matter and Client lifecycle (`record_state='active'`, not deleted), not the Matter's procedural `status`. Placement references the existing private asset rather than copying bytes. The action returns the database-selected immutable version, and the Hub navigates from that exact result instead of optimistically deleting the queue row and performing a fallible post-commit refresh.
- **Developer and coordinator evidence:** one Terra/medium writer implemented the bounded packet and two consolidated repairs. The first real browser run exposed the procedural-status defect in the canonical database command; a forward migration fixed it without rewriting history. A clean disposable local reset replayed all 158 migrations and the repository SQL/concurrency harness, seeded and verified private synthetic PDFs, and exited successfully. Node 24 focused route/search/queue tests passed 13/13; TypeScript, scoped ESLint and diff hygiene passed. Chromium then completed ready global Intake → `tribunal` Matter → exact document/version/page-1 Workbench with rendered private PDF → Back to Matter in 15.6 seconds. The best-effort outbox wake was unavailable locally, while durable scheduled-recovery work remained authoritative.
- **Independent QA:** Luna/high reviewed exact clean commit `8e38b9b` and found no P0–P3 defects. Migration, 5/5 exact-route tests, TypeScript, scoped lint and commit hygiene passed. Static adversarial review confirmed the preserved authority, tenant, lifecycle, duplicate, replay and byte-reference fences. QA could not independently launch disposable Supabase/browser acceptance because its sandbox blocked Supabase telemetry under the home directory; coordinator clean-replay and browser evidence above therefore establish those runtime gates. The committed SQL fixture does not separately enumerate every foreign/inactive/deleted/stale/uploader case, but the unchanged fences and broader passing harness retain that coverage.
- **Exclusions / remaining gates:** this closes ordinary ready-Intake manual placement, not ambiguous-placement or possible-duplicate typed Review, automatic placement, provider/acquisition quality, hosted Storage/schema-cache behavior, representative performance, deployment or production data. Confidential production remains blocked on named go-live/rollback/support/incident ownership, compliant hosting, tested database-plus-private-Storage recovery, provider/configuration acceptance and exact deployed rehearsal. No remote service, paid provider, production data or real email was used.
