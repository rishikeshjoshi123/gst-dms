# D11-T12 — Source-grounded explicit legal due dates through typed Review

- **Outcome / owner / date:** D11-T12; current implementation task; 2026-09-15. A current immutable document version with a valid, exact, explicitly stated `document.legal_date.due` extraction candidate can show a source-cited *provisional* legal date in its Matter agenda and typed Review. Only an authorised Owner, Admin or Associate can verify the cited date, correct it with a reason, reject it, or clear it. A provisional date is not a verified deadline or Dashboard attention fact.
- **Base / code:** D06-T15 evidence `dc78d2b`; implementation `b7bd384`; independent-QA Dashboard refresh repair `48a1261`. Migration `00165`, deterministic generated types, producer/binder/finisher ordering, canonical resolver/readers, Review and Matter agenda interfaces, and isolated SQL/concurrency/browser acceptance fixtures comprise the packet. D11-T11's governed manual deadlines, D08-T02's typed extraction Review, D06-T12's exact private-PDF Workbench, and current immutable provenance mapping are the reconciled dependencies.
- **Authority and source behavior:** The producer admits only valid explicit date-only, page/quote/anchor-grounded current-version candidates. It does not infer statutory, relative, or event dates. Normal processing reconciles old extraction Review before date production; nonprocessing Copy/attachment binding is independently idempotent. One immutable candidate binding creates an inert provisional deadline and one Review item. The fixed-authority resolver checks membership, tenant, current source/version/anchor and optimistic revision under row locks, applies actor-bound replay semantics, and appends immutable decision and deadline-version history. Competing decisions have one winner; source replacement/Trash cannot present stale verified attention. Authenticated direct deadline/history access and foreign-tenant/Viewer mutation remain denied. Successful human decisions invalidate Review, Matter, Document and Dashboard reads; unsuccessful outcomes do not.
- **UI / local acceptance:** Typed Review presents the exact cited source and explicit verify/correct/reject/clear choices; Matter labels provisional dates as needing verification and distinguishes them from verified timezone-truthful agenda conditions. The writer's owned disposable 165-migration replay, error-level database lint, deterministic type parity, rollback/authority/Copy/source-replacement/replay SQL, two-actor concurrency, authenticated RPC, 19 focused tests, TypeScript, scoped lint, isolated webpack build and stateful Chromium 1/1 at 320px dark/keyboard and desktop passed. The repair adds a focused Dashboard invalidation regression; its 10 focused tests, TypeScript, scoped lint and diff hygiene passed without rerunning unchanged database/browser/build layers.
- **Independent QA / remaining gates:** Terra/high exact-commit QA on `b7bd384` found one P2 Dashboard invalidation omission and no other P0–P3. Its 19 focused tests, TypeScript, touched lint and disposable due-date SQL harness passed; it did not independently repeat costly Chromium/build, and a separate isolated Review probe's fixture assertions were not captured. Final exact-commit QA accepted `48a1261` with no P0–P3: retained focused tests plus repair 20/20, TypeScript, scoped lint and diff hygiene passed; unchanged costly SQL/browser/build were not duplicated. Paid/provider extraction quality and independent real corpus remain D12-gated; no ordinary OCR quality is proved by synthetic candidates. Reminder scheduling/email, inferred deadlines, conditional Search, broad organisation agenda, deadline Activity registry, hosted Storage/schema, representative scale, production ownership/hosting/recovery/incident decisions, and exact deployed confidential-data rehearsal remain open. No remote service, production data, paid provider, real email or deployment changed.
