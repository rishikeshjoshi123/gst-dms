# D03-T08 — Executable first-production release matrix

- **Outcome / owner / date:** D03-T08; bounded implementation task; 2026-09-15. The repository-owned pilot manifest now records the settled September 14 release boundary and a provider-free preflight distinguishes release-matrix consistency from confidential-production readiness. Canonical scope is the pilot plan's September 14 scope, “Freeze the pilot journey and fixtures,” interfaces and release acceptance sections.
- **Base and tested code:** implementation commit `2afcbebd50ff39feef7cba4589c1f53cdab6735b` on `dev`, based on `e9eb227`. It reconciles D03-T05's manifest foundation to the October 12–15 target and October 20 final date; the complete signup → upload → processing/manual recovery → typed Review → placement → exact Workbench → Matter journey; both upload origins; release-critical versus compatibility routes; mandatory, conditional and deferred capabilities; relationship suggestions as the last optional cut; PDF/storage limits; and local/pre-production versus confidential-production data policy.
- **Preflight and gate behavior:** `preflight:pilot-release:local` validates only repository policy and exits successfully without claiming the application journey is ready. `preflight:pilot-release` exits nonzero while exact revision/environment identity, named go-live/rollback/support/incident ownership, compliant hosting, database-plus-private-Storage recovery and provider/deployed acceptance remain unverified. Even a future verified identity flag fails closed unless the named environment, organisation UUID and 40-hex revision inputs are valid. The report prints no identity values, credentials or secrets and performs no network, provider, database or deployment action.
- **Developer/coordinator evidence:** the sole Terra/medium writer passed 17 focused tests, TypeScript, scoped lint and diff checks. Coordinator review rejected misleading local-readiness naming, missing Team/Trash critical routes, unvalidated identity inputs and one duplicate route classification; the writer repaired them. On Node 24.8.0 the coordinator then passed 17/17 focused tests, scoped lint, TypeScript, runtime-contract 2/2, migration-numbering 3/3 and the full Next 16.2.9 Turbopack production build (23 static pages). Local preflight exited 0 and the confidential release preflight exited 1 with the expected blockers.
- **Independent QA:** Luna/high QA inspected exact `2afcbeb` from an isolated `git archive` copy, passed the equivalent direct Node/tsx suite 17/17, both preflight exit modes, source/diff/security review and found no P0–P3 issue. Its npm TSX wrapper was sandbox-blocked by IPC `EPERM`; clean-copy typecheck lacked generated `.next/types`, and its Turbopack build was interrupted after stalling. Those limitations are explicit; coordinator Node 24 type/build evidence covers the affected unchanged commit without a duplicate QA claim.
- **Integration / remaining work:** locally integrated, not deployed. This closes only the executable scope matrix and current fail-closed gate report. It does not establish mandatory journey implementation, provider quality, schedules, remote configuration, recovery, monitoring, incident ownership or confidential-data acceptance. The next recommended packet is the earliest ready mandatory upload-successor: truthful processing failure/manual recovery through its applicable typed Review path, without starting conditional features.
