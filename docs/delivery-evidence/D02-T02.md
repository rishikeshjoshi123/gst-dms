# D02-T02 — Client mutation authority

- **Outcome / owner / date:** D02-T02; current implementation task; 2026-09-08. This tranche implements Organisation [caller closure](../plans/platform/2026-08-26-organisation-administration.md#existing-caller-and-directory-closure), [capability rules](../plans/platform/2026-08-26-organisation-administration.md#roles-ownership-and-capabilities), and relevant [acceptance](../plans/platform/2026-08-26-organisation-administration.md#testing-and-acceptance-criteria): Viewer access no longer implies client write authority.
- **Base and tested code:** base `19f81dc` with unrelated dirty concept, design-system, approval, proxy, Task-reader, and planning edits preserved. Implementation commit `6f4b76c` contains only the commands/schema, generated types/refinement, client actions/create caller, and focused tests.
- **Developer evidence:** migration `00125` applied to the disposable local schema and local type generation emitted the receipt/revision/RPC contract. The rollback SQL fixture passes operational Associate/Admin/Owner create paths; Viewer, suspended, removed and cross-tenant denial; active-client enforcement; create/update replay; changed-key-payload rejection; stale-revision conflict; one Activity row per committed effect; and direct authenticated/service-role table/receipt/RPC denial. A two-session fixture proves membership suspension waits for one atomic create plus Activity/receipt, then denies later mutation. The live create dialog holds one opaque key across retry; both exported actions use authenticated RPCs and contain no service-role client writes. Refiner tests (3), focused source tests (2), ordinary TypeScript, scoped ESLint, migration uniqueness (125), schema lint, whitespace, and both Next 16.2.9 builds pass under Node 24.
- **Independent QA:** pending. This security/migration tranche requires a separate read-only reviewer before D02 integration; the current task remained the only writer.
- **Coordinator check:** inspected the full scoped diff and all live `clients` writes. Direct client INSERT/UPDATE policies and grants are removed from browser and service roles; the postgres-owned commands derive exactly one active membership, reject Viewer, lock membership/client state, normalize bounded identifiers, serialize a unified idempotency namespace, use CAS on update, and record Activity and receipts transactionally. Existing governed Trash commands remain security-definer lifecycle paths.
- **Integration / deployment:** locally committed as `6f4b76c`; not deployed. Clean replay remains unverified because the local Supabase CLI reset fails before replay; additive apply, generated types, rollback SQL, concurrency, schema lint, and builds were verified.
- **Remaining work:** this closes D02-T02 only. D02 remains in review pending independent QA, matter/document direct-write closure, safe-directory migration, live note moderation-reason UX, and clean replay. The exported update action has no current UI caller; its direct command/action contract is verified without claiming a live edit workflow.
