# D12-T02 — Authoritative private source for document processing

- **Outcome / contract / date:** D12-T02, 2026-09-15, closes the local live-worker source-authority prerequisite of the [September 14 mandatory journey](../decision-history/2026-09-14-first-production-release-scope.md). A delivery-leased processing child now receives only its run ID and lease token. The service-only grant derives the exact current valid document version and available private PDF asset from the running claim, tenant, active Document/Matter/Client and Trash state. The worker downloads that granted object, verifies byte count and SHA-256 against the server asset, and rechecks source authority before provenance begin, page acquisition and model entry. A task-provided path, ignored direct matter read and legacy direct duplicate query no longer decide processing. D04 upload-side exact-duplicate protection remains intact.
- **Implementation / security:** `a4797bd`, bounded independent-QA repair `8fa70fa`. Migration `00166` binds grant and provenance begin in one transaction with hierarchy locks, denies browser RPC execution, and returns no locator for stale/invalid source. Missing Storage, hash mismatch and transient errors fail closed without turning into a fabricated Review result. Worker/task results and logs do not emit document/tenant IDs, object keys, hashes or content. The existing validated provenance/typed Review finisher remains authoritative; neither paid provider quality nor unsafe extraction reprocess is enabled here.
- **Recovery / provenance:** After begin but before model invocation, an exact still-running historical processing/nested claim may cancel uncalled work even when both leases have expired. It checks every attempt for absent call marker, provider IDs, usage and candidates, renews the nested terminal fence only under those locks, terminalizes the run and attempt with `source_revoked_before_model`, creates content-free exact-version lineage for Trash ownership and cancels the outer run atomically. The generic `provider_failed` schema state is **not** evidence of a provider call in this case; the explicit safe code and null call/usage fields distinguish no-call. The call-start marker is immutable. Marked possibly-called work cannot be reset to queued through either the current wrapper or legacy service begin, and cannot be falsely cancelled; it requires governed operator recovery after timeout. Document Copy excludes only this no-call binding from validated materialization, while retaining approved native page source and validated evidence.
- **Verification / QA:** One Sol/medium writer ran focused worker/Trigger tests, TypeScript, scoped lint and the pinned-Node-24 isolated `run-processing-source.mjs --types --copy --concurrency`: migration replay through `00166`, error-level DB lint, repeated generated-type parity, rollback tenant/lease/version/asset/Trash/hash/binding and Copy SQL, plus two-session begin-versus-Trash ordering. Independent Terra/high exact `a4797bd` QA reproduced two P1 expiry/replay gaps despite passing checks; one bounded repair added marker/expiry/successor fixtures. Independent exact `8fa70fa` re-QA passed those cases with no P0–P3, isolated replay/type/Copy/concurrency, focused tests 9/9, typecheck, migration uniqueness and diff hygiene. Owned Docker stack/data were removed; ordinary local stack and unrelated workspace changes were untouched. No paid provider, remote configuration, production data, real email or deployment was used.
- **Remaining acceptance:** D12 still requires the authorised independently adjudicated 60–100-page real corpus and bounded paid Mumbai Google Document AI/`gemini-2.5-flash` quality, regional, cost and billing evidence. D13 still requires named go-live/rollback/support/incident owners, compliant hosting, tested database-plus-private-Storage recovery, monitoring and exact deployed confidential-data rehearsal. Synthetic structural checks are not provider or production acceptance.
