# D02-T09 — Retire legacy document review writes

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `2e78393`

**State:** review; independent QA and clean migration replay remain unverified.

The legacy `dismissReviewFlag` action now fails before database-client creation, both relationship helpers no longer rewrite overloaded `documents.status`/`review_reason`, and the dashboard surfaces the unavailable result instead of refreshing as though dismissal succeeded. Migration `00132_document_legacy_review_update_authority` removes authenticated/service direct `UPDATE` authority over `status`, `review_status`, `review_reason`, `reviewed_at`, and `reviewed_by`. Typed Review items and database-owned processing commands remain the intended authorities.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: rollback-only `document_legacy_review_update_authority.sql`; local error-level schema lint; the 132-version migration check; the scoped eight-test document/chaining suite; `npm run typecheck`; `git diff --check`; and both webpack and default Turbopack production builds. The privilege-only migration does not change generated TypeScript schema shape. Scoped ESLint has no new diagnostics; existing `any` debt remains in untouched action lines, plus a pre-existing unused `Link` import in the dashboard panel.

Coordinator review inspected the staged diff, verified dismissal is fail-closed, confirmed cross-matter relationship detection still emits its existing notification but does not manufacture legacy Review state, and checked the live dashboard error path. Independent QA is pending because no independent reviewer was authorized. Deployment is not verified.

The disposable database still cannot clean-reset under Supabase CLI 2.111.0 (`LegacyDbBootstrapError`). Migration `00132` was applied explicitly on the locally current additive schema and verified with rollback-only SQL. This closes only D02-T09; residual unused direct document-update grants, typed Review migration/consumer work, live note-moderation reason UX, independent QA, clean replay, and release acceptance remain.
