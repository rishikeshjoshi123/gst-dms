# D01-T05 — Deterministic generated enum ordering

- **Outcome / owner / date:** D01-T05; current implementation task; 2026-09-08. This tranche closes the local generated-type parity defect under Platform Operations’ [development usage and release cutover](../plans/platform/2026-08-27-platform-operations.md#development-usage-and-release-cutover) and the delivery workflow’s [verification contract](../plans/operations/2026-09-08-agent-delivery-workflow.md#verification-and-bounded-repair).
- **Base and tested code:** base `85f34a8`; implementation commit `ce6d9d6`. Unrelated dirty approval, concept, design-system, proxy, and Task-reader changes were preserved and excluded. A clean disposable local Supabase replay through migration `00135` first exposed enum-order and Constants parity drift in the tracked generated file.
- **Developer evidence:** the type refiner now sorts the generated `Database.public.Enums` and `Constants.public.Enums` blocks by enum name, preserves every enum member sequence, retains nullable-RPC refinement, and emits one final newline. Behavioral refiner tests pass 4/4, including equivalent differently ordered inputs and malformed known-RPC failure. Two successive `npm run db:types:local` runs were byte-stable; TypeScript, scoped ESLint, migration checks, local error-level schema lint, and diff checks passed.
- **Independent QA:** Sol/high read-only QA reviewed `85f34a8` plus the exact unstaged three-file diff before commit. Two independently generated/refined local outputs matched each other and the 340,239-byte tracked file. AST comparison found 85 identically named and valued enum definitions in both maps. Adversarial probes proved changed enum-member order remains semantically distinct, extra trailing newlines normalize, and malformed required RPC types fail closed. It also rechecked the D01 Node/runtime boundary and 46 focused worker/outbox/native-PDF tests; no defects were found.
- **Coordinator check:** inspected the complete diff and live `db:types:local` caller. The generated API changed only by canonical enum-block ordering and restoration of the two membership enum Constants; table, function, nullability, and enum-member semantics are unchanged. Clean local reset, schema lint, migration/type checks, and a Next 16.2.9 webpack production build passed. The build shell emitted the known Supabase Node-20 deprecation warning; repository CI and Trigger remain pinned to Node 24.
- **Integration / deployment:** locally committed as `ce6d9d6`; not deployed. Remote Trigger preview, worker/provider/native rehearsal, promotion, rollback, and application deployment were not attempted and remain unverified.
- **Remaining work:** this closes D01-T05 and restores clean-replay/type-generation parity. D01 remains in `review` pending its remote Trigger and release-environment gates; no production claim is made.
