# D03-T03 — Disable pilot Case Brief generation

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `04d9796`

**State:** review; independent QA, browser acceptance, and deployed worker verification remain unverified.

The design-partner pilot now has one explicit, fail-closed Case Brief generation policy. The authenticated legacy Wiki action returns a truthful unavailable result before creating a Supabase client or scheduling background work. A direct `generate-matter-wiki` worker dispatch returns `disabled_by_release_policy` before service-role access, effective-metadata reads, usage accounting, or the Gemini call. The live Matter compatibility UI exposes neither Generate nor Regenerate controls; its empty state identifies Case Brief generation as unavailable while preserving historical content rendering. This closes the plan's required UI, server-trigger, and worker boundaries without deleting the later approved Case Brief implementation.

The touched compatibility component now uses the generated Wiki row/JSON types and derives optimistic local edits without effect-driven prop mirroring, eliminating its previous `any` and synchronous-effect lint failures. Manual edits to existing compatibility content remain unchanged; the tranche disables AI generation and automatic maintenance only.

Developer evidence passed on Node 24 / Next 16.2.9: a new four-test policy suite proves the disabled default, server-action ordering, worker ordering, and absence of live UI triggers. The existing effective Wiki context and Gemini provider-contract suites also pass, for 15 focused tests total. Changed-file ESLint, TypeScript, migration uniqueness (134), whitespace validation, and both webpack and default Turbopack production builds pass. No database shape changed.

Coordinator review inspected the shared policy, both executable boundaries, live UI, staged diff, and existing bounded effective-metadata/model tests. Independent QA was unavailable under the single-writer instruction. The implementation is locally committed as `04d9796`; Trigger preview and deployment are not verified. D03 remains in review pending the exact enforced release matrix, retention approval, browser/worker acceptance, independent QA, and remote release verification.
