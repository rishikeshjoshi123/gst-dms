# D02-T06 — Retire legacy document metadata writes

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `04c7994`

**State:** review; independent QA and clean migration replay remain unverified.

This tranche retires the unused `updateDocumentMetadata` action before database-client construction and removes authenticated/service direct `UPDATE` privileges for `ai_prompt_version`, `confidence_scores`, `doc_date`, `doc_type`, `financial_year`, `raw_metadata`, and `reference_number`. The existing service-owned `record_current_document_inspector_correction` remains the approved path: it binds corrections to the current immutable version/candidate and appends the governed field decision rather than rewriting mutable legacy metadata. No live caller used the retired action.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: rollback-only `document_metadata_update_authority.sql`; the existing `current_document_inspector_correction.sql`; local error-level schema lint; the 129-version migration check; the scoped eight-test document action suite; `npm run typecheck`; `git diff --check`; and both webpack and default Turbopack production builds. The privilege-only migration does not change generated TypeScript schema shape.

Changed-file ESLint now reports only the pre-existing `any` diagnostic at `src/lib/actions/document.ts:38`; the changed action and test introduce no lint findings. Coordinator review inspected the staged diff, confirmed no repository caller imports `updateDocumentMetadata`, and verified the guard contains neither client creation nor direct document update. Independent QA is pending because no independent reviewer was authorized. Deployment is not verified.

The disposable database still cannot clean-reset under Supabase CLI 2.111.0 (`LegacyDbBootstrapError`). Migration `00129` was applied explicitly on the locally current additive schema and verified with rollback-only SQL. This closes only D02-T06; direct document move, classification, legacy review/status mutations, live note-moderation reason UX, independent QA, clean replay, and release acceptance remain.
