# D06-T13 — Associate shared Intake access

- **Outcome / owner / date:** D06-T13; bounded implementation coordinator; 2026-09-11. This tranche gives Owner/Admin/Associate the approved ordinary organisation-wide Intake workflow while keeping Viewer and foreign-tenant access closed.
- **Base and integrated code:** base `79de711`; implementation `f0f54dd`, repaired through `b66c064f6e2589a734171fd6b3e7dceb1386d62a`. Browser reads now use authenticated capability-scoped RPCs rather than service-role lifecycle-table reads. `My uploads` remains the default and the locally remembered `All uploads` view survives Server Action refreshes. Viewer receives a capability-derived no-access state with no scope, upload, queue, source or triage controls.
- **Provenance and source authority:** migration `00144` exposes a content-safe uploader UUID/name without email or Storage locator. Shared assignment creates the document/version under the original uploader while recording the Associate separately as assignment and Activity actor. Exact replay retains one document/version, assignment and Activity event with the same live/receipt/result lifecycle revision. The Owner shared fixture has a distinct canonical private four-page PDF and verified bytes/hash.
- **Developer and coordinator evidence:** clean database replay, Team/upload/Trash+Intake SQL, both private Storage objects, type generation/refinement, TypeScript, 52 scoped source tests, scoped lint, diff checks and provider-neutral webpack build passed. The browser suite passed 9/9, including Associate selection of the Owner row, visible uploader comparison, signed PDF canvas, close/return with `All uploads` and the row preserved, plus Viewer control absence.
- **Independent QA:** Sol/high reviewer `/root/tranche_qa` repeated full acceptance on exact `b66c064` and independently opened the Owner source as Associate. A rolled-back SQL probe verified Owner document/version provenance, Associate assignment/activity attribution, stable revision `2` across exact replay, RPC privilege boundaries and no email/locator disclosure. No P0–P3 finding remained.
- **Integration / limits:** locally integrated; not deployed. Browser assignment itself was not performed; the authoritative SQL command plus real browser shared-source flow passed. Actual 200% browser zoom remains unverified. No remote service, scheduler or email changed.
