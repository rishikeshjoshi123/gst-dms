# D04-T03 — Real local signed upload and cancellation acceptance

- **Outcome / owner / date:** D04-T03; bounded implementation run; 2026-09-11. This tranche executes the existing upload authority fixture and proves the browser's live direct-private-Storage path, including completion, cancellation, and same-file reselection. It remains local evidence, not hosted token-expiry or release acceptance.
- **Base and commit:** base `f78f10e`; implementation `dcf8a53`. The browser drives Dashboard → Document Hub, confirms `/inbox` redirects to the same authoritative hub, enters a Matter Workbench, completes one real signed TUS transfer, and cancels two held transfers of the same selected file. A guarded loopback/service-role verifier then checks tenant ownership and the entire finalized session/intake/reservation/receipt/asset chain, exact object bytes/hash/MIME, one authoritative completion, and explicit Storage 404s for both cancelled objects.
- **Repairs:** local execution exposed that the signed resumable token had been sent to `/storage/v1/upload/resumable`; Supabase's signed TUS endpoint is `/storage/v1/upload/resumable/sign`. URL shaping now selects the signed route for both local and hosted base URLs. The upload modal also interpreted an all-cancelled batch as success and closed itself, preventing same-file reselection. It now keeps the batch available whenever any outcome is cancelled while retaining the existing successful close path.
- **Authority and adversarial evidence:** the existing SQL fixture proves byte/reservation limits, replay behavior, authorization, and cancellation inside a rolled-back transaction. Browser post-verification proves no cancelled object survived. Independent QA additionally attempted foreign-tenant cancellation and completion-receipt lookup; both returned `not_found` and caused no state change. No policy, grant, dependency, or service-role exposure was added by this commit; the privileged verifier refuses non-loopback targets.
- **Verification:** independent Sol/high QA ran `npm run acceptance:local` at exact commit `dcf8a5352865ef22c1edd71c335c7fc11a6dcf74`: clean migrations through `00142`, Team/upload rollback fixtures, deterministic users/PDF/private Storage seed, 7/7 Chromium scenarios in 30.5 seconds, and the post-browser authority verifier all passed. Its wider focused run passed 19/19. Node-runtime tests passed 2/2; TypeScript, scoped ESLint, migration checks, diff checks, and a provider-neutralized Next 16.2.9 webpack production build passed. QA found no P0–P3 issue.
- **Integration / limits:** locally committed; not deployed. Remote signed-token expiry, an actual 25 MiB transfer (the scalar boundary is SQL-tested), hosted schema cache/Storage, provider workers, and the exact release journey remain unverified. No remote service was changed and no email was sent.
