# D08-T01 — Task reader Server Function boundary

- **Outcome / owner / date:** D08-T01; current implementation task; 2026-09-08. This repairs the live Task reader boundary under Work/Review’s [legacy consumer closure](../plans/features/2026-08-25-work-review-activity-notifications.md#legacy-consumer-closure-from-the-september-review) and existing [secured Task reader](../plans/features/2026-08-25-work-review-activity-notifications.md#completed-prerequisite-records).
- **Base and tested code:** base `c6b38cc`; implementation commit `b60016d`. The three-file candidate was preserved dirty work identified by the handoff and was reconciled before ownership. Unrelated Team, concept, design-system, proxy, approval, and ledger changes were excluded.
- **Developer evidence:** `TaskReaderError` moved from the file-level `'use server'` action module to a plain shared server module. All five live Task readers throw that same class and the `/tasks` server page retains its distinct list, detail/history, and comments failure states. RPCs, tenant authority, queries, limits, copy, and UI are unchanged. The focused Task model suite passes 5/5; TypeScript, scoped ESLint, diff checks, and the Next 16.2.9 webpack production build pass. The emitted Server Function manifest contains the async Task actions and no class export.
- **Independent QA:** not required for this narrow framework export-boundary repair. It changes no authorization, persistence, async state, source identity, command semantics, or user-visible behavior; broader D08 workflow QA remains required for later Review work.
- **Coordinator check:** read the installed Next 16 `use server` guide, inspected every `TaskReaderError` caller and the complete three-file diff, and confirmed the production build accepts the actual action manifest. The client workspace retains its existing generic retry/error path; the server page still distinguishes known reader failures without converting them to empty success.
- **Integration / deployment:** locally committed as `b60016d`; not deployed. No browser/RPC failure injection or release-environment acceptance was performed.
- **Remaining work:** this closes only D08-T01. D08 remains `planned`: the approved Task current-state consumer is intact, while the material Review producer/resolver/UI workflow remains blocked on `WORK-REVIEW-CONCEPT-2026-09-01` and its later end-to-end acceptance.
