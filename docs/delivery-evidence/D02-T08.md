# D02-T08 — Retire legacy document classification writes

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `06e930b`

**State:** review; independent QA and clean migration replay remain unverified.

The legacy `setDocumentClass` action now returns an explicit unavailable result before database-client construction. Migration `00131_document_classification_update_authority` removes authenticated/service direct `UPDATE` authority over `document_class` and `document_category`. This preserves the approved lifecycle contract: demotion requires an impact preview, reasoned confirmation, archival rather than deletion of procedural relationships and derived facts, and scoped reevaluation; promotion likewise enters governed relationship/deadline/financial evaluation. The former action instead deleted links before its document update and could leave partial state.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: rollback-only `document_classification_update_authority.sql`; local error-level schema lint; the 131-version migration check; the scoped eight-test document action suite; `npm run typecheck`; `git diff --check`; and both webpack and default Turbopack production builds. The privilege-only migration does not change generated TypeScript schema shape. Changed-file ESLint reports only the pre-existing `any` diagnostic at `src/lib/actions/document.ts:38`.

Coordinator review inspected the staged diff, verified classification fails before document/link access, and confirmed the destructive link deletion and direct class update are removed. Independent QA is pending because no independent reviewer was authorized. Deployment is not verified.

The disposable database still cannot clean-reset under Supabase CLI 2.111.0 (`LegacyDbBootstrapError`). Migration `00131` was applied explicitly on the locally current additive schema and verified with rollback-only SQL. This closes only D02-T08; the future governed classification workflow, legacy review/status writes, live note-moderation reason UX, independent QA, clean replay, and release acceptance remain.
