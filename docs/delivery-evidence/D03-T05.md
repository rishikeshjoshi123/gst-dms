# D03-T05 — Add the typed pilot release manifest

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `60036ae`

**State:** review; retention approval, deployment identity, independent QA, and remote release acceptance remain unverified.

The pilot contract now has one typed, repository-owned manifest rather than prose-only release scope. It records the public, workspace, and development-only route families; the four tenant role envelopes; invite-bound verified-email entry and one-current-organisation rule; enabled and disabled AI capabilities; enabled, disabled, and approval-blocked workers; the PDF-only 25 MiB file limit; the 350 MiB pilot organisation unique-asset envelope; pre-production, production, and local evaluation data policies; and the exact environment-variable names that must supply release environment, authorised organisation, and tested revision. No organisation ID, revision, credential, or confidential fixture is committed.

The shared executable policy now reads the manifest for Case Brief generation and public organisation creation, keeping the already-enforced action/worker/database boundaries aligned with the matrix. Automatic retention and permanent purge are explicitly `approval_required`, with their three workers listed separately. This makes the unresolved `PILOT-RETENTION-SCOPE-2026-09-08` decision machine-readable without choosing an activation policy by assumption; the manifest is descriptive/server policy input, not a browser authorization grant.

Developer evidence passed on Node 24 / Next 16.2.9: four manifest tests verify settled entry/AI boundaries, the exact unresolved retention set, worker classification, PDF/storage limits, data handling, and deployment-identity inputs. The combined signup, organisation, Wiki policy, and manifest suites pass 14 tests. Changed-file ESLint, TypeScript, whitespace validation, and both webpack and default Turbopack production builds pass. No schema or live environment changed.

Coordinator review inspected every manifest cell against the approved pilot plan and current worker inventory. Independent QA was unavailable under the single-writer instruction. The implementation is locally committed as `60036ae`; it is not deployed. D03's currently settled local implementation boundaries are recorded, but the outcome remains in review until retention is decided, the manifest receives real deployment identity, clean-browser/email and worker checks pass, independent QA completes, and the exact release revision is accepted remotely.
