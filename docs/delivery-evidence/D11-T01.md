# D11-T01 — Truthful overdue deadline attention

- **Outcome / owner / date:** D11-T01; current implementation task; 2026-09-08. This tranche closes the enabled dashboard attention defect under Deadlines and Financials’ [truthful existing attention and reminder behavior](../plans/features/2026-08-26-deadlines-and-financials.md#truthful-existing-attention-and-reminder-behavior).
- **Base and tested code:** base `7e90b06`; implementation commit `af2a18e`. Unrelated approval, Team/concept, design-system, portal, proxy, ledger, and handoff changes were preserved and excluded.
- **Developer evidence:** migration `00136` adds an authenticated, exact-current-membership operational-timezone reader with no caller-supplied organisation ID. The dashboard reader now returns a bounded discriminated payload, includes unresolved overdue dates, retains non-deleted Matter and tenant scoping, excludes resolved rows, and orders oldest due first. Pure date-key logic labels overdue/today/future values against the owning organisation date without browser-timezone parsing. The card says `Deadline attention`, provides truthful empty/unavailable states, uses equal-width textual status badges, and makes no reminder, verification, assignment, or lifecycle claim.
- **Independent QA:** Sol/high read-only QA inspected the actual uncommitted migration, action, Server Component, client component, generated type, helper, and tests. Its authenticated rollback fixture selected older-overdue, yesterday-overdue, and future rows in order; a limit of two retained the two oldest; resolved, deleted-Matter, and cross-organisation rows were excluded. Anonymous, suspended, removed, absent, duplicate-current, and service-role callers received no timezone authority. QA initially found misleading unavailable copy and unequal repeated badge widths; the coordinator repaired both, and the same QA passed the final diff.
- **Coordinator check:** clean disposable local replay through all 136 migrations passed twice (the second removed a temporary browser fixture). The authority SQL fixture, error-level schema lint, migration inventory/refiner tests, 7/7 date-boundary tests, deterministic generated RPC type, Node 24 TypeScript, changed-file/changed-line lint, diff checks, and the Next 16.2.9 webpack production build passed. Adversarial coverage includes opposite sides of the international date line, leap/month/year rollovers, impossible dates/timezones, duplicate membership corruption, and cross-tenant rows.
- **Browser evidence / gap:** the live local login page and Server Action were reached, but the browser controller did not retain values in the React action form, so no authenticated dashboard render was established. Therefore 320 px, 200% zoom, light/dark, keyboard/screen-reader, and touch-scroll browser acceptance remains unverified; source inspection and production compilation are not recorded as browser proof. The temporary local user/data were removed by a clean reset and the development server was stopped.
- **Integration / deployment:** locally committed as `af2a18e`; not deployed. No remote database, reminder worker, notification delivery, or release environment was changed.
- **Remaining work:** this closes only D11’s enabled dashboard overdue/date-boundary correction. D11 remains `planned`: lazy active-section reads, URL state, pagination/true-count work, the canonical deadline lifecycle, and authenticated responsive acceptance remain. The placeholder reminder worker is still not a delivery implementation.
