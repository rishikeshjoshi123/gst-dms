# D07-T02 — Case Brief compatibility source links

- **Outcome / owner / date:** D07-T02; current implementation task; 2026-09-08. This closes the enabled readable CaseWiki/Case Brief Markdown compatibility-link audit under [Matter Notes and Cited Case Brief](../plans/features/2026-08-25-notes-and-case-brief.md#existing-notes-and-source-integrity-repairs).
- **Base and tested code:** base `1834760`; implementation commit `9661c50`. Only the compatibility classifier, renderer, caller replacement, and focused tests changed. All unrelated approval, Team/concept, design-system, portal, proxy, ledger, handoff, and plan edits were preserved.
- **Developer evidence:** a pure source-link classifier recognizes only strict root-relative canonical or legacy document paths. Complete version/page pairs canonicalize to the shared immutable Workbench route; read-only Trash links retain the server-supplied Matter lineage. Missing, incomplete, malformed, repeated, hash-only, traversal, encoded-separator, double-encoded, backslash, and extra-path document-like links preserve their authored label but render noninteractively as `Source location unverified`. External, mail, and ordinary non-document links remain under ReactMarkdown's safe URL policy; `javascript:` and `data:` URLs remain blocked. The readable compatibility UI and pilot-disabled generation boundary are unchanged.
- **Independent QA:** Sol/high inspected the actual unstaged helper, rendered ReactMarkdown behavior, live Matter/Trash composition, canonical route, and D07-T01 foundation. Its first review found a double-encoded separator/traversal bypass. The coordinator rejected residual percent escapes after decoding and added adversarial cases; the same reviewer then passed the exact repaired candidate with no material findings. It explicitly found active Matter query scoping unnecessary for this bounded compatibility contract because canonical navigation still binds the immutable version to the organisation-authorized exact document; Trash continues to require Matter lineage.
- **Coordinator check:** 29/29 Node 24 compatibility-render, source-classifier, canonical-route, pilot-policy, and Trash tests passed. TypeScript, scoped lint, diff checks, and the Next 16.2.9 webpack production build passed. The renderer test verifies visible author text, non-link ambiguity state, descriptive exact action, focus classes, effective 44 px height, safe ordinary links, and unsafe-scheme removal.
- **Browser evidence / gap:** no authenticated interactive acceptance was established. Keyboard/accessibility-tree behavior, visible focus, 320 px wrapping, 200% zoom, dark mode, and active/Trash navigation remain unverified in a browser; server rendering and source tests are not browser evidence.
- **Integration / deployment:** locally committed as `9661c50`; not deployed. No database, persisted wiki content, provider, generation job, remote environment, or external service changed.
- **Remaining work:** D07 remains in review for authenticated browser acceptance, region/highlight behavior beyond the enabled text selection adapter, D06 source prerequisites, and release-environment verification. The compatibility link audit is complete; Case Brief generation and the post-pilot structured Brief remain deliberately disabled/out of this tranche.
