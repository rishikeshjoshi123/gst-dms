# D08-T08 — source-grounded possible-duplicate Review

- **Outcome / date / boundary:** 2026-09-15. When exactly two different current private PDF assets carry the same active human-verified, source-grounded, namespaced official self-identifier, one typed `possible_duplicate` Review shows both exact quoted source pages and current document/version identities. An authorised lawyer records a reasoned `distinct_documents` or `possible_same_document` interpretation. Both logical documents, PDF bytes, effective facts and filings stay separate; no automatic hard block, merge, deletion, move, replacement or provider inference occurs. Hub exact duplicate §§170–178, Review §§112–128 and the September 14 first-release journey govern.
- **Dependencies / code:** D09-T05 verified document key `e7f918c` intentionally permits the same exact tuple across different documents and makes ambiguous reference targets opaque; D08-T05/T06 typed Review `7cb8cc3`/`d1f04b1`, D08-T07 concurrent safety `811b85b`, D06 exact Workbench, D13-T03 source-unavailable `9f6baf1` and existing server-byte hash upload fence are ancestors. Scoped implementation `71c7244` adds migration `00170` with private immutable per-document snapshots and decisions, exact two-source/current-hash eligibility, stale/key/source/asset/lifecycle hooks, one typed queue/detail/resolver and same-pair negative decision memory; generated types, server action/model, shared Review composition and disposable SQL/Storage/Chromium runners. The new resolver changes no document placement or source record. Three or more same-key documents, including an unavailable/Copy extra source, **abstain** and close an obsolete two-source item; they are not handled by this packet and remain release acceptance.
- **Writer / coordinator observations:** Sol/medium sole writer captured exit 0 for 170 migration replay, zero error-level database lint, deterministic repeated generated types, focused `--sql-duplicate` rollback fixtures for two-source production, active two→three closure, fresh pair, same-hash fence, normal/foreign/Viewer/inactive/ambiguous role opacity, stale/key-conflict/replay and both non-mutating typed decisions with one Activity each. A first real-Storage browser run failed because the client detail schema rejected irrelevant `source_analysis_run_id:null`; authenticated queue/detail RPC and queue shape worked. The typed detail projection omitted that irrelevant nullable field instead of weakening the schema. Final local `--probe-duplicate` Viewer RPC/Zod passed, and isolated production-webpack `--browser-duplicate` exited 0: two distinct synthetic one-page private PDFs were uploaded, downloaded and checked against database byte hashes, both exact Workbench version/page-1 canvases rendered without source error, Viewer mobile-dark was read-only, Owner mobile-dark/desktop-light keyboard completed both interpretation decisions with truthful closed wording. Focused 7/7 model/action tests, TypeScript, scoped lint and diff hygiene passed. Only exclusively owned disposable stack/build/Storage fixtures were used and removed; ordinary local stack/live `.next` untouched.
- **Independent QA / remaining:** Terra/high exact `71c72448c30b7ffcd49173e9b7b3e0e5310aff6b` read-only QA found no P0–P3. An initial sandboxed SQL attempt exited 1 on Docker socket permission and was not accepted; approved scoped rerun `--sql-duplicate` exited 0 with stderr empty, clean lint and all `DO` assertions. Separate `--browser-duplicate` exited 0 with stderr empty, two private byte/hash-verified synthetic PDFs and Chromium 1/1 in 42.5s, with visible page-1 canvas for each distinct exact version link. TypeScript, touched lint and diff checks exit 0; migration 3/3, generated/refined type 4/4 and focused Review-action 1/1 passed. Owned stacks removed, ordinary local services untouched. Deliberate 200%-zoom and vertical stable-header scrolling were **unrun** beyond 320px dark/no-horizontal-overflow, keyboard tabs and mobile/desktop checks. No representative possible-duplicate precision/Review-rate or broad filename/fuzzy/embedding calibration is established, and a three-way shared-key collision has no grouped Review. Ordinary acquisition/extraction still requires D12 independently adjudicated real corpus and separately authorised paid Mumbai quality; D13 named operators, compliant host, tested database-plus-private-Storage recovery, incident response and exact deployed acceptance still block confidential upload. No paid provider, real email, production data, remote service or deployment changed.
