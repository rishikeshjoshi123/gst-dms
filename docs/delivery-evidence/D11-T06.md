# D11-T06 — effective relationship core

- **Outcome / owner / date:** D11-T06; current implementation task; 2026-09-12. This tranche implements the Matter plan's [Timeline relationship authority](../plans/features/2026-08-25-matter-workspace-and-procedural-timeline.md#timeline-philosophy-and-source-data), Hub [procedural-relationship model](../plans/features/2026-08-25-document-hub-ingestion-and-workbench.md#reference-and-procedural-relationship-overhaul), and their scoped relationship acceptance. The observable result is that the selected live chronology inspector reads canonical effective relationships without inferring from legacy `document_links`; it is the prerequisite for the next graph tranche.
- **Base and tested code:** base `6cfeb256b2a845dfe8d276ee5ffafe94dc5b1d5a`; implementation commit and exact QA revision `44e741c6ed2befae37aa51341224a82dcbd5034e`. The commit contains migration `00146`, its rollback fixture/runner registration, deterministic generated types, the narrow server reader and fail-closed shape, and the live Relationships inspector. Existing instruction, decision, plan, ledger and handoff changes were preserved and excluded.
- **Developer evidence:** a clean disposable local reset replayed all 146 migrations. Node 24 `npm run acceptance:db` passed every registered SQL fixture, onboarding concurrency, seed and private-Storage verification. The focused relationship fixture proved Owner/Admin/Associate activation, Viewer/suspended/foreign denial, active same-matter proceeding endpoints, separate types, duplicate/inverse/cycle/CAS rejection, reason-required archive, idempotent replay, append-only history, private tables, catalogue phrases, projection exclusion and exact Activity/outbox/no-partial-write counts. Local type generation/refinement, 4/4 refiner tests, 26/26 focused tests, TypeScript, scoped ESLint, migration checks, diff checks and the provider-neutralized Next 16.2.9 webpack build passed.
- **Independent QA:** Sol/high reviewer `/root/relationship_qa` inspected exact `44e741c`, repeated the clean Node 24 database acceptance, found zero error-level database-lint results, matched regenerated types byte-for-byte, and passed 9/9 migration/type/runtime plus 22/22 focused Timeline/UI tests, TypeScript, scoped ESLint, migration/diff checks and the webpack build. An independent two-actor race returned `ok` then `cycle_detected`, leaving exactly one relationship, decision, receipt, Activity and outbox event. Viewer selection returned one authorised edge; suspended/foreign selection returned unavailable/empty; an authorised unlinked selection returned empty. No P0–P3 finding remained.
- **Coordinator check:** inspected the complete scoped diff and real selected-inspector caller. A direct rollback-only adversarial fixture rerun passed the tenant, endpoint, inverse, cycle, archive, replay, projection and atomicity matrix. Legacy links remain readable only for old compatibility code and are neither backfilled nor consulted by the effective projection.
- **Integration / deployment:** locally integrated in `44e741c`; not deployed. No remote schema, provider, email or client data changed.
- **Remaining work:** this closes only D11-T06. Authenticated browser rendering, physical accessibility, representative scale, Realtime, candidate/AI matching, Review resolution, legacy-link disposition/cutover, D09 identity/matching and deployment remain unverified or later work. The next ready graph scope is the LR deterministic layout and read-only graph consumer over this projection; manual relationship UI remains separate and must preserve the Review gate.
