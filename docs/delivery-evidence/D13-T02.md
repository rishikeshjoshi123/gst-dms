# D13-T02 — Local source-grounded successful extraction journey

- **Outcome / date / authority:** D13-T02; 2026-09-15. A fresh email-confirmed Owner completing the existing synthetic release journey now sees a successful, source-cited metadata extraction conflict in typed Review, chooses one printed candidate, reopens the exact immutable PDF page/version in Workbench and continues into the Matter. The already-tested terminal provider-failure/manual-recovery and global-placement paths remain separate and truthful. This is local integration evidence, not ordinary paid OCR/model quality.
- **Base and code:** D11-T12 evidence `2b5e558`; implementation `f2d1465`. The packet extends D13-T01's owned confirmation-required isolated `dms-release-journey-162` runner/SQL/browser without changing production commands, schema, UI, remote services or a second acceptance framework. D08-T02's extraction-conflict Review and current `00165` finisher/provenance contracts are necessary ancestors. A separately generated four-page synthetic PDF visibly prints `Document type OIO` on page 1 and `Document type SCN` on page 2; its generator yields byte-identical output and leaves the original shared PDF unchanged.
- **Source and decision assertions:** Browser native-page acquisition accepts all four pages and transmits the acquired text only to the exclusively owned fixture process. The test compares the current conflict version's server-observed asset SHA-256 with the exact uploaded bytes, including its suffix. The trusted *no-call* worker precondition records one clean eligible OIO candidate, proves it becomes automatically effective without gratuitous Review, then records a distinct eligible SCN candidate from the same immutable source and proves precisely one material extraction-conflict item. Both quoted spans have exact page/character anchors checked against acquired page artifacts. Product browser Review opens the exact page-2 PDF, chooses SCN with a reason, and closes; final SQL checks one accepted effective field, two append-only field decisions, one Review decision and one Activity event. Actor/key replay must be idempotent and a different-key post-closure stale decision must fail closed. Three exact private-Storage upload/Intake/version/assignment chains, chronology, governed graph and verified manual deadline remain asserted. Synthetic `proceeding` classification for the graph endpoint is a documented fixture precondition, not proof of automatic classification or an invented relationship.
- **Verification / independent QA:** The writer's fourth isolated full 165-migration replay/error-level database lint/deterministic types, isolated production-webpack build and stateful Chromium 1/1 (55.4 seconds) passed, including typed extraction decision, recovery, placement, Workbench, chronology/graph, deadline and 320px dark/keyboard checks. Early browser stops corrected an exact quoted-text locator, third graph endpoint fixture classification and a stale pre-D11-T12 deadline heading; those failures were not product passes. The subsequent exact hash and decision replay guards passed TypeScript, scoped lint, JS syntax and diff hygiene but were not in that writer run. Independent Terra/high exact-commit QA found no P0–P3 at `f2d1465`, checked deterministic fixture regeneration/original PDF preservation, Node 24 runner tests 8/8, TypeScript, touched lint and syntax. Its isolated full runner returned only an initial banner plus eventual cleanup without retained terminal output, so the coordinator separately observed one exact pinned-Node-24 run: 165 migrations, zero error-level lint, deterministic generated/refined types, production-webpack Chromium 1/1 in about 1.1 minutes, and final SQL checks including hash/replay/stale source assertions before exclusively owned stack cleanup. The first sandboxed Docker attempt was denied; scoped escalation succeeded. Ordinary local stack/live `.next` were not touched.
- **Still open:** D12 independently adjudicated 60–100-page real corpus, bounded paid Mumbai Google Document AI/`gemini-2.5-flash` accuracy/regional/latency/cost gates, calibrated possible-duplicate producer, actual provider failure/success throughput, representative scale, physical AT and hosted TUS/Storage/schema are not established. D13 still requires named go-live/support/rollback/incident owners, compliant host, tested database-plus-private-Storage recovery, monitoring and exact deployed confidential-data rehearsal. No paid provider, real email, remote/production data or deployment changed.
