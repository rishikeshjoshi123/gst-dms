# D06-T03 — Add truthful foreground fallback freshness

**Owner task / date:** current implementation coordinator, 2026-09-08

**Base / implementation revision:** `1b54d18` / `5c6fd4d`

**State:** review; local fallback contract passed, while selective Realtime, authenticated browser timing, independent QA and deployment acceptance remain unverified.

Discovery found no live Document Hub subscription or approved private Broadcast emitter in the current application. This tranche therefore does not add a broad table subscription or claim `Live`. The Hub now presents the truthful last successful authoritative-fetch age, prefixed with `Offline` when applicable, and explains that live updates are unavailable. The former manual `Refresh queue` control was removed in line with the approved freshness contract.

While the page is foregrounded, one owned fallback loop reconciles the canonical queue every 55–65 seconds. Focus, foreground return, and network reconnection trigger an immediate fetch. Hidden pages clear the timer; offline pages retain the last successful snapshot without issuing network reads. Cleanup removes every listener and timer, and callback changes cannot accumulate duplicate loops. Successful empty snapshots and genuine failures continue through D06-T01's explicit queue-result boundary, so freshness advances only after an authoritative success.

Developer evidence passed on Node 24 / Next 16.2.9: five focused freshness/queue/source tests cover age/offline labels, jittered cadence, foreground gating, focus/online/offline listeners, successful-empty replacement, failure preservation and source fencing. TypeScript, changed-file ESLint, whitespace validation, and the webpack production build pass. Coordinator review inspected timer ownership, visibility/network branches, result reconciliation, selection closure and the absence of a false `Live` label.

Independent QA is pending under sequential single-writer execution. Authenticated browser timing was not exercised because no disposable signed-in fixture was available. This tranche is the approved fallback, not the selective Realtime implementation: private topic policy, allowlisted event emitters, singleton connection management, post-subscription reconciliation, instrumentation, quota/refusal behavior and feature-flag rollback remain unimplemented. D06 also retains pagination/true-count and remaining viewer/live-consumer acceptance.
