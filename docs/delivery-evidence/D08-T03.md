# D08-T03 — Terminal extraction processing recovery Review

- **Outcome / owner / date:** D08-T03; current implementation task; 2026-09-15. A terminal current-PDF extraction failure now creates one typed, high-priority `processing_recovery` Review item. Owner, Admin and Associate can verify five minimum document facts against the exact source and continue manually; Viewer remains read-only. Canonical acceptance comes from the Work/Review supported-recovery contract, the Hub failure/manual-recovery path and the AI exceptional-fallback policy.
- **Base and tested code:** implementation commit `e6f8f9d39e13184d3072c171c79420a6491fffc0`, followed by test-only repair `3eed22bf45703d98e5eec7d8542ebaf53da3cd42`, based on D03-T08 evidence commit `9cf298c`. Migration `00157`, generated database types, Review model/action/UI, Dashboard projection and registered SQL/concurrency/browser fixtures are included.
- **Delivered authority and lifecycle:** `invalid_model_output`, `provider_failed` and domain-invalid `review_required` outcomes deduplicate against the exact source-analysis run and expose only safe reason, impact and immutable version/page identity. The typed resolver requires document type, reference number, date, direction, issuer and a reason. It authenticates through the canonical Review actor, fences tenant, capability, current version, document lifecycle, source/run identity and Trash availability, serializes the document boundary and binds actor idempotency. One transaction appends five explicit `manual_recovery` candidates without provider-evidence claims, accepted field decisions, the Review decision, effective-metadata recomputation, matching recovery-state clearance and one Activity/outbox effect. Source replacement closes obsolete work. No extraction retry or provider call is offered or enqueued because the repository has no safe extraction-reprocess worker.
- **Developer evidence:** Node 24 isolated acceptance replayed all 157 migrations, returned no error-level database lint, generated/refined database types twice with exact parity, and passed extraction-conflict regression, processing-recovery RLS/replay/lifecycle, concurrent two-actor recovery, source replacement and both producer/resolver race orders. The recovery concurrency result was one closure, five manual facts and one Activity event. TypeScript, scoped lint, Review model 4/4, migration/type checks 7/7 and diff checks passed. The first browser attempt exposed a test-only stale navigation assumption after the Decision tab closed; no product mutation assertion failed. The writer repaired the script to return to Evidence and reacquire the source link.
- **Independent QA / coordinator:** Luna/high QA inspected exact `e6f8f9d`, passed migration/type/model/TypeScript/scoped-lint and the complete isolated SQL/concurrency/race suite, and found no P0–P3 issue. Coordinator source review confirmed append-only manual facts, atomic effects, source/role fences and no retry/provider path. A coordinator browser run reproduced the test-navigation defect. After repair, the writer and independent QA each passed the single 360px recovery journey against the production webpack bundle (36.8s and 37.0s): typed manual decision, closure, Evidence-tab exact-version/page link, visible effective metadata and private PDF in Workbench. QA accepted exact `3eed22b` with no P0–P3.
- **Isolation / deployment:** all tests used synthetic data and exclusively owned local `dms-review-153` stacks on `553xx` ports; stacks, temporary workdirs and an orphaned local acceptance server were stopped/removed. No remote schema/Storage, paid provider, deployment, production data or real email was used. Hosted/browser/provider-quality, physical assistive-technology and representative-performance acceptance remain unverified.
- **Remaining work:** this closes the exceptional terminal-extraction manual-recovery Review packet, not ordinary acquisition/extraction quality, ambiguous placement/duplicate Review, relationship/deadline Review or the deployed release journey. The next ready mandatory journey packet should close typed placement decision and canonical placement through exact Workbench/Matter reopening before conditional work.
