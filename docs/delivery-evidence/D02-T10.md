# D02-T10 — Close direct document updates

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `72bdd6e`

**State:** review; independent QA and clean migration replay remain unverified.

Migration `00133_document_update_authority_closure` drops the legacy document UPDATE policy and removes every table- and column-level `UPDATE` privilege from browser and service roles. Current application inventory contains no direct `documents.update` caller after D02-T06 through T09. Governed SECURITY DEFINER commands retain document materialization, immutable inspector correction, processing transitions, Trash/Restore, and matter financial-year propagation.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: rollback-only `document_update_authority_closure.sql` checks every document column plus policy/table grants; the full canonical `document_materialization_commands.sql`, `current_document_inspector_correction.sql`, and `matter_mutation_authority.sql` suites pass after the revoke. Local error-level schema lint, the 133-version migration check, application source inventory, `npm run typecheck`, `git diff --check`, and both webpack and default Turbopack production builds also passed. The privilege-only migration does not change generated TypeScript schema shape.

Coordinator review inspected the staged migration/test, verified no direct application updater remains, and exercised the decisive canonical commands that still update document state as database-owned transitions. Independent QA is pending because no independent reviewer was authorized. Deployment is not verified.

The disposable database still cannot clean-reset under Supabase CLI 2.111.0 (`LegacyDbBootstrapError`). Migration `00133` was applied explicitly on the locally current additive schema and verified with rollback-only SQL. This completes the direct document INSERT/UPDATE authority closure, not all of D02: live note-moderation reason UX, independent QA, clean replay, and release acceptance remain. Governed successor workflows for Move/Copy, classification, and typed Review stay under their owning outcomes.
