# D11-T03 — bounded supporting-files collection

- **Outcome / owner / date:** D11-T03; current implementation task; 2026-09-08. This tranche bounds the enabled Matter Files collection without claiming the still-unavailable complete shell-count, category, association, or Timeline contracts.
- **Base and implementation:** base/evidence parent `4b37aac`; implementation commit `bfb326b`. The commit contains only twelve scoped Files reader, route, caller, Workbench-return, UI, and focused test files. Existing blocker, ledger, handoff-top, concept, design-system, portal, platform-plan, and proxy changes were preserved.
- **Reader and authority evidence:** active Files now returns one typed page with an exact total, a default limit of 50 and maximum of 100, deterministic `created_at DESC, id DESC` ordering, `fetchedAt`, per-item lifecycle revisions, and an explicit `sourceRevision: null` because no collection revision exists. Page and separately queried off-page selection use the authenticated Supabase client and identical organisation, matter, supporting-class, active-state, and non-deleted fences. Browser rows exclude storage paths. Trash pages are produced only from the already-authorised exact snapshot and issue no active read.
- **Route and workflow evidence:** `filesOffset` and `filesLimit` are strictly normalized; malformed, repeated, unsafe, and overflow values fall back or clamp. Previous/Next links expose truthful ranges and totals, retain selected document, inspector, unrelated state, and repeated filters, and keep 44px targets inside the Files scroller. Selected files remain inspectable off-page without expanding the collection or disclosing inaccessible IDs. Both Files-to-Workbench links carry a generated same-matter return path; the Workbench validates it against the authorised document’s actual matter before using it for its breadcrumb and Back action, rejecting absolute, protocol-relative, cross-matter, hashed, malformed, and repeated values.
- **Independent QA:** separate Sol/high read-only QA reviewed the actual base-to-worktree diff and live caller. Its first pass failed the tranche because the Workbench returned to a bare Matter route and lost Files URL state. The coordinator added the validated same-matter return contract. The same reviewer then passed the repaired revision with no P0–P3 findings and reconfirmed count/page fences, ordering, off-page selection, non-disclosure, Trash isolation, ranges, query preservation, scroller ownership, touch targets, and safe projection.
- **Coordinator verification:** 50 focused canonical-document, Files paging/read/render/route, Matter shell, selection, and Trash tests pass with `node --import tsx --test`. TypeScript, scoped ESLint, and `git diff --check` pass. The final Next 16.2.9 webpack production build passes; it emits only the existing Supabase Node 20 deprecation warning. A decisive adversarial test rejects external and cross-matter return targets while preserving repeated same-matter query state.
- **Gaps / no-change statement:** the Mac remained locked, so authenticated browser reload/Back/Forward, keyboard/focus/screen-reader, phone/tablet/desktop/200% zoom, light/dark/reduced-motion, and scroll acceptance were not run. No authenticated disposable-database execution established live RLS/count/error behavior. Offset pages are deterministic individually but are not a cross-request snapshot, so concurrent inserts/deletes ahead of an offset can duplicate or skip an item. No migration, database, remote service, deployment, or production data changed.
- **Remaining work:** D11 still needs bounded canonical Timeline chronology, complete typed shell counts once their authoritative Notes/Deadlines/relationship projections exist, supporting category and evidence-association foundations, measured scale, selective freshness/Realtime, released delegated panels, and authenticated responsive acceptance. The existing Files cards are not claimed as the final canonical table/mobile-list redesign.
