# D04-T02 — direct-upload lifecycle and cancellation hardening

- **Outcome / owner / date:** D04-T02; current implementation task; 2026-09-08. This tranche closes the locally reproducible direct-upload accounting, response-loss, same-tab recovery, and cancellation defects without claiming deployed Storage or browser acceptance.
- **Base and implementation:** base/evidence parent `7a7a972`; implementation commit `5914aea`. The commit contains only migration `00138`, its rollback acceptance fixture and generated RPC types, the shared resumable transport/recovery boundary, all three live upload callers, and focused tests. Existing approval, ledger/handoff-top, design-system, concept, portal, platform-plan, and proxy changes were preserved.
- **Authority and accounting evidence:** every unobserved signed session reserves the private bucket's 50 MiB per-object ceiling rather than its forgeable declared size. Cancelled and expired unobserved assets retain that ceiling as physical-byte accounting. Completion and cancellation bind the locked session to exact actor, organisation, and original idempotency identity. Completion response loss reads an identity-bound committed receipt instead of allocating new work. Cancel/finalize/fail serialize to one terminal winner; cancelled sessions cannot obtain a replacement token or be rewritten by a later failure.
- **Transport and cleanup evidence:** exact file reselection in the same tab restores the durable idempotency/session identity without persisting PDF bytes, signed tokens, bucket names, or object paths. A behavioral deferred-lookup test proves cancellation prevents a later TUS `start()`, while abort failure cannot skip the authoritative cancel command. Cleanup never tombstones a terminal asset while a token could recreate it: eligibility waits until the CaseChain session expiry plus Supabase Storage's installed two-hour signed-upload lifetime and a five-minute margin. Until confirmed deletion, retained-byte accounting remains conservative.
- **Live workflow evidence:** dashboard, Inbox, and Matter upload surfaces all use the shared recovery identity and expose a visible labelled `Cancel` control with a minimum 44px target while transfer is active. A cancel result removes the queue entry; a completion that lost the cancel race returns the committed success/duplicate authority.
- **Independent QA:** separate Sol/high read-only QA inspected the complete base-to-worktree diff, installed TUS and Storage client behavior, the SQL commands, and all live callers. Its first pass found delayed-start, premature tombstone, cancel/fail rewrite, and cancelled-result defects; a second pass found the late token-mint horizon. After coordinator repairs, the reviewer returned PASS with no P0–P3 findings and explicitly reconfirmed all original and repair findings closed.
- **Coordinator verification:** 18 focused upload/recovery/lifecycle tests pass, including the decisive delayed-resume cancellation case. TypeScript, scoped ESLint, migration numbering (138 unique), migration-check tests (3), generated-type refinement tests (4), `git diff --check`, and the Next 16.2.9 webpack production build pass. The build emits only the existing Supabase Node 20 deprecation warning.
- **Gaps / remaining work:** the rollback SQL acceptance fixture was inspected but not executed because no disposable database was confirmed. The Mac remained locked, so authenticated browser/TUS cancellation, reload/reselection, keyboard/focus, responsive/zoom/dark-mode, and actual 25 MiB boundary checks were not run. No real Storage token-expiry boundary, remote service, migration deployment, or release acceptance was exercised. D04 remains in review for those environment checks; D05 may now treat the local D04 authority contract as implemented but still needs its own decision-complete first-attachment work and acceptance.
