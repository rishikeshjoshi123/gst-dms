# D15-T02 — Governed Team invitations

- **Outcome / owner / date:** D15-T02; current implementation task; 2026-09-15. An authorised Owner or Admin can administer organisation invitations from the first-class Team workspace: create Associate/Viewer invitations, with Admin invitations restricted to the Owner; inspect pending or terminal history; resend with a rotated seven-day link; and revoke. A matching verified account still joins only through the explicit onboarding acceptance action. This implements the invitation portion of the September 14 administration minimum; suspension remains separate.
- **Base and accepted code:** implementation `9e8786a5be022c6de24d46727e52481bed54af5c`, followed by delivery-retry repair `f7d50668a2fb394bf28d6504d692f5e735c7849f`, based on D11-T11 evidence commit `10f72e8`. Migration `00160`, generated types, Team UI/actions, Settings removal, loopback mail capture, isolated runner, SQL/concurrency fixtures and Chromium scenarios are included. D15-T01 supplies the tenant-safe Team directory and D03-T07 supplies verified create-or-join onboarding.
- **Authority and lifecycle:** canonical fixed-search-path commands derive exactly one active tenant membership, re-evaluate capabilities, restrict Admin invitations to the Owner, bind replay keys to request subjects, serialize organisation/address rate and duplicate decisions, and reject current active/suspended recipients without tenant disclosure. Resend supersedes the old selector hash and extends expiry; revoke/reject preserve canonical reason-bound replay and administration history. Direct invitation/audit/receipt table access, service-role invitation readers and unauthorised email history remain closed. The Team reader defaults to live pending invitations and exposes bounded terminal filters only to invitation administrators; forged Viewer/corrupt-context URLs normalize to Members.
- **Delivery and UI:** invitation mail is sent only after the governed command. Non-production delivery fails closed unless an explicit loopback capture endpoint is configured; the acceptance mailbox captures without logging selector values. Delivery finalization is checked before success. An ambiguous same-key create/resend replay performs no second send, cannot show success, refreshes Team and directs the administrator to a deliberate Resend, which rotates the uncertain prior link. Team owns Members/Invitations navigation, role explanations, visible actions, destructive confirmation, fixed badges, long email wrapping, responsive table/cards, focus return, dark mode, one deliberate scroller and effective 44px touch targets. Personal Settings no longer reads invitation history.
- **Developer/coordinator evidence:** clean isolated replay through all 160 migrations, parsed error-level database lint, authority/privacy/lifecycle/rate/replay SQL, real two-session same-address concurrency, deterministic generated/refined types, focused tests, migration/type/runtime checks, TypeScript, scoped lint and diff hygiene passed. Production webpack passed after one earlier bounded optimization stall. The exact repaired commit was then run through `run-team-invitations.mjs --types --browser`: database gates passed, generated types were byte-identical, and Chromium passed 3/3 for actual Owner create → loopback-captured link → resend/token rotation → revoke/history, 320px dark keyboard/focus/overflow/touch behavior, and forged Viewer denial. The disposable stack and data were removed.
- **Independent QA:** Terra/high QA found one P1 at `9e8786a`: a mail-send success followed by delivery-record failure could make a same-key retry falsely report success. The separate `f7d5066` repair makes ambiguous replays non-successful and non-sending with an explicit refresh/Resend recovery path. Re-QA of exact clean `f7d50668a2fb394bf28d6504d692f5e735c7849f` found no P0–P3; 14 focused tests, TypeScript, scoped lint and diff checks passed. Its sandbox lacked Node 24/Docker ownership, so the coordinator exact-commit packet run above supplies runtime evidence.
- **Exclusions / gates:** immediate suspension/reactivation, role changes, ownership transfer, removal/departure, organisation-settings redesign, expiry scheduling, production Resend/DNS, provider configuration, remote services, deployment and production data are excluded. D15 still requires shell-wide immediate Owner/Admin suspension of an ordinary member. D13 still requires provider-quality extraction, remaining typed Review, workers, named go-live/incident authority, compliant hosting, tested database/private-Storage recovery and exact deployed confidential-data rehearsal.
