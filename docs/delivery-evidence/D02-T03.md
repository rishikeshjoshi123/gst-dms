# D02-T03 — Tenant-safe member directory consumers

- **Outcome / owner / date:** D02-T03; current implementation task; 2026-09-08. This tranche implements Organisation [directory closure](../plans/platform/2026-08-26-organisation-administration.md#existing-caller-and-directory-closure), [profile privacy](../plans/platform/2026-08-26-organisation-administration.md#team-directory-and-member-profiles), and relevant [acceptance](../plans/platform/2026-08-26-organisation-administration.md#testing-and-acceptance-criteria): Notes, Matter and Activity no longer reconstruct identities from the global Auth-admin directory.
- **Base and tested code:** base `6f4b76c` with unrelated dirty concept, design-system, approval, proxy, Task-reader, and planning edits preserved. Implementation commit `975e0ff` contains only the safe projection extension, generated types/refinement, shared mapper, four consumers, and focused tests.
- **Developer evidence:** migration `00126` applied successfully and local type generation emits the added `user_id`; the type refiner now preserves nullable email/display-name/title truth. The rollback SQL fixture proves active Associate visibility is limited to active same-tenant members, only self email is returned, Admin receives authorised active/suspended emails, removed/foreign rows remain absent, suspended callers receive no directory, and service role cannot execute the projection. Repository search and focused source tests (2) find no `auth.admin.listUsers` in `src`; operational assignee options exclude Viewer and inactive members. Refiner tests (3), ordinary TypeScript, scoped ESLint, migration uniqueness (126), schema lint, whitespace, and both Next 16.2.9 builds pass under Node 24.
- **Independent QA:** pending. This privacy/migration tranche requires separate read-only review before D02 integration; the sequential task remained the only writer.
- **Coordinator check:** inspected the projection, complete scoped diff, and every former Auth-admin caller. The shared request-cached directory chooses display name, caller-authorised email, then a non-email ID fallback. Notes retain authorship without inventing removed-member email; Matter/Notes assignee lists expose only active operational members. Activity replaces both the Auth-admin lookup and service-role document enrichment, so forged or legacy metadata cannot resolve documents outside the caller’s RLS.
- **Integration / deployment:** locally committed as `975e0ff`; not deployed. Clean replay remains unverified because the Supabase reset inspection failure persists; additive apply, generated types, rollback SQL, schema lint and builds were verified.
- **Remaining work:** this closes D02-T03 only. D02 remains in review pending independent QA, remaining matter/document direct-write closure, live note-moderation reason UX, and clean replay. Historical removed authors without an immutable legacy snapshot deliberately render a neutral ID fallback rather than an email guess.
