# D13-T01 — Stateful local first-release journey rehearsal

- **Outcome / owner / date:** D13-T01; current implementation task; 2026-09-15. One reusable, stateful synthetic Chromium run now rehearses the approved mandatory first-release path in a single confirmation-required local stack. A new Owner signs up, verifies captured local email and explicitly creates the organisation; creates a Client and two lifecycle-active Matters; uploads distinct private PDFs from global and Matter origins; follows a truthful terminal processing/manual-recovery typed Review and trusted ambiguous global-Intake placement Review; reopens the exact PDF/version/page in Workbench and its Matter; inspects chronology, a truthful read-only graph and a manually verified legal deadline.
- **Base and accepted code:** initial integration `8ce7c11` based on D08-T04 evidence `9132c76`; signup/token/signal repair `de8281a`; exact URL, ownership and readiness repair `33b6625`. The packet adds `acceptance:release-journey`, an isolated Supabase/production-webpack runner, bounded database preparation/verification, browser configuration/spec and local acceptance documentation. It changes no production UI, schema or domain command.
- **Rehearsal boundary:** the browser uses product signup, Client/Matter, signed upload and typed decision actions. Only worker-owned validation and a no-call `provider_failed` outcome are prepared as synthetic preconditions; the first-release UI cannot author the proceeding-class relationship endpoints, so the fixture classifies them while the graph edge itself uses the governed relationship command. The service-only trusted placement producer receives structured candidate evidence without making an extraction/provider call. Confirmation mail is captured on loopback, its token-bearing verify URL is checked against the exact owned Auth origin/path/query before request, and the callback is checked before browser navigation. No Owner is seeded or silently confirmed.
- **Database and browser evidence:** Node 24 clean isolated replay covered all 162 migrations, zero error-level database lint and two byte-identical generated/refined type runs. The production-webpack Chromium journey checked both private-Storage upload origins and queue rows, typed recovery/placement closure, exact Workbench PDF/version/page and Matter return, desktop chronology/graph, manually verified deadline details and 320px dark keyboard/touch/overflow. Final SQL asserted verified Owner/tenant/role, active Client/Matter lineage, private bucket/object/hash/page validation, intended/global Intake assignment, exactly two canonical document/version/assignment chains, exact Review/manual candidates and placement decision/Activity effects without duplication. Focused Node 24 boundary/readiness/runner tests 8/8, TypeScript, scoped lint, migration/type/runtime tests and diff hygiene passed.
- **Independent QA / repair:** Terra/high QA on `8ce7c11` encountered intermittent signup failure in a fresh stack and found missing signal cleanup and pre-request token-URL validation. An unchanged clean run passed, so the exact earlier Auth cause was not observed or claimed. The repair introduced a per-run Owner address, strict Auth/mail readiness, safe non-content signup diagnostics and real SIGINT/SIGTERM cleanup. QA on `de8281a` then found extra callback parameters accepted, a fixed-profile concurrent-run collision and overbroad 4xx readiness. The second repair closed those. A simultaneous-run probe refused the second process without stopping the first, and SIGINT cleaned its exact stack/lock/temp. Final independent exact-commit QA passed `33b6625` with no P0–P3 and independently reran the entire journey (Chromium 1/1 in 50.7s).
- **Isolation and open release gate:** `dms-release-journey-162` uses loopback ports 56220–56224 and app 3113, an atomic owned-profile lock and temporary build copy; all containers, locks, Playwright output and temporary workspaces were removed. The ordinary local `dms` stack and user-owned `.next` process were untouched. This proves synthetic local integration only. D12 acquisition/extraction corpus/provider quality, possible-duplicate calibrated producer, relationship suggestions, reminders, real email, representative load, physical assistive technology, compliant host, database-plus-private-Storage recovery, named incident/go-live owners, monitoring/rollback and exact deployed confidential-data acceptance remain open. No remote service, paid provider, production data or deployment changed.
