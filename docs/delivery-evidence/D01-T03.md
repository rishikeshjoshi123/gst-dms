# D01-T03 — Node 24 and Trigger runtime alignment

- **Outcome / owner / date:** D01-T03; current implementation task; 2026-09-08. This tranche implements Platform Operations’ [Trigger.dev Node runtime alignment](../plans/platform/2026-08-27-platform-operations.md#deferred-release-prerequisite-triggerdev-node-runtime-alignment) without deploying or promoting a worker.
- **Base and tested code:** base `b34255e` with unrelated dirty concept, design-system, approval, proxy, and Task-reader edits preserved. Implementation commit `2531dde` contains the runtime/package contract, CI versions, Trigger config/imports, and focused test.
- **Developer evidence:** `.nvmrc` and package engines pin Node 24; `packageManager` pins npm 11.6.0; Node types and lockfile use `@types/node` 24.13.3. Both repository workflows now select Node 24. `trigger.config.ts` loads through the installed SDK with runtime `node-24`, and active application/worker imports use the current `@trigger.dev/sdk` root export while preserving task IDs, payloads, retries, schedules, leases, and version locking. A clean `npm ci` succeeds under installed Node 24.8.0/npm 11.6.0.
- **Focused verification:** `npm run test:runtime-contract` passes two runtime/import assertions. Six focused worker/outbox suites pass 29 tests covering safe outcome mapping, missing-object behavior, idempotency, event validation, lease delivery/failure, bounded draining, tenant-derived processing identity, scoped reprocess fencing, wake failure, and relationship/page-text placement. Full TypeScript, scoped ESLint, and whitespace checks pass. Loading the actual Trigger config returns `node-24`.
- **Independent QA:** pending. This is an asynchronous worker/release boundary and requires a separate reviewer before D01 integration; the current task remained the single active writer.
- **Coordinator/build check:** inspected all active Trigger SDK imports and the complete scoped diff. Under Node 24, network-enabled Next 16.2.9 webpack and default Turbopack builds both compile, type-check, generate all 23 static pages, and complete route/build tracing without Node-engine warnings. At the time of this check, the inactive historical `patch_inbox.js` helper had an old import string and was not a runtime caller; that one-off helper was later removed.
- **Integration / deployment:** locally committed as `2531dde`; Trigger preview deployment, task execution, promotion, and rollback are not verified and were not attempted because remote consequential actions are outside current authorization.
- **Remaining work:** D01 remains in review pending independent QA, disposable database replay/SQL checks, broad lint classification, and exact preview/release rehearsal. This tranche closes the local runtime-alignment prerequisite only.
