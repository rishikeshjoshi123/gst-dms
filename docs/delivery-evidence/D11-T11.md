# D11-T11 — Governed manually verified legal deadlines

- **Outcome / owner / date:** D11-T11; current implementation task; 2026-09-15. In an active Matter, Owner, Admin and Associate can add a human-checked legal deadline, amend it with a reason, and record `Satisfied` or `Cancelled` without losing history. Viewer has a bounded read-only agenda. Date-only conditions are derived in the organisation operational timezone and shown independently as `Upcoming`, `Due soon`, `Due today` or `Missed`; an open past deadline remains missed until an explicit outcome.
- **Base and accepted code:** implementation `d7f7365d3c9b7ea6eb20dec3e2a5b6f78ff36992`, followed by QA repair `2d8a488f6254050c73e0235aaaafb812ed46d08c`, based on D06-T14 evidence commit `d7ef28f`. Migration `00159`, generated database types, canonical actions/readers, Matter agenda UI, isolated acceptance runner and SQL/concurrency/browser fixtures are included. D11-T01/T02/T04 supply timezone attention, lazy Matter shell and authoritative chronology foundations.
- **Authority and data behavior:** manual rows begin `verified` with an explicit human basis. Fixed-search-path `SECURITY DEFINER` commands derive exactly one current active membership, enforce active same-tenant Client/Matter lineage and the settled Owner/Admin/Associate mutation policy, serialize actor-bound idempotency and optimistic revision, and append immutable content versions/outcomes/receipts. Direct authenticated/anonymous deadline/history reads and writes are revoked; bounded Matter and Dashboard-attention RPCs are the only application readers. Replays converge, mismatched keys and stale revisions fail closed, and competing amendments produce one winner. Legacy nullable, blank, control-character and overlong descriptions normalize deterministically before constraints, remain `legacy`/`provisional`, and receive no fabricated verification provenance. Current amended basis round-trips correctly; history uses tenant-safe member display labels without email or UUID leakage.
- **UI and verification:** the former unavailable Matter section is now a compact verified-manual agenda with desktop semantic table, 320px list/detail presentation, shared dialogs, keyboard focus restoration, 44px touch controls, semantic statuses, one deliberate scroller and no page overflow. It makes no reminder or monitoring-delivery claim. One Sol/medium writer completed implementation and two bounded repair rounds. Clean isolated replay applied all 159 migrations; error-level database lint, adversarial legacy backfill, role/lineage/direct-authority/replay/date-boundary SQL, concurrency, deterministic generated/refined types, 19 focused tests, migration/type/runtime checks, TypeScript, scoped lint and diff hygiene passed. The post-repair production webpack server and Chromium acceptance passed 2/2 in 39.7 seconds, covering 320px keyboard create → Missed → amend → Upcoming → satisfy/history, desktop continuation, Viewer denial, dark mode and overflow.
- **Independent QA:** Terra/high QA found two P1 defects and one P2 harness weakness at `d7f7365`: unsafe legacy backfill, retained broad direct SELECT authority and non-fatal DB lint. After the separate repair commit, it found no P0–P3 at exact clean `2d8a488`; 12 focused tests, TypeScript, scoped lint, nine migration/type/runtime tests, syntax and diff checks passed. Its sandbox returned only startup output for the isolated browser attempt before cleanup, so the writer's post-repair runtime pass above remains the browser evidence.
- **Exclusions / gates:** extracted/provisional deadline candidates and typed Review, scheduled events, assignees/subscribers/reminders/email, organisation `/deadlines`, Tasks, Financials, provider work, Realtime and deployment are excluded. The current Activity/outbox registry has no approved deadline subject/envelope, so immutable deadline history ships without inventing an unsafe event; registry integration remains open. Confidential deployment still requires named ownership, compliant hosting, tested database/private-Storage recovery, provider/configuration acceptance, monitoring and exact deployed rehearsal. No remote service, production data, paid provider or real email was used.
