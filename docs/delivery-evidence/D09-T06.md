# D09-T06 — Governed Matter boundary Move/Copy repair

- **Outcome / owner / date:** D09-T06; current implementation task; 2026-09-13. An authorised Owner, Admin or Associate can use the existing assigned-document workbench action to preview the exact impact, record a reason and either move one logical document to another active Matter or intentionally create a lineage-linked copy. Viewer and inactive/ambiguous/foreign memberships remain denied, and neither path silently creates, splits or merges a Matter. Canonical acceptance is the Hub boundary-repair and Lifecycle Move/Copy contract under the September 11 Matter-identity decision.
- **Base and tested code:** implementation commit `b5c6305`, authority/focus repair `a19cea6`, and accepted Move-navigation repair `24e19a78ab7751fd34f6281ab83312552adc977e`, based on accepted D09-T05 evidence `ef42ce0`. Migration `00152`, generated types, registered SQL/fault/concurrency/Search fixtures, the Server Action and existing Matter document dialog are included. Existing instruction, decision/plan, proxy, portal, design-system and concept changes remained outside all three implementation commits.
- **Delivered authority and behavior:** preview and execution share document-scoped dependency fences, source/target revisions and a deterministic fingerprint. Execution requires a bounded reason and idempotency key, is atomic under concurrency and records content-minimised private receipts plus exact Activity/outbox effects. Move retains the logical document and immutable versions while rehoming compatible notes, exact citations, deadlines and tasks; active identity obligations, incompatible dependencies and Trash/current-source conflicts block it explicitly. Copy creates a distinct logical document/version, reuses the organisation-local immutable asset and base analysis, records document/page-artifact/field-decision provenance, preserves accepted/corrected/rejected/cleared human outcomes, and queues the retained page text for Search without new OCR/provider work. Revoked identifier history remains immutable without permanently blocking a later governed Move. The UI hides the action without a server-derived contribution capability, provides a two-stage impact/reason flow with bounded scrolling and pending guards, restores focus after Copy/Cancel/Escape, and redirects a completed Move to the same document under the authoritative target Matter with destination-workbench focus.
- **Developer and coordinator evidence:** clean Node 24 acceptance replay applied all 152 migrations and passed every registered fixture, private Storage seed, D09-T06 authority/tenant/rollback/idempotency/lifecycle/provenance/Search cases, the existing forty D09-T05 reference races, Matter-identity races and onboarding races. Database error lint, generated/refined type parity (`53583660b9008cae463700bcc4728781fda5237ba6da61adb4ed9610ff026907`), TypeScript, scoped tests/lint/diff checks and an isolated provider-neutral Next 16.2.9 webpack build passed. Architecture review first rejected lost effective decisions, non-indexable copied text, revoked-identity blocking and global table locks; the same writer repaired all four before the implementation commit. Coordinator testing reproduced only the unchanged stale financial-year synchronization assertion, not a tranche regression.
- **Independent QA:** Sol/medium QA rejected `b5c6305` for Viewer launcher exposure and missing close-focus restoration, then rejected `a19cea6` because a successful Move refreshed the obsolete source-Matter URL into a 404. Separate repair commits addressed each cycle. Fresh QA accepted exact `24e19a7` with no P0–P3 finding after reset/reseed, 13/13 focused checks, typecheck, scoped lint/diff, a 22/22-page production build and authenticated browser tests at 1440×900 and 320×700 dark. Live Copy and Move each produced exactly one receipt; Copy produced one new document/Search intent, while Move used replace navigation to the validated target route and Back did not revive the obsolete URL. Supplemental true Chromium 200% zoom (DPR 1→2; 735×375 effective viewport) proved the populated impact body independently scrollable without horizontal overflow, the 44px reason field and anchored confirmation reachable, and Escape/keyboard Cancel focus restoration.
- **Integration / deployment:** locally integrated at `24e19a7`. No deployment, hosted schema/Storage mutation, provider call, remote service, production data or real email occurred.
- **Remaining work:** this closes D09-T06 and the local D10 Move/Copy replacement boundary, not all D09/D10 release acceptance. Physical assistive-technology review, representative-load/p95 measurement, scheduled Search-worker drain, hosted environment/provider checks and deployment remain open. Automatic/bulk placement or split/merge, fuzzy matching, procedural relationship generation, replacement UI and live typed Review remain excluded. D08-T02, the first canonical typed extraction Review producer/resolver and secured queue slice under the approved Review concept, is the next ready pilot candidate.
