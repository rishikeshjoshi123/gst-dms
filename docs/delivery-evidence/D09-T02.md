# D09-T02 — Matter creation and Restore identity cutover

- **Outcome / owner / date:** D09-T02; current implementation task; 2026-09-12. This tranche applies the [September 11 Matter-identity decision](../decision-history/2026-09-11-matter-identity-and-extraction-normalization.md) to governed manual creation, the legacy placement fallback and [Trash Restore](../plans/platform/2026-08-24-resource-trash-retention-and-purge.md#restoration). Distinct proceedings for one client/year can coexist and restore without treating financial year as identity; organisation Matter codes and D09-T01 verified external keys remain reserved across active and Trash.
- **Base and tested code:** base/evidence `36efe1f`; implementation `2263954`; allocator repair and exact accepted revision `e41e65f`. The commits contain migration `00148`, rollback and concurrency fixtures, final-state D09-T01 fixture adaptation, fixture-runner registration, manual-only assignment compatibility, and corrected creation/Restore messages. Existing instruction, decision, plan, ledger, handoff, proxy and concept work stayed outside the commits.
- **Developer evidence:** clean Node 24 database acceptance replayed all 148 migrations, registered fixtures, both concurrency harnesses, seed and private Storage. Sequential and simultaneous Owner/Associate same-client/year creations produced distinct Matter IDs and codes with one receipt, legacy Activity row, canonical Activity and projector-outbox effect per request. The fixture proves code numbering gaps are not reused; active/Trash codes remain held; one Matter retains two current document financial-year facts without a fabricated multi-year Matter value; same-client/year Restore preserves IDs, code, evidence, descendants and effects; and verified external keys remain held in Trash. Rollback-only corrupt-state probes drive both public preflight and Restore for Matter-code and verified-key collisions, returning the same safe blocker with one blocked receipt, unchanged trashed resources and no restore effects. Existing role, tenant, replay/CAS, direct-DML, parent/content/expiry/purge-state protections remain covered. Client/FY lookup is active-only, deterministic, bounded to 20 manual suggestions and never auto-assigns; exact-reference behavior remains unchanged. Focused tests passed 13/13, migration tests 3/3, refiner tests 4/4, generated types matched byte-for-byte, TypeScript/lint/diff checks passed, and the provider-neutral webpack build passed.
- **Independent QA:** `/root/relationship_qa` rejected `2263954` after proving that fixed-width `lpad` truncated suffix 100 to 10 and wildcard `LIKE` let an underscore abbreviation scan other prefixes. Repair `e41e65f` uses literal-prefix comparison and minimum-width, non-truncating formatting. Independent probes returned `RCC-2728-100` with suffixes 10/99 held and `BC_-2627-01` despite alphanumeric decoys. Re-QA repeated clean database acceptance, concurrency, types, tests, lint, build and structural Restore comparison; no P0–P3 finding remained.
- **Integration / deployment:** locally integrated at `e41e65f`; not deployed. Fixtures used disposable local data only. No remote service, hosted database or email changed.
- **Remaining work:** this closes D09-T02 only. A next D09 normalization tranche should establish typed tax-period/field and self/outbound-reference candidate contracts before bidirectional exact matching. Placement-engine policy/storage, OCR/provider work, targeted reevaluation, split/copy repair, Copy/Move, Review UI and deployment remain separate; no creation-time identifier was invented because verified activation still requires current source evidence.
