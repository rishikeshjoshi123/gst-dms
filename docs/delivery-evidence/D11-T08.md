# D11-T08 — authenticated Matter graph browser acceptance

- **Outcome / owner / date:** D11-T08; current implementation task; 2026-09-12. This tranche closes D11-T07's local authenticated browser gap by extending the established D01-T06 loopback-only acceptance runner. The observable result is a populated governed Matter graph for authorised desktop users, with chronology retained on constrained presentations and at true 200% Chromium page zoom.
- **Base and tested code:** base `bd351e5`; implementation and application-focus repair `b8148ab`; acceptance-helper cleanup repair and exact accepted revision `8ad0382`. The commits contain six acceptance/graph files. Existing instruction, decision, plan, ledger, handoff, proxy and concept work remained outside both commits.
- **Developer evidence:** the deterministic seed activates `issued_pursuant_to` through `activate_document_relationship`, then asserts the exact persisted active relationship, activation decision and command receipt, including actor, endpoints, type, lifecycle, revision, idempotency, result code, reason and fingerprint. A clean local reset replayed all 146 migrations and registered SQL fixtures. Focused graph browser checks passed 3/3 before repair and 4/4 after cleanup hardening; the full feature suite passed 12/12 before repair. Browser coverage observed the effective progression edge and canonical relationship list, an unlinked node, exact `184×120` geometry, Owner/Viewer access, foreign 404, keyboard node selection and Close focus restoration, filter/view/Back/Forward state, endpoint removal, scroll ownership, no page overflow, light/dark, reduced motion, 320px/900px zero graph-action fallback and true 200% Chromium reflow. Node 24 TypeScript, scoped ESLint, focused unit tests, upload verification, diff checks and the provider-neutral webpack build passed.
- **Independent QA:** Sol/high reviewer `/root/relationship_qa` first rejected `b8148ab` for exceptional-path leakage of the disposable Chromium extension/profile. At exact `8ad0382`, QA found no P0–P3 issues. Its isolated `npm run acceptance:local` passed a clean database reset/seed, 13/13 feature Chromium tests, upload authority verification and 2/2 confirmation-required onboarding checks. An independent database probe returned the expected relationship/receipt/decision chain and zero legacy-link dependence. QA also proved cleanup after invalid setup, page acquisition and rejecting context close; no temporary zoom directories remained, and a cross-port Chromium probe showed the extension was inert outside the exact origin. Typecheck, lint, webpack build and diff checks passed.
- **Coordinator check:** inspected the complete six-file change and three-file repair, verified the real canvas caller, and reran 20/20 graph/chronology/layout tests. The only application defect found was the missing graph `MatterTimelineFocusCommit`; the repair restores focus to the initiating node after closing the inspector.
- **Integration / deployment:** locally integrated at `8ad0382`; not deployed. All database/browser identities and relationships were disposable local fixtures. No remote service or real email changed.
- **Performance / remaining work:** successful local cold graph readiness ranged from 1.334–6.837 seconds and keyboard-to-inspector from 218–342 ms; these are development smoke observations, not representative p95. Production-scale FMP/interaction measurement, physical assistive-technology review and deployment remain open. Relationship mutation/Review, candidates/AI, D09, Realtime and legacy-link disposition remain separate. This closes D11-T08 only, not parent D11.
