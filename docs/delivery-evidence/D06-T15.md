# D06-T15 — Document Inbox / Upload Queue live naming

- **Outcome / owner / contract / date:** D06-T15, current implementation task, 2026-09-15. The first-release user enters the overall **Document Inbox** from live navigation/breadcrumbs and sees the processing-focused **Upload Queue** in the global or Matter-origin upload path. The settled [September 14 release decision](../decision-history/2026-09-14-first-production-release-scope.md), [workspace naming decision](../discovery/document-workspace-naming.md) and canonical [Hub vocabulary contract](../plans/features/2026-08-25-document-hub-ingestion-and-workbench.md#product-vocabulary-routes-and-ownership) govern the copy. `/documents`, `/review`, internal IDs and historical evidence were not renamed.
- **Base / implementation / coordinator:** implementation `df07272` on D12-T01 evidence `f7d8cf8`; bounded 320px/acceptance repair `b0769a0`; browser assertion commits `66b5e8d` and exact-heading locator repair `ff25482`. The coordinator inspected the complete live caller diff and preserved the route/Review boundary. Sidebar (collapsed/mobile accessible label included), fallback/live breadcrumbs, page metadata and existing Dashboard empty guidance use Document Inbox. A visible Upload Queue heading stays above ownership/Search/status/Upload PDFs controls in a stable queue pane; labelled search, area, table, empty state and mobile return use that view name. Matter-origin context retains a descriptive Return/Clear action and wraps at phone width. The source pane retains exact-PDF semantics and the same selection/scroll ownership. No reusable UI contract or data permission changed.
- **Developer evidence:** Terra/medium writer passed TypeScript, scoped changed-file ESLint and focused route/navigation/compatibility tests. Known pre-existing `DashboardContent.tsx` broad ESLint `any`/hook diagnostics remain unrelated to its one-line copy change, not a blanket waiver. The coordinator's exclusively owned no-env copy passed Node 24 Next 16 **webpack** production build, including `/documents` and `/review`; default Turbopack rejected an out-of-root `node_modules` symlink and sandboxed webpack initially could not fetch project Google Fonts, then the approved isolated font fetch built successfully. The temporary copy was removed; live `.next` was untouched.
- **Independent QA / browser:** Luna/high exact `df07272` QA found the old-label acceptance locator and a narrow Matter context row. Exact `b0769a0` re-QA closed both with no new P0–P3 and Node 24 focused tests 10/10, TypeScript and scoped lint. The coordinator extended the existing **owned disposable** stateful first-release Chromium rehearsal with actual link/heading/search/Matter actions, keyboard focus, dark 320px 44px targets and overflow checks. Its first run reached the authenticated Inbox but failed only from the browser locator matching the empty-state heading too; exact `ff25482` fixed that selector. The next run passed Chromium 1/1 in 58.3 seconds after replaying all 164 migrations, zero error-level database lint and byte-stable generated/refined types. It exercised fresh confirmed Owner, both signed upload origins, truthful manual recovery, typed placement, exact Workbench/Matter, chronology/graph and verified manual deadline. The runner cleaned its exclusively owned Supabase stack and data; final read-only QA on `ff25482` found no P0–P3 and did not duplicate the browser/build run.
- **Limits / next:** this integrates the named local UI packet, not real ordinary provider extraction, possible-duplicate/deadline Review, optional suggestions, hosted TUS/Storage, performance or deployed acceptance. D12 corpus/paid-provider evidence and D13 named go-live/rollback/incident owners, compliant host, tested database-plus-private-Storage recovery and exact confidential deployment rehearsal remain open. No remote service, paid provider, production data, real email or deployment was changed.
