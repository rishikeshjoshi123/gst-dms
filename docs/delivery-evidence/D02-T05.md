# D02-T05 — Govern document creation

**Owner task / date:** current implementation coordinator, 2026-09-08

**Revision:** `92cd27f`

**State:** review; independent QA and clean migration replay remain unverified.

This tranche closes direct `documents` insertion without claiming the remaining document-update boundary. Migration `00128_document_insert_authority` removes the legacy insert policy and table/column `INSERT` grants from browser and service roles. Existing capability-checked metadata and Intake materialization commands remain the only document-creation path. The legacy binary-copy branch in `reassignDocumentMatter` is disabled before client or Storage access until the approved atomic, asset-reusing Move/Copy workflow in [document lifecycle](../plans/platform/2026-08-24-document-record-and-file-lifecycle.md) is delivered; ordinary move remains unchanged for the next authority tranche.

Developer evidence passed on the local Node 24 / Next 16.2.9 checkout: `supabase/tests/document_insert_authority.sql` proves canonical Associate creation, Viewer and suspended denial, cross-tenant non-disclosure, and direct authenticated/service insert denial; the existing `document_materialization_commands.sql` passes after replacing stale capability-array coupling and legacy direct-trash setup with current public projections/commands. Local error-level schema lint, the 128-version migration check, the scoped seven-test document action suite, `npm run typecheck`, `git diff --check`, and both webpack and default Turbopack production builds passed. The migration changes privileges only, so generated TypeScript schema shape is unchanged.

Changed-file ESLint reports five existing diagnostics in untouched regions of `src/lib/actions/document.ts`; the changed test file is clean. The broader `src/lib/trash/exact-resource.test.ts` reaches and passes its document checks but retains one stale assertion expecting the pre-D02-T01 direct `deleteNote` implementation; that unrelated baseline coupling is not claimed as passing here.

Coordinator check inspected the staged diff and live caller: copy fails before privileged clients, no direct document insert/upload remains, canonical creation still works, and only the five named files were committed. Independent QA is pending because no independent reviewer was authorized. Deployment is not verified.

The disposable database still cannot clean-reset under Supabase CLI 2.111.0 (`LegacyDbBootstrapError`). Migration `00128` was applied explicitly on the locally current additive schema and verified with rollback-only SQL. This closes only D02-T05; document `UPDATE` authority, live note-moderation reason UX, independent QA, clean replay, and release acceptance remain.
