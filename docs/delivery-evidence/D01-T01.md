# D01-T01 — Deterministic nullable-RPC type refinement

- **Outcome / owner / date:** D01-T01; current implementation task; 2026-09-08. This prerequisite implements Platform Operations’ [generation/refinement requirement](../plans/platform/2026-08-27-platform-operations.md#development-usage-and-release-cutover) and the Pilot’s [release-blocker repair](../plans/operations/2026-08-29-design-partner-pilot-execution-sequence.md#0-reconcile-the-current-checkout-and-close-release-blockers).
- **Base and tested code:** base `0ae1bb6` with unrelated dirty concept, design-system, approval, proxy, and Task-reader edits preserved; implementation commit `2f46074` contains only the generation workflow, refiner/tests, checked-in generated types, and Task RPC type proof.
- **Developer evidence:** `npm run test:db:types` passes three deterministic tests covering all declared nullable RPC results, idempotence, missing-signature rejection, and unexpected-type rejection. `npm run typecheck` passes, clearing the Task fixture plus Task/provider RPC errors recorded in the incoming handoff. `npm run test:db:migrations` and `npm run db:migrations:check` pass with 123 unique migrations. Scoped ESLint and `git diff --check` pass. The workflow now installs dependencies, checks the checked-in contract before migration push, regenerates through an atomic wrapper, refines it, and checks consumers before committing generated types.
- **Independent QA:** pending. The repository requires an independent check because these types feed security-sensitive Task and provider-usage commands; the single active writer performed no parallel helper review.
- **Coordinator check:** inspected the complete scoped diff and the live `/tasks`, Task action, and document-processing provider-usage consumers. A direct failure-path probe confirmed that missing `SUPABASE_PROJECT_REF` fails before writing and leaves `database.types.ts` byte-identical. Refining the full checked-in file a second time is byte-identical. A drifted fixture fails closed rather than widening or erasing nullability.
- **Build and environment:** the default Next 16.2.9 Turbopack build remained in compilation and was stopped after a bounded wait. `next build --webpack` fails on the pre-existing application-to-worker import of native `@napi-rs/canvas`; sandboxed `fonts.googleapis.com` access also fails. Local Node 20.15.1 is below several installed packages’ declared engines. No local/remote Supabase regeneration or Docker SQL acceptance ran; this tranche changes no schema.
- **Integration / deployment:** locally committed as `2f46074`; deployment not verified.
- **Remaining work:** this closes only D01-T01. D01 remains in `review` until independent QA and the worker/native build boundary, Node/package-manager pin, database replay, broader lint baseline, and release checks are completed.
