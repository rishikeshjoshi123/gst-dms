import { defineConfig,devices } from '@playwright/test'
if(Number(process.versions.node.split('.')[0])!==24)throw new Error(`Local acceptance requires Node 24 exactly; received ${process.versions.node}.`)
export default defineConfig({testDir:'./tests/acceptance',testMatch:'member-suspension.spec.ts',fullyParallel:false,workers:1,retries:0,timeout:60_000,expect:{timeout:12_000},outputDir:'/tmp/casechain-member-suspension-playwright-results',reporter:[['line']],use:{baseURL:'http://127.0.0.1:3261',screenshot:'only-on-failure',trace:'retain-on-failure'},webServer:{command:`"${process.execPath}" scripts/acceptance/start-member-suspension-server.mjs`,url:'http://127.0.0.1:3261/login',reuseExistingServer:false,timeout:120_000},projects:[{name:'chromium',use:{...devices['Desktop Chrome']}}]})
